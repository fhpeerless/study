export const note1 = {
    title: "电脑解码文字的方式",
    contentmd: `
## 电脑解码文字的方式

计算机底层只能存储 **0 和 1**（二进制），要让计算机显示文字，就需要一套「编号→文字」的映射规则，这就是**编码**。

完整的解码流程：

\`\`\`
硬盘上的二进制数据 → UTF-8 解码 → Unicode 编号 → 字形渲染 → 屏幕上显示文字
\`\`\`

**举个例子：** 汉字「中」

| 阶段 | 值 |
|------|-----|
| 硬盘二进制 | \`11100100 10111000 10101101\` |
| UTF-8 解码后 | Unicode 编号 \`U+4E2D\` |
| 字形查找 | 显示为「中」 |

### CPU 在每一步做了什么？

| 步骤 | CPU 参与程度 | CPU 具体做了什么 |
|------|-------------|-----------------|
| ① 硬盘读取二进制 | ⭐ 低 | CPU 向磁盘控制器发出读取指令，之后由 **DMA（直接内存访问）** 将数据从硬盘搬运到内存，CPU 基本不参与传输过程 |
| ② UTF-8 解码 | ⭐⭐⭐ 高 | **CPU 核心工作**：逐字节读取 UTF-8 编码，判断字节类型（首字节/续字节），计算 Unicode 编号。例如 \`0xE4 0xB8 0xAD\` → 拆分有效位 → 拼接为 \`0x4E2D\` |
| ③ Unicode 编号存储 | ⭐⭐ 中 | CPU 将解码后的 Unicode 码点写入内存（分配空间、存储整数值） |
| ④ 字形渲染 | ⭐⭐⭐ 高 | CPU（或 GPU）根据 Unicode 编号在字体文件中查找对应的矢量轮廓/位图，计算像素位置，进行光栅化、抗锯齿等图形运算 |
| ⑤ 屏幕显示 | ⭐ 低 | CPU 将渲染好的像素数据写入显存（帧缓冲），之后由 **GPU/显示控制器** 按刷新率输出到屏幕，CPU 不再参与 |

> 💡 **关键发现：** 步骤②「UTF-8 解码」是 CPU 参与最多的纯计算环节，也是 **Go 比 Python 快的关键所在**。

### Unicode 与 UTF-8 的关系

- **Unicode** 是一个「字符集」，给世界上每个字符分配了一个唯一编号（如「中」= \`U+4E2D\`）
- **UTF-8** 是一种「编码方式」，规定如何把 Unicode 编号转换为二进制字节存储

> 💡 简单理解：Unicode 是「字典」，UTF-8 是「翻译规则」。Unicode 告诉你「中」的编号是 4E2D，UTF-8 告诉你 4E2D 在硬盘上存为 3 个字节 \`E4 B8 AD\`。
`,
    timestamp: "2026-04-29 18:00",
    embed: ``
};

export const note2 = {
    title: "Go语言的编码方式与字符串存储",
    contentmd: `
## Go 语言的编码方式

Go 语言的源代码文件**默认使用 UTF-8 编码**。这意味着：

- 字符串字面量直接就是 UTF-8 编码的字节序列
- 源码中可以直接写中文、日文、Emoji 等 Unicode 字符
- 无需额外声明编码方式

\`\`\`go copy
s := "你好Go"  // 这个字符串在内存中就是 UTF-8 编码的字节序列
\`\`\`

Go 语言的设计者（Ken Thompson 和 Rob Pike）正是 UTF-8 编码的发明者，所以 Go 对 UTF-8 的支持是**原生且彻底**的。

---

## Go 语言的字符串存储与显示

### 核心概念：字符串 = UTF-8 字节切片

Go 中字符串的本质是**只读的字节切片（byte slice）**，存储的就是 UTF-8 编码的原始字节：

\`\`\`
Go 字符串 = 硬盘里的 UTF-8 二进制，直接搬进内存
\`\`\`

用代码验证：

\`\`\`go copy
package main

import "fmt"

func main() {
    s := "中"
    
    // 字符串长度 = UTF-8 字节数，不是字符数！
    fmt.Println(len(s))  // 输出: 3（"中"的 UTF-8 编码占 3 个字节）
    
    // 打印每个字节
    fmt.Printf("%x\\n", []byte(s))  // 输出: [e4 b8 ad]
}
\`\`\`

### 字符串 vs rune

Go 对「字符」有两种视角：

| 类型 | 含义 | 示例 |
|------|------|------|
| \`byte\` (\`uint8\`) | UTF-8 编码的**一个字节** | 「中」= 3 个 byte |
| \`rune\` (\`int32\`) | 一个 **Unicode 码点**（一个字符） | 「中」= 1 个 rune |

\`\`\`go copy
s := "你好Go"

// 按字节遍历
for i := 0; i < len(s); i++ {
    fmt.Printf("字节索引%d: %x\\n", i, s[i])
}
// 输出8个字节: 你(3字节) + 好(3字节) + G(1字节) + o(1字节)

// 按字符遍历
for idx, ch := range s {
    fmt.Printf("字符索引%d: %c (U+%04X)\\n", idx, ch, ch)
}
// 输出4个字符: 你、好、G、o
\`\`\`

### 获取字符串的「真实字符数」

\`\`\`go copy
import "unicode/utf8"

s := "你好Go"
fmt.Println(len(s))           // 8（字节数）
fmt.Println(utf8.RuneCountInString(s))  // 4（字符数）
\`\`\`
`,
    timestamp: "2026-04-29 18:00",
    embed: ``
};

export const note3 = {
    title: "Go与Python字符串编码的区别",
    contentmd: `
## 与 Python 字符串编码的区别

这是 Go 和 Python 在字符串处理上的**核心差异**：

| 对比项 | Go | Python 3 |
|--------|-----|----------|
| **内存存储** | UTF-8 字节序列 | Unicode 码点序列（内部实现） |
| **\`len()\`** | 返回**字节数** | 返回**字符数** |
| **遍历方式** | \`for range\` 按字符，\`for i\` 按字节 | \`for ch in s\` 按字符 |
| **编码转换** | 通常不需要，本身就是 UTF-8 | \`str.encode()\` / \`bytes.decode()\` |
| **字符串不可变** | ✅ 不可变 | ✅ 不可变 |

### 具体对比

**Python 3：**

\`\`\`python
s = "你好Go"
print(len(s))    # 4（字符数）
print(s[0])      # 你（按字符索引）
print(s[1])      # 好

# 获取字节数需要编码
print(len(s.encode('utf-8')))  # 8（字节数）
\`\`\`

**Go：**

\`\`\`go copy
s := "你好Go"
fmt.Println(len(s))  // 8（字节数！不是字符数）

// 不能直接用索引取字符
fmt.Println(string(s[0]))  // 输出乱码！因为 s[0] 只是第一个字节

// 正确方式：按 rune 索引
runes := []rune(s)
fmt.Println(string(runes[0]))  // 你
fmt.Println(string(runes[1]))  // 好
\`\`\`

### 为什么设计不同？

- **Go 选择 UTF-8 字节序列**：字符串和文件 I/O 之间无需编码转换，性能更高，内存更紧凑。大多数网络协议和文件格式本身就是 UTF-8，Go 字符串可以直接读写，零拷贝。
- **Python 选择 Unicode 码点**：\`len()\` 返回字符数更直观，字符串索引按字符访问更方便，但需要在 I/O 时做编码/解码转换。

### Go 在编码层面比 Python 快在哪？

答案就在 **UTF-8 解码** 和 **Unicode 编号存储** 这两步：

\`\`\`
                    ┌──────────────────────────────────────────────┐
                    │   Go 和 Python 的分水岭就在这里              │
                    ▼                                              │
硬盘二进制 ──→ [UTF-8 解码] ──→ Unicode 编号 ──→ 字形渲染 ──→ 显示
                  │                    │
                  │  ┌─────────────────┘
                  │  │
                  │  ├─ Python：必须执行完整解码，将 UTF-8 字节转为 Unicode 码点存入内存
                  │  │   （CPU 需要逐字节解析、位运算、内存分配）
                  │  │
                  │  └─ Go：字符串本身就是 UTF-8 字节，很多时候跳过解码步骤！
                  │      （CPU 直接使用原始字节，零解码开销）
\`\`\`

| 场景 | Go（UTF-8 字节存储） | Python（Unicode 码点存储） |
|------|---------------------|--------------------------|
| **从硬盘读取字符串** | 字节直接作为字符串使用，**无需解码** | 必须调用 \`.decode('utf-8')\` 将字节解码为 Unicode |
| **字符串在内存中** | 保持 UTF-8 字节原样，**零额外 CPU 开销** | 每个字符展开为码点（1~4 字节），CPU 需要分配更大内存 |
| **写入硬盘/网络** | 直接写出字节，**无需编码** | 必须调用 \`.encode('utf-8')\` 将码点编码回字节 |
| **遍历字符** | \`for range\` 按需解码，**惰性处理** | 已是码点，直接访问 |

\`\`\`python
# Python：读取文件时 CPU 必须做完整解码
with open("data.txt", "r", encoding="utf-8") as f:
    text = f.read()
# CPU 工作：读取字节 → 逐字节 UTF-8 解码 → 为每个字符分配内存存码点
\`\`\`

\`\`\`go copy
// Go：读取文件时 CPU 几乎不做解码
data, _ := os.ReadFile("data.txt")
text := string(data)
// CPU 工作：读取字节 → 直接作为字符串使用（零解码）
\`\`\`

> 💡 **总结：** Go 之所以在字符串 I/O 场景更快，是因为它**跳过了 UTF-8 解码/编码步骤**。Go 字符串和硬盘上的存储格式一致（都是 UTF-8 字节），读进来就能用，写出去直接写；而 Python 必须在「字节世界」和「字符世界」之间来回转换，每次转换都要 CPU 做额外的解码/编码运算。
`,
    timestamp: "2026-04-29 18:00",
    embed: ``
};

export const note4 = {
    title: "编译型vs解释型：Go比Python快的根本原因",
    contentmd: `

---

## 打包后的 exe → CPU 执行的完整流程

这是你最关心的部分，我一步步拆开：

### 1. 用户双击 exe

操作系统（Windows/macOS）加载这个 exe 到内存。

### 2. 启动器运行

exe 里的小启动程序先跑，做两件事：
- 解压内置的 Python 解释器和你的代码
- 启动 Python 解释器

### 3. 解释器开始工作

和直接运行 \`python 脚本.py\` 完全一样：
- 读取你的代码
- 编译成字节码
- 交给 Python 虚拟机执行

### 4. 虚拟机 → 操作系统 → CPU

Python 虚拟机不能直接碰 CPU，它会：
- 调用**操作系统接口**（Windows API / Linux 系统调用）
- 操作系统把指令转成**机器码**
- 最后交给 CPU 执行运算、读写内存、显示窗口

### 最终链路（最精简版）

\`\`\`
打包exe → 解压出Python解释器 → 执行字节码 → 操作系统 → 机器码 → CPU
\`\`\`

> 💡 **关键理解：** 即使 Python 打包成了 exe，本质上还是「解释器 + 源码」的捆绑包，运行时依然要走完整的解释执行流程，和 Go 编译出的原生 exe 完全不同。

---

## Go 编译的 exe → CPU 执行的完整流程

和上面的 Python 打包 exe 对比，看看 Go 的 exe 有多直接：

### 1. 用户双击 exe

操作系统（Windows/macOS）加载这个 exe 到内存。

### 2. 操作系统直接加载机器码

exe 里装的就是**CPU 原生机器码**，操作系统做三件事：
- 读取 exe 文件头（PE/ELF 格式），确定代码段和数据段的位置
- 将机器码加载到内存的代码段（.text），数据加载到数据段（.data）
- 设置程序入口点（main 函数的内存地址）

### 3. CPU 从入口点直接取指执行

没有解释器，没有字节码，没有虚拟机：
- CPU 从 main 地址开始**取指 → 译码 → 执行 → 写回**，标准流水线
- 每一条指令都是 CPU 原生指令，直接执行
- 遇到函数调用，直接跳转到内存中的机器码地址

### 4. 需要系统调用时

Go 程序需要读写文件、打印输出时：
- 直接执行 \`syscall\` 指令（或调用操作系统 API）
- 操作系统接管，完成 I/O 操作后返回
- **没有中间层**，字符串直接以 UTF-8 字节传给内核

### 最终链路（最精简版）

\`\`\`
Go编译exe → 操作系统加载机器码 → CPU直接执行
\`\`\`

> 💡 **对比核心：** Python 打包 exe = 解压 + 解释器 + 字节码 + 操作系统 + CPU（5 层），Go 编译 exe = 操作系统 + CPU（2 层）。Go 砍掉了中间所有翻译环节，CPU 拿到的就是自己能直接执行的指令。

---

## 总结：Go 比 Python 快的两层原因

1. **根本原因**：Go 是编译型语言，CPU 直接执行机器码；Python 是解释型语言，CPU 要先跑解释器，解释器再翻译执行
2. **字符串层面的原因**：Go 字符串保持 UTF-8 字节原样，跳过了解码/编码步骤；Python 必须在字节和码点之间来回转换

> 编译型 vs 解释型是最大的性能差距来源，字符串编码设计是锦上添花。
`,
    timestamp: "2026-04-29 18:00",
    embed: ``
};

export default [note1, note2, note3, note4];
