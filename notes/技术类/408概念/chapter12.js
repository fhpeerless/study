export const note1 = {
    title: "操作系统（四）：文件系统与I/O设备管理",
    contentmd: `
# 操作系统 · 文件系统与I/O设备管理 · 408考试速通复习

> 定位：概念+高频考点速记。重点掌握：文件物理结构对比、目录结构、磁盘调度算法计算、位示图计算、SPOOLing原理、缓冲管理对比。

---

## 一、文件系统基础

### 1. 文件的概念

**文件**：以计算机硬盘为载体、存储在某种介质上、具有符号名的相关信息的集合。

- **文件属性**：文件名、类型、大小、创建/修改时间、创建者、保护信息等。
- **文件基本操作**：创建、打开、关闭、读、写、删除、截断。
- **打开文件**：将文件目录项（FCB）复制到内存的打开文件表中，避免每次访问都查目录。
- **打开文件表**：系统级（整个系统一张）+ 用户级（每个进程一张）。

### 2. 文件的逻辑结构

**逻辑结构**：从用户角度看到的文件组织形式（独立于物理存储）。

- **无结构文件（流式文件）**：由字符流组成，如文本文件。无内部结构。
- **有结构文件（记录式文件）**：由若干记录组成。

**有结构文件的组织方式：**

| 类型 | 特点 | 查找效率 |
|------|------|---------|
| **顺序文件** | 记录按关键字顺序排列；串结构（按存入顺序）和顺序结构（按关键字排序） | 顺序存取快；变长记录难随机存取 |
| **索引文件** | 为变长记录建立索引表（关键字+指针），索引表按关键字排序 | 支持随机存取，速度快；多占索引空间 |
| **索引顺序文件** | 记录分组，每组建立索引；组内有序/无序 | 比顺序文件快，比索引文件省空间 |
| **直接文件（散列文件）** | 通过哈希函数直接定位记录 | 查找极快；需处理冲突 |

### 3. 文件的物理结构（重点）

**物理结构**：文件在外存上的存储组织形式（分配方式）。

| 类型 | 分配方式 | 优点 | 缺点 |
|------|---------|------|------|
| **连续分配** | 文件占用连续磁盘块 | 顺序/随机存取快，目录项简单（起始块+长度） | 外部碎片；文件难以增长；需预先知道长度 |
| **链接分配** | 每个块含下一块指针 | 无外部碎片；文件可增长；动态分配 | 不支持随机存取（需顺序遍历）；指针占空间；可靠性差 |
| **链接分配-显式（FAT）** | 指针集中存于文件分配表FAT | 随机存取可（在FAT中遍历，FAT在内存） | FAT占用内存 |
| **索引分配** | 每个文件有索引表，记录所有块号 | 支持随机存取；无外部碎片 | 索引表占空间；小文件浪费；大文件需多级索引 |

**索引分配处理大文件（多级索引）：**
- **一级索引**：索引表存放直接块号。文件大小受限于索引表能容纳的块数。
- **两级索引**：一级索引指向二级索引表，再指向数据块。文件可更大。
- **多级索引 + 直接块（如UNIX inode）**：
  - 直接地址（直接块）：12 个，存小文件。
  - 一次间接：1 个，指向一级索引表。
  - 二次间接：1 个，指向二级索引表。
  - 三次间接：1 个，指向三级索引表。

> **计算题**：给定块大小、块号位数，计算最大文件长度。

---

## 二、文件目录

### 1. 目录的概念

**文件目录**：文件控制块 FCB 的有序集合，用于实现"按名存取"。

**文件控制块 FCB**：包含文件基本信息（文件名、物理位置、逻辑结构、物理结构、创建时间、访问权限等）。**FCB 中最重要的信息是文件名和物理位置**。

**索引节点 inode**：将文件描述信息与文件名分离，FCB 仅存文件名和指向 inode 的指针。
- **目的**：减少目录项大小，使更多目录项能放入内存，加快查找。
- **磁盘 inode**：完整文件信息，存储在磁盘上。
- **内存 inode**：打开文件时复制到内存，增加"打开计数"等字段。

**磁盘 inode 的详细内容（第4版强化）：**

每个文件在磁盘上对应一个唯一的 inode，inode 的编号 i-number 是文件的唯一标识。一个文件系统格式化后，inode 数量（即文件总数上限）就已确定。

inode 中包含的关键字段：
- **文件类型与访问权限**：普通文件、目录、链接等；rwx 权限位。
- **文件所有者信息**：拥有者 UID、所属组 GID。
- **文件大小**：以字节为单位的文件长度。
- **时间戳**：最后访问时间（atime）、最后修改时间（mtime）、inode 自身最后修改时间（ctime）。
- **链接计数（nlink）**：指向该 inode 的目录项数（硬链接数）。nlink=0 时文件被真正删除。
- **数据块指针**：指向文件数据块的指针数组。典型结构为：
  - 直接块指针（如 12 个）：直接指向数据块，适合小文件。
  - 一级间接块指针：指向一个索引块，索引块中存放数据块指针。
  - 二级间接块指针：指向一级索引块，一级再指向数据块。
  - 三级间接块指针：类似地多级间接寻址。
- **文件属性和标志**：如不可变标志、追加写标志等。

**inode 相关注意事项：**
- 文件系统格式化时确定 inode 总数 = 总磁盘空间 / (平均文件大小 + inode 开销)。若小文件极多，可能 inode 用完但磁盘空间未满。
- 目录本质上是一种特殊文件，其"数据块"中存放目录项（文件名 → inode 编号的映射）。
- 删除文件时：清除目录项 → inode 链接计数减 1 → 若 nlink=0 则释放 inode 和数据块，但**数据并未被擦除**（仅标记空闲），可被恢复。

### 2. 目录结构

| 结构 | 特点 | 优缺点 |
|------|------|--------|
| **单级目录** | 整个系统一张目录表 | 简单；不允许重名；查找慢 |
| **两级目录** | 主文件目录 MFD + 用户文件目录 UFD | 解决重名；不能分类；不能共享 |
| **树形目录** | 多级层次结构 | 层次清晰，便于分类管理；查找需路径，较慢 |
| **无环图目录** | 树形 + 共享子目录/文件（用链接） | 便于共享；需处理引用计数；不能有环 |

**路径名：**
- **绝对路径**：从根目录开始的完整路径。
- **相对路径**：从当前工作目录开始的路径。
- 当前目录（工作目录）：避免每次从根查找，加快速度。

**树形目录的查找：**
- 每访问一级目录需读入该目录文件（I/O操作）。
- 路径越长，I/O次数越多。**当前目录机制可减少I/O次数**。

---

## 三、文件存储空间管理

### 1. 外存空闲空间管理方法

| 方法 | 原理 | 特点 |
|------|------|------|
| **空闲表法** | 连续分配，记录每个空闲区的起始块和长度 | 适合连续分配；有外部碎片 |
| **空闲链法** | 将所有空闲块用链表串起来；有空闲盘块链和空闲盘区链 | 简单；链过长；不适合随机分配 |
| **位示图法（重点）** | 用一位表示一个盘块空闲(0)/占用(1) | 占空间小，可常驻内存；分配/回收快 |
| **成组链接法** | 空闲块成组，每组第一块记录本组块数和下一组指针（UNIX采用） | 适合大文件系统；管理高效 |

### 2. 位示图计算（重点）

**位示图（Bitmap）**：用一位（bit）对应一个磁盘块，0表示空闲，1表示已分配。

**计算要点：**
- 位示图大小 = 盘块数 / 8 字节（每位对应一块）。
- 已知块号 b，求其在位示图中的位置：
  - 字号 i = b / (字长 × 8)... 即 b / 字长位数（从0开始）
  - 位号 j = b % 字长位数
- 已知字号 i、位号 j，求块号：b = i × 字长位数 + j
- 分配时找空闲位(0)，置1；回收时置0。

> 注意题目中"字号、位号从0还是从1开始"，以及"盘块号从0还是从1开始"。

### 3. 成组链接法（重点，第4版强化）

**成组链接法**是 UNIX 系统采用的空闲盘块管理方法，结合了空闲表法和空闲链法的优点。

**核心思想：**
将空闲盘块分成若干组（如每组 100 个块），每组的**第一个盘块**作为"组长块"，其内容包含：
- 本组中所有空闲盘块的块号（栈形式存放）。
- 下一组的组长块号（栈底元素，即指针）。

**专用块（超级块中的空闲盘块号栈）：**
- 系统在内存中设一个"空闲盘块号栈"，常驻内存。
- 栈中存放第一组空闲块的块号，以及已入栈的块数 N。
- 栈大小为每组块数 + 1（多一个位置存放下一组指针）。

**空闲盘块分配步骤：**
1. 检查内存栈是否已空（N = 0？）。
2. 若 N > 0：栈顶出栈，分配该块；N = N - 1。
3. 若 N = 0 且栈底元素（下一组指针）≠ 0：将下一组组长块读入内存栈，覆盖当前栈内容，然后分配栈顶块。
4. 若 N = 0 且下一组指针 = 0：无空闲块，分配失败。

**空闲盘块回收步骤：**
1. 检查内存栈是否已满（N = 栈容量？）。
2. 若 N < 栈容量：将回收块号压入栈顶，N = N + 1。
3. 若 N = 栈容量（栈满）：将当前栈内容写入回收块（回收块成为新组长块），栈清空；在新栈中，栈底存放原第一组指针，然后将回收块号压入栈顶，N = 1。

**成组链接法优点：**
- 分配和回收的大部分操作在内存栈中完成，速度快。
- 不要求空闲块连续，适应离散分配。
- 兼具空闲表法的高效和空闲链法的灵活性。
- 空闲块号集中管理，利用率高。

**典型考题形式：**
- 给定每组块数和操作序列，模拟分配/回收过程，追踪内存栈的变化。
- 注意区分空闲盘块号栈中"栈顶"和"栈底"的位置约定（通常栈底存下一组指针）。

---

## 四、文件共享与保护

### 1. 文件共享

**文件共享**：多个用户/进程共同使用同一个文件。

**实现方式：**
- **基于索引节点的共享（硬链接）**：多个目录项指向同一 inode，inode 中有"链接计数"。删除一个链接只是计数减1，计数为0才真正删除。
  - 不能跨文件系统；不能链接目录（防止环）。
- **符号链接（软链接）**：创建一个新文件（Link 类型），内容为目标文件的路径。
  - 可跨文件系统；可链接目录；目标删除则失效；占额外空间。

### 2. 文件保护

**文件保护**：防止文件被破坏或未授权访问。

**访问类型：** 读、写、执行、删除、附加等。

**保护机制：**
- **口令保护**：文件设口令，简单但安全性低。
- **密码保护（加密）**：文件内容加密，安全性高，但需加解密开销。
- **访问控制列表 ACL（重点）**：每个文件关联一个 ACL，记录每个用户/用户组对该文件的访问权限。
  - 精细到每个用户。
  - 缺点：ACL 较长，开销大。
- **访问控制矩阵**：行=用户，列=文件，元素=权限。稀疏时浪费空间。
- **权限矩阵的简化**：按用户类型（owner、group、other）分组设置权限（如UNIX的 rwxrwxrwx）。

---

## 五、磁盘结构与管理

### 1. 磁盘物理结构

**磁盘**：由若干**盘片**组成，每个盘片有上下两个记录面，每个面有一个磁头。

- **磁道（Track）**：盘片上的同心圆。
- **柱面（Cylinder）**：所有盘片上相同半径的磁道组成的圆柱面。柱面号 = 磁道号。
- **扇区（Sector）**：磁道被划分为若干扇区，是磁盘读写的**最小单位**。
- **盘块**：OS虚拟的块单位，通常为扇区的整数倍。

**磁盘地址 = 柱面号 + 盘面号（磁头号）+ 扇区号**。

**磁盘访问时间 = 寻道时间 + 旋转延迟 + 传输时间：**
- **寻道时间 Ts**：磁头移动到目标柱面，**主要耗时**，机械运动。
- **旋转延迟 Tr**：目标扇区旋转到磁头下，与转速有关。平均旋转延迟 = 转一圈时间/2。
- **传输时间 Tt**：读写数据时间，与数据量、转速有关。

> **寻道时间是磁盘调度优化的主要目标**（机械运动最慢）。

### 2. 磁盘调度算法（重点）

#### (1) 先来先服务 FCFS

- 按请求到达顺序服务。
- 简单公平，但寻道距离长，性能差。

#### (2) 最短寻道时间优先 SSTF

- 选择距当前磁头最近的请求。
- 性能较好；但可能**饥饿**（远处请求长期得不到服务）。

#### (3) 电梯调度 SCAN（扫描算法/电梯算法）

- 磁头在当前方向上移动，服务沿途请求，到达最远请求后**反向**继续服务。
- 类似电梯运行。无饥饿；但两端请求等待时间长。
- 注意：SCAN 一定会走到磁盘**最边缘**（即使没有请求），再折返。

#### (4) 循环扫描 C-SCAN

- 磁头单向移动服务请求（如从内向外），到达边缘后**快速返回**到起点（不服务），再同向扫描。
- 对两端请求更公平；返回时花费时间。

#### (5) LOOK / C-LOOK

- **LOOK**：SCAN 的改进，磁头只需移动到**最远请求**位置即反向，不必到磁盘边缘。
- **C-LOOK**：C-SCAN 的改进，到达最远请求后快速返回到最近请求，再同向扫描。

**算法对比：**

| 算法 | 是否饥饿 | 特点 |
|------|---------|------|
| FCFS | 否 | 公平简单，寻道长 |
| SSTF | **可能** | 性能好但饥饿 |
| SCAN | 否 | 电梯算法，两端等待长 |
| C-SCAN | 否 | 单向扫描，两端公平 |
| LOOK | 否 | SCAN优化，不到边缘 |
| C-LOOK | 否 | C-SCAN优化 |

> **计算题**：给定磁头初始位置和请求序列，求各算法的寻道总距离。注意 SCAN/LOOK 需指定初始方向。

### 3. 磁盘的其他概念

- **磁盘格式化**：低级格式化（划分扇区）+ 逻辑格式化（创建文件系统）。
- **引导块**：存放引导程序，开机时由ROM引导程序加载执行。
- **坏块**：损坏的扇区，由OS或控制器标记并替换。

---

## 六、I/O 设备管理

### 1. I/O 设备的基本概念

**I/O 设备分类：**
- 按**使用特性**：存储设备（磁盘、磁带）、输入输出设备（键盘、显示器、打印机）。
- 按**传输速率**：低速（键盘）、中速（打印机）、高速（磁盘、网卡）。
- 按**信息交换单位**：
  - **块设备**：以数据块为单位传输，如磁盘。可寻址。
  - **字符设备**：以字符为单位传输，如键盘、打印机。不可寻址。

**I/O 控制方式（重点对比）：**

| 方式 | CPU 干预 | 数据传输单位 | 传输过程 | 效率 |
|------|---------|-------------|---------|------|
| **程序查询（轮询）** | 全程 | 字 | CPU不断查询设备状态 | 最低，CPU等待 |
| **程序中断** | 中断时 | 字 | 设备就绪发中断，CPU中断处理 | 较高，CPU与设备并行 |
| **DMA** | 开始和结束 | **块** | DMA控制器直接与内存交换数据 | 高，CPU仅在开始/结束干预 |
| **通道** | 开始和结束 | 一组块 | 通道执行通道程序，独立于CPU | 最高，CPU干预最少 |

**DMA（直接存储器存取）：**
- 数据传输单位是**块**。
- DMA 控制器直接控制内存与设备间的数据交换，**不经过 CPU**。
- CPU 仅在传输开始和结束时干预。
- DMA 方式下，CPU 和 I/O 设备**并行工作**。
- 周期窃取（挪用）：DMA 与 CPU 同时访问内存时，DMA 优先（或交替）。

**通道（Channel）：**
- 比 DMA 更高级，能执行**通道程序**。
- 通道类型：字节多路通道（低速多设备）、数组选择通道（高速单设备）、数组多路通道（高速多设备）。

### 2. 设备控制器

**设备控制器**：CPU 与 I/O 设备之间的接口，是硬件。

- 功能：接收 CPU 命令、控制设备、数据缓冲、差错控制。
- 包含：设备控制器与 CPU 接口（寄存器）、与设备接口、I/O 逻辑。
- **I/O 端口**：控制器中的寄存器，编址方式：
  - **统一编址（内存映射I/O）**：I/O 端口与内存统一编址，用相同指令访问。
  - **独立编址（I/O映射）**：I/O 端口独立编址，用专门的 I/O 指令（in/out）访问。

### 3. I/O 软件层次结构

从高到低：

| 层次 | 功能 |
|------|------|
| **用户层 I/O 软件** | 提供库函数、系统调用接口（如 printf、open） |
| **设备独立性软件** | 提供统一接口、设备名解析、缓冲管理、错误报告、设备分配 |
| **设备驱动程序** | 将上层请求转为设备控制命令，与硬件相关 |
| **中断处理程序** | 处理 I/O 完成中断 |
| **硬件** | 设备控制器、设备本身 |

> **设备驱动程序**是 I/O 系统中与硬件最相关的部分，每个设备一个驱动。

### 4. 设备分配

**设备分配的数据结构：**
- **系统设备表 SDT**：系统所有设备表。
- **设备控制表 DCT**：每个设备一张。
- **控制器控制表 COCT**：每个控制器一张。
- **通道控制表 CHCT**：每个通道一张。

**设备分配方式：**
- **静态分配**：进程运行前分配，运行结束释放。不会死锁，但利用率低。
- **动态分配**：进程运行中按需分配。利用率高，可能死锁。

**设备分配算法：** FCFS、优先级。一般不用复杂算法。

**设备独立性（设备无关性）**：应用程序使用的逻辑设备名与物理设备无关，由OS映射。提高设备分配灵活性和资源利用率。

**设备分配过程：** 分配设备 → 分配控制器 → 分配通道。三者都可用才能开始 I/O。

### 5. SPOOLing 假脱机技术（重点）

**SPOOLing（Simultaneous Peripheral Operations On-Line）**：用软件将独占设备"改造"为共享设备的技术。

**组成：**
1. **输入井/输出井**：磁盘上的两个区域，模拟低速设备。
   - 输入井：收容从输入设备来的数据。
   - 输出井：收容要输出到设备的数据。
2. **输入缓冲区/输出缓冲区**：内存中，暂存输入输出井与设备间数据。
3. **输入进程（预输入程序）SPi** 和 **输出进程（缓输出程序）SPo**：负责数据传输。
4. **井管理程序**：管理井中数据。

**工作原理（以打印机为例）：**
- 用户进程请求打印 → SPOOLing 将打印数据送入磁盘输出井 → 进程立即返回（不阻塞）。
- SPOOLing 缓输出进程在打印机空闲时，从输出井取出数据打印。
- 多个用户的打印请求在输出井排队，**共享一台打印机**。

**SPOOLing 特点：**
- 提高了 I/O 速度（从低速设备转到高速磁盘）。
- 将独占设备**改造为共享设备**。
- 实现了虚拟设备功能（一台设备虚拟为多台）。
- 进程不需等待慢速设备，提高并发度。

> **SPOOLing 是用软件实现的技术**，将独占设备变共享设备，本质是"用空间换时间"。

### 6. 缓冲管理（重点）

**缓冲**：在内存中开辟区域，暂存 I/O 数据，缓解 CPU 与设备速度不匹配问题。

**缓冲的引入目的：**
1. 缓和 CPU 与 I/O 设备间速度不匹配的矛盾。
2. 减少对 CPU 的中断频率，放宽中断响应时间。
3. 提高 CPU 与 I/O 设备的并行性。

**缓冲区分类：**

| 类型 | 数量 | 特点 | 平均处理一块时间 |
|------|------|------|----------------|
| **单缓冲** | 1 个 | 设备→缓冲区，CPU 从缓冲区取；两者串行 | max(T, C) + M |
| **双缓冲** | 2 个 | 一个接收，一个处理，交替使用 | max(T+C, M)；CPU与设备可并行 |
| **循环缓冲** | 多个（环形） | 多缓冲区组成循环队列，in指针和out指针 | 进一步提高并行性 |
| **缓冲池** | 多个 | 公共缓冲区，可被多进程共享 | 最高效，多进程共享 |

> **T** = 设备输入一块时间，**C** = CPU 处理一块时间，**M** = 缓冲区到工作区传输时间。

**单缓冲分析：**
- 假设缓冲区在内存，设备→缓冲区需 T，缓冲区→工作区需 M，CPU处理需 C。
- 处理一块的时间 ≈ max(T, C) + M。
- 设备与CPU不能完全并行。

**双缓冲分析：**
- 设备将数据放入缓冲区1，CPU 从缓冲区2 取数据处理。
- 处理一块时间 ≈ max(T+C, M)。
- 当 T+C < M 时，瓶颈是传输；通常 T 和 C 可重叠。

**缓冲区与快表的区分：** 缓冲区用于 I/O 与 CPU 间；快表 TLB 用于加速地址转换。

**硬件缓冲 vs 软件缓冲：**
- 硬件缓冲：由专用寄存器实现，成本高。
- 软件缓冲：由内存区域实现，成本低，常用。

**高速缓存 vs 缓冲：**
- **缓存（Cache）**：提高访问速度，存放**活跃**数据，缩短读取时间。
- **缓冲（Buffer）**：缓和速度不匹配，存放**正在传输**的数据，平滑传输。

---

## 七、虚拟化技术简介（第4版新增）

**虚拟化在 I/O 和文件系统中的应用：**

虚拟化技术（详见内存管理章节）在文件系统和 I/O 管理中应用广泛：

**存储虚拟化：**
- **虚拟磁盘（Virtual Disk）**：Hypervisor 为虚拟机提供虚拟磁盘，Guest OS 以为是真实物理磁盘。虚拟磁盘实际是宿主机上的一个文件（如 VMDK、QCOW2）。
- **精简置备（Thin Provisioning）**：虚拟磁盘文件初始很小，随 Guest OS 写入数据而动态增长，节省物理存储空间。
- **快照（Snapshot）**：保存虚拟机磁盘文件在某时刻的状态，可快速回滚。写时拷贝技术实现。

**I/O 设备虚拟化对文件系统的影响：**
- 虚拟机通过虚拟磁盘访问文件，Guest OS 的文件系统操作最终由 Hypervisor 翻译为宿主机的实际文件 I/O。
- 双重文件系统层：Guest OS 有 ext4/xfs 等文件系统，宿主机可能有 NTFS/ext4。每层带来额外开销。
- **virtio-blk / virtio-scsi**：半虚拟化块设备驱动，绕过模拟层，Guest OS 通过共享内存环与 Hypervisor 高效通信，大幅提升 I/O 性能。

**虚拟化环境中的 I/O 调度变化：**
- 传统 I/O 调度仅考虑单个 OS 内的进程，虚拟化下需考虑多虚拟机间的 I/O 公平性。
- Hypervisor 层需额外 I/O 调度，防止某个虚拟机独占磁盘 I/O 影响其他虚拟机。

**虚拟化与 SPOOLing 的对比：**
| 技术 | 层次 | 目的 | 方式 |
|------|------|------|------|
| SPOOLing | OS 内部 | 把独占设备变成共享设备 | 用磁盘缓冲，进程不直接操作低速设备 |
| 虚拟化 | 硬件之上 | 把物理硬件抽象为多份 | 通过 Hypervisor 模拟/直通硬件 |


## 八、易错点速记

- 文件**逻辑结构**面向用户（流式/记录式）；**物理结构**面向存储（连续/链接/索引）。
- **连续分配有外部碎片**；链接和索引分配无外部碎片但有内部碎片（最后一块）。
- **索引分配支持随机访问**；链接分配不支持（除显式FAT在内存可遍历）。
- **FCB 中最重要的是文件名和物理位置**；引入 inode 是为了分离文件名与描述信息，加快查找。
- **树形目录查找需多次 I/O**（每级目录一次）；当前目录可减少 I/O。
- **位示图**：1 bit 对应 1 块；计算字号位号注意起始编号和字长。
- **硬链接**（共享inode）不能跨文件系统；**软链接**（符号链接）可跨文件系统但目标删除失效。
- **ACL 是细粒度访问控制**，可针对每个用户设置权限。
- 磁盘**寻道时间**是机械运动，最慢，是调度优化目标。
- **SSTF 可能饥饿**，SCAN/C-SCAN 不会。
- **SCAN 走到磁盘边缘，LOOK 走到最远请求**；C-SCAN/C-LOOK 单向扫描。
- **DMA 传输单位是块**，CPU 仅在首尾干预；通道更高级，执行通道程序。
- **SPOOLing 把独占设备改造成共享设备**，用磁盘模拟设备，是"虚拟设备"技术。
- **SPOOLing 组成**：输入井/输出井（磁盘）+ 输入/输出缓冲区（内存）+ 输入/输出进程 + 井管理程序。
- **缓冲目的**：缓和速度不匹配、减少中断频率、提高并行性。
- **单缓冲处理一块时间 = max(T,C) + M**；双缓冲 ≈ max(T+C, M)。
- **设备驱动程序**是 I/O 软件中与硬件最相关的层次。
- **设备独立性**：用逻辑设备名请求设备，OS 映射到物理设备。
- 程序查询方式 CPU 与设备**串行**；中断、DMA、通道方式 CPU 与设备**并行**。
- 文件打开操作把 FCB/inode 调入内存的**打开文件表**。
- 打印机是独占设备，通过 SPOOLing 可被多进程"共享"。
- 索引顺序文件 = 索引文件 + 顺序文件，组间索引组内顺序。
- UNIX inode 采用**多级索引**：直接块 + 一/二/三次间接，兼顾小文件和大文件。
- **inode** 内容包含：类型权限、所有者、时间戳、链接计数、数据块指针（直接/间接）。
- **inode 链接计数（nlink）**：硬链接数，nlink=0 才真删除；**i-number** 是文件唯一标识。
- **成组链接法**：空闲块分组，组长块存本组块号和下组指针；内存有空闲盘块号栈，分配/回收类似栈操作。
- **虚拟磁盘**：Hypervisor 以文件形式为虚拟机提供磁盘（VMDK/QCOW2）；**精简置备**动态增长节省空间。
- **virtio** 是半虚拟化驱动，Guest OS 与 Hypervisor 通过共享内存环通信，I/O 性能接近物理机。`,
    timestamp: "2026-08-06 18:30",
    embed: ``
};

export default [note1];
