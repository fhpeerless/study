export const note1 = {
    title: "计算机组成原理（二）：指令系统与CPU数据通路",
    contentmd: `
# 计算机组成原理（二）· 指令系统与CPU数据通路

> 定位：408考研复习第二章。重点掌握：指令格式、寻址方式有效地址计算、CISC vs RISC对比、CPU内部寄存器组与数据通路、指令执行过程（取指/间址/执行/中断）、硬布线控制器与微程序控制器对比。

---

## 一、指令系统基础

### 1. 指令格式

**指令 = 操作码 OP + 地址码 A**

- **操作码字段**：指出指令该执行什么操作。
- **地址码字段**：给出被操作数的信息（地址或寄存器号）。

### 2. 按地址码个数分类

| 类型 | 格式 | 操作描述 |
|------|------|---------|
| 零地址指令 | OP | 操作数隐含（如堆栈操作、空操作NOP） |
| 一地址指令 | OP A1 | (ACC) OP (A1) → ACC，另一操作数隐含在累加器 |
| 二地址指令 | OP A1 A2 | (A1) OP (A2) → A1，结果存A1 |
| 三地址指令 | OP A1 A2 A3 | (A1) OP (A2) → A3 |
| 四地址指令 | OP A1 A2 A3 A4 | (A1) OP (A2) → A3，下条指令地址A4 |

### 3. 操作码扩展技术

- **固定长度操作码**：n位操作码 → 2^n 条指令，译码简单。
- **扩展操作码**：操作码长度可变，地址码位数少时操作码可扩展。
  - 例：16位指令，4位操作码 + 3×4位地址 → 15条三地址指令（0000~1110），1111用作扩展标志。
  - 原则：不允许短码是长码的前缀（前缀原则）。

### 4. 指令字长

- **指令字长 = 操作码长度 + 地址码长度**
- **单字长指令**、**半字长指令**、**双字长指令**。
- 通常指令字长等于机器字长的整数倍。

---

## 二、寻址方式（必考重点）

### 1. 寻址方式概念

- **寻址方式**：确定本条指令数据地址及下一条待执行指令地址的方法。
- **有效地址 EA**：操作数所在的真实主存地址。
- **形式地址 A**：指令中给出的地址码字段。

### 2. 指令寻址 vs 数据寻址

- **指令寻址**：确定下一条指令地址。
  - 顺序寻址：PC + 1 → PC
  - 跳跃寻址：由转移指令给出
- **数据寻址**：确定本指令操作数地址。

### 3. 数据寻址方式对比表

| 寻址方式 | 有效地址 EA | 特点 | 速度 |
|---------|-----------|------|------|
| **立即寻址** | 操作数直接在指令中（无EA） | 不访问内存，速度快，但位数受限 | 最快 |
| **直接寻址** | EA = A | 简单，但地址位数受限 | 快 |
| **间接寻址** | EA = (A)，A中存操作数地址 | 扩大寻址范围，但访问内存多次 | 慢 |
| **寄存器寻址** | EA = Ri，操作数在寄存器中 | 不访问内存，速度快 | 快 |
| **寄存器间接寻址** | EA = (Ri) | 寄存器存地址，访问内存一次 | 较快 |
| **基址寻址** | EA = (BR) + A，BR为基址寄存器 | 程序浮动，多道并发 | 中 |
| **变址寻址** | EA = (IX) + A，IX为变址寄存器 | 处理数组、循环，IX内容可变 | 中 |
| **相对寻址** | EA = (PC) + A | 转移目标地址相对PC浮动 | 中 |
| **堆栈寻址** | 由SP隐含给出 | 后进先出，用于子程序调用 | 中 |

### 4. 基址 vs 变址（易混淆）

| 对比项 | 基址寻址 | 变址寻址 |
|--------|---------|---------|
| 寄存器 | 基址寄存器BR（专用或通用） | 变址寄存器IX（通用） |
| 谁变化 | 基址寄存器内容由OS管理（程序浮动） | 变址寄存器内容用户改变（循环/数组） |
| 形式地址A | 可变 | 固定（数组首址） |
| 主要用途 | 解决程序重定位、多道程序 | 处理数组、循环 |

### 5. 偏移寻址三兄弟

基址、变址、相对寻址统称偏移寻址：**EA = (R) + A**，其中R为寄存器，A为形式地址（带符号）。
- 基址：R为基址寄存器
- 变址：R为变址寄存器
- 相对：R为程序计数器PC

---

## 三、CISC vs RISC

| 对比项 | CISC（复杂指令集） | RISC（精简指令集） |
|--------|-------------------|-------------------|
| 指令系统 | 复杂、庞大 | 简单、精简 |
| 指令数目 | 多（>100条） | 少（通常<100条） |
| 指令长度 | 可变 | 固定 |
| 寻址方式 | 多种 | 少（基本寻址） |
| 指令执行时间 | 不一 | 大多一个时钟周期完成 |
| 通用寄存器 | 较少 | 较多 |
| 访存指令 | 多数指令可访存 | 只有LOAD/STORE访存 |
| 控制方式 | 多用微程序控制器 | 多用硬布线控制器 |
| 流水线 | 难以实现 | 易实现，效率高 |
| 代表 | x86 | ARM、MIPS、RISC-V |

- **RISC关键技术**：重叠寄存器窗口、优化编译、流水线、硬布线控制。
- **80-20定律**：80%的程序运行时间花在20%的指令上 → RISC简化这20%核心指令。

---

## 四、CPU基本结构

### 1. CPU组成

**CPU = 运算器 + 控制器 + 寄存器组 + 内部总线**

### 2. 运算器（ALU及附属部件）

| 部件 | 功能 |
|------|------|
| **ALU（算术逻辑单元）** | 执行算术、逻辑运算 |
| **ACC（累加器）** | 暂存操作数和运算结果 |
| **MQ（乘商寄存器）** | 乘除法时暂存高位/低位 |
| **X（操作数寄存器）** | 暂存ALU的一个操作数 |
| **PSW（程序状态字）** | 保存进位C、溢出V、零位Z、符号位S等标志 |

### 3. 控制器（CU）

控制器功能：取指令、分析指令、执行指令、控制I/O操作、异常处理。

| 部件 | 功能 |
|------|------|
| **PC（程序计数器）** | 存放下一条要执行指令的地址，取指后自动加1 |
| **IR（指令寄存器）** | 存放当前正在执行的指令 |
| **MAR（存储器地址寄存器）** | 存放要访问的存储单元地址 |
| **MDR（存储器数据寄存器）** | 存放从存储器读出或写入的数据 |
| **ID（指令译码器）** | 对操作码译码，产生控制信号 |
| **时序发生器** | 产生时钟节拍、周期信号 |

### 4. 寄存器分类

| 类型 | 寄存器 | 用途 |
|------|--------|------|
| 用户可见 | 通用寄存器、ACC、PSW | 程序员可编程访问 |
| 用户不可见 | MAR、MDR、IR、PC（部分可见） | CPU内部使用 |

### 5. 关键寄存器位数关系

- **PC位数** = MAR位数（取决于存储器容量）
- **MAR位数** = 存储单元数（按字节编址时即地址总线宽度）
- **MDR位数** = 存储字长 = 数据总线宽度
- **IR位数** = 指令字长

---

## 五、数据通路

### 1. 数据通路概念

**数据通路**：数据在功能部件之间传送的路径。

- **数据通路部件**：ALU、通用寄存器、多路选择器、内总线、暂存器。
- **数据通路基本结构**：
  - **单总线结构**：所有部件连一条总线，同一时刻只能一对部件通信。
  - **双总线结构**：两条总线，可同时传送两个数据。
  - **三总线结构**：三条总线，效率最高。

### 2. 数据通路执行示例（加法指令 ADD R1, R2）

1. PC → MAR
2. MAR → 内存
3. 内存 → MDR
4. MDR → IR（完成取指）
5. PC + 1 → PC
6. IR(地址码) → R1, R2（执行）
7. R1 + R2 → ALU → R1（结果回写）

### 3. 数据通路与时钟

- **时钟周期**：计算机中最小时间单位。
- **机器周期**：通常等于主存读写周期，包含若干时钟周期。
- **指令周期**：CPU从取指到执行完成一条指令的时间，包含若干机器周期。

### 4. 单总线数据通路详解

单总线结构CPU中，所有部件通过一条内部总线相连，同一时刻只能传送一个数据。需要借助**暂存器**（如TEMP、Y、Z等）保存中间结果。

**典型单总线CPU取指流程（所有指令公用的公共操作）：**
- T0：PC → MAR（发送地址）
- T1：M[MAR] → MDR，Read（读内存）
- T2：MDR → IR（指令→指令寄存器），PC+1 → PC
- T3：IR(OP) → 控制器（指令译码）

**ADD R1, R2 (R1←R1+R2) 执行阶段：**
- T4：R1 → Y（送入ALU输入暂存器）
- T5：R2 + Y → Z（ALU运算，结果存Z）
- T6：Z → R1（结果写回R1）

**LOAD R1, addr（从内存加载）执行阶段：**
- T4：IR(addr) → MAR
- T5：M[MAR] → MDR
- T6：MDR → R1

**STORE R1, addr（写入内存）执行阶段：**
- T4：IR(addr) → MAR，R1 → MDR
- T5：MDR → M[MAR]，Write

**多总线结构优势：**
- 双总线可同时传送源操作数和结果，减少暂存器。
- 三总线可并行取操作数，进一步提高速度。

### 5. 数据通路与控制信号

- 每个微操作对应一组控制信号。如 `R1out, Yin` 表示将R1内容送上总线并锁入Y暂存器。
- 控制信号的**时序**由节拍产生器决定，**组合逻辑**由指令译码决定。
- 在微程序控制器中，一条指令对应一段微程序（一系列微指令），每条微指令产生一组控制信号。

---

## 六、指令执行过程

### 1. 指令周期组成

**指令周期 = 取指周期 + 间址周期 + 执行周期 + 中断周期**（间址、中断周期可省）

| 周期 | 功能 | 关键操作 |
|------|------|---------|
| **取指周期** | 从内存取指令 | PC → MAR → M(MAR) → MDR → IR，PC+1 → PC |
| **间址周期** | 取操作数有效地址 | IR(地址码) → MAR → M(MAR) → MDR |
| **执行周期** | 执行指令操作码规定的操作 | 由操作码决定 |
| **中断周期** | 处理中断请求 | 保存断点 → 取中断向量 → 中断服务程序 |

### 2. CPU内部寄存器数据流向

    PC → MAR → 内存(地址)
    内存 → MDR → IR（取指）
    内存 → MDR → ALU（取数）
    ALU → ACC / 通用寄存器（运算结果）
    ACC → MDR → 内存（存数）

### 3. CPU工作流程

    1. 取指：PC→MAR→内存→MDR→IR；PC+1→PC
    2. 译码：IR操作码→ID→控制信号
    3. 间址（如果需要）：取操作数有效地址
    4. 执行：按操作码完成运算
    5. 中断检测：若有中断则响应
    6. 回到第1步，执行下一条指令

---

## 七、控制器设计

### 1. 控制器分类

| 类型 | 实现方式 | 特点 |
|------|---------|------|
| **硬布线控制器**（组合逻辑控制器） | 由门电路、触发器等硬件逻辑电路直接产生控制信号 | 速度快、设计复杂、修改困难、RISC常用 |
| **微程序控制器** | 用微程序存储控制信号，由存储逻辑实现 | 速度慢、设计规整、易修改、CISC常用 |

### 2. 微程序控制器核心概念

**核心思想**：每条机器指令对应一段微程序，微程序由若干微指令组成，每条微指令产生一组微操作控制信号。

| 概念 | 含义 |
|------|------|
| **微命令** | 控制部件的最小命令（如打开某控制门），构成控制信号 |
| **微操作** | 微命令执行的最小操作 |
| **微指令** | 若干微命令的集合，存放于控制存储器CM中 |
| **微程序** | 微指令序列，对应一条机器指令 |
| **微地址** | 微指令在控制存储器CM中的地址 |
| **控制存储器CM** | 存放微程序的ROM，属于CPU内部 |

### 3. 微指令格式

- **水平型微指令**：一条微指令定义多个并行微操作，微指令字较长，编码效率低。
- **垂直型微指令**：类似机器指令，一条微指令定义一个微操作，微指令字短，但并行性差。
- **混合型微指令**：二者折中。

| 对比项 | 水平型 | 垂直型 |
|--------|--------|--------|
| 微指令字长 | 长 | 短 |
| 微程序长度 | 短 | 长 |
| 并行性 | 强 | 弱 |
| 执行速度 | 快 | 慢 |

### 4. 微指令编码方式

- **直接编码**：每一位代表一个微命令，结构简单但字长太长。
- **字段直接编码**：将微命令分组，互斥的微命令放同一字段，每段留一种状态表示"不操作"。
- **字段间接编码**：某字段的微命令需由另一字段解释。

### 5. 微地址形成方式

- **增量计数器法**：μPC + 1 → μPC（与PC类似）
- **下址字段法**：由微指令中下址字段直接给出
- **测试网络法**：由机器指令操作码通过映射转移
- **增量与下址结合**

### 6. 硬布线 vs 微程序对比

| 对比项 | 硬布线控制器 | 微程序控制器 |
|--------|------------|------------|
| 实现方式 | 组合逻辑电路 | 存储逻辑（ROM） |
| 速度 | 快 | 慢 |
| 规整性 | 不规整 | 规整 |
| 修改扩充 | 困难 | 容易 |
| 应用 | RISC | CISC |
| 成本 | 复杂指令时成本高 | 复杂指令时成本低 |

---

## 八、指令流水线基础

### 1. 流水线基本概念

- **流水线**：将指令执行过程分为多个子过程，每个子过程由不同部件并行处理。
- **流水线段数**：通常取5段（取指IF、译码ID、执行EX、访存MEM、写回WB）。

### 2. 流水线冲突（Hazard）

| 冲突类型 | 原因 | 解决方法 |
|---------|------|---------|
| **结构冲突** | 多条指令同时使用同一部件 | 资源重复、分时复用 |
| **数据冲突** | 后续指令需要前面指令的结果 | 数据旁路转发、流水线停顿、编译优化调度 |
| **控制冲突** | 转移指令改变PC | 分支预测、延迟转移、提前计算条件 |

### 3. 流水线性能指标

- **吞吐率 TP = 指令数 / 总时间**
- **加速比 S = 不用流水线时间 / 用流水线时间**
- **效率 = 流水线设备利用率**

---

## 九、高级流水线技术

### 1. 超标量（Superscalar）

**定义**：在**一个时钟周期内**并行发射、执行**多条独立指令**。需要配置多个功能部件（如多ALU、多访存部件）。

- 关键：指令级并行（ILP），发射宽度为 n（每周期 n 条指令），理想加速比 = n × 流水线加速比。
- 需要硬件动态调度支持（如 Tomasulo 算法、寄存器重命名）。
- 实现复杂，但仍为单指令流体系结构。

### 2. 超流水线（Superpipelined）

**定义**：将流水线每段的深度进一步细分（如分成两段），使时钟周期缩短，在相同时间内发射更多指令。

- 时钟频率是常规流水线的 **n 倍**，段数是常规流水线的 **n 倍**。
- 在同一个时钟周期只发射一条指令（单发射），但时钟周期更短，发射频率更高。
- 段数增多导致各段间锁存器增多，额外开销增大。

### 3. 超长指令字 VLIW（Very Long Instruction Word）

**定义**：编译器将多条可并行执行的指令打包成一个超长指令字，每个操作对应一个功能部件，**由编译器静态调度**。

- 硬件简单（不需要动态调度硬件），编译器承担调度责任。
- 所有功能部件**同步**操作，同时执行 VLIW 中各操作。
- VLIW 指令长度固定且很长（通常 128~1024 位）。
- 应用于 DSP、GPU 等嵌入式领域。

### 4. 超标量 vs 超流水线 vs VLIW 对比

| 特性 | 超标量 | 超流水线 | VLIW |
|------|--------|---------|------|
| 发射方式 | 空间并行（多指令同时发射） | 时间并行（高频发射） | 编译器静态打包 |
| 每周期发射数 | n 条 | 1 条（频率 n 倍） | 1 个 VLIW（内含 n 操作） |
| 调度方式 | 硬件动态 | 硬件为主 | 编译器静态 |
| 硬件复杂度 | 高（多套硬件） | 中（细分流水段） | 低（简单硬件） |
| 指令级并行 | 硬件发现 | 基本无 | 编译器发现 |

### 5. 流水线深度与性能

- **段数越多**=时钟周期越短=频率越高，但流水线停顿和刷新代价越大，相邻段间锁存器开销也增大。
- 现代 CPU 流水段数通常 10~20 段（如 Intel Core 为 14~19 段），过高段数（如 Pentium 4 的 31 段 NetBurst）反而降低效率。
- **流水线停顿代价** = 停顿周期数 × 指令发射率 = 流水段数 × 频率，段数多则停顿代价大。

---

## 十、典型计算题套路

### 1. 寻址方式计算有效地址

- 直接寻址：EA = A
- 间接寻址：EA = (A)
- 寄存器间接：EA = (R)
- 基址：EA = (BR) + A
- 变址：EA = (IX) + A
- 相对：EA = (PC) + A

### 2. 指令格式设计

- 操作码位数 n → 最多2^n条指令
- 地址码位数 m → 可直接寻址2^m个存储单元
- 给定指令字长，需平衡操作码与地址码位数

### 3. 微指令设计

- 水平型微指令字长 = 微命令位数 + 下址字段 + 控制字段
- 字段直接编码：每个字段位数 = log2(互斥微命令数+1)（加1表示不操作）

### 4. CPU执行时间计算

- 总时钟周期数 = Σ(各类指令数 × CPI)
- 执行时间 = 总时钟周期数 × 时钟周期 = 总时钟周期数 / 主频

---

## 十一、速通口诀

- **指令三要素**：操作码（做什么）+ 地址码（对谁做）。
- **寻址九式**：立即无EA、直接看A、间接看(A)、寄存器看R、基址BR浮动、变址IX循环、相对PC跳转、堆栈SP隐含。
- **基址 vs 变址**：基址是OS的（程序浮动）、变址是用户的（数组循环）。
- **CPU五寄存器**：PC存下条地址、IR存当前指令、MAR存访存地址、MDR存访存数据、ACC存结果。
- **指令周期四段**：取指、间址、执行、中断。
- **硬布线快、微程序易**：RISC用硬布线、CISC用微程序。
- **微程序三件套**：微命令是信号、微指令是集合、微程序是对应机器指令。
- **流水线三冲突**：结构、数据、控制；解决：资源重复、旁路转发、分支预测。
- **单总线取指**：PC→MAR→M(MAR)→MDR→IR；ADD 例：R1→Y, R2+Y→Z, Z→R1。
- **超标量=空间并行**（多发射）、**超流水线=时间并行**（高频）、**VLIW=编译器静态打包**。
`,
    timestamp: "2026-08-06 22:50",
    embed: ``
};

export default [note1];
