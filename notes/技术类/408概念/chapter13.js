export const note1 = {
    title: "计算机网络（一）：概述与物理层",
    contentmd: `
# 计算机网络 · 概述与物理层

> 408 考点分布：网络体系结构、性能指标（时延计算）、奈奎斯特/香农定理、编码与调制、信道复用、物理层设备为高频考点。

---

## 一、计算机网络概述

### 1. 概念与组成

- **计算机网络**：把地理位置不同、具有独立功能的多台计算机及其外部设备，通过通信线路和设备连接起来，在网络协议和网络操作系统的管理下实现**资源共享和信息传递**的系统。
- 三大组成要素：**计算机系统**、**通信线路与设备**、**网络协议和网络软件**。
- 从逻辑功能上看，网络由**资源子网**（主机、终端、软件，负责数据处理）和**通信子网**（路由器、链路，负责数据传输）两部分组成。

### 2. 网络分类

**按覆盖范围分：**

| 类型 | 英文缩写 | 覆盖范围 | 典型实例 |
|------|---------|---------|---------|
| 局域网 | LAN | 几米到十几千米，房间、楼宇、校园 | 以太网、Wi-Fi |
| 城域网 | MAN | 一座城市，几千米到几十千米 | 城市光纤网 |
| 广域网 | WAN | 跨国、跨洲，几十到几千千米 | Internet 骨干网 |
| 个人区域网 | PAN | 10 米以内，个人工作区 | 蓝牙、ZigBee |

- 局域网**速度快、误码率低、拓扑简单**；广域网**速度慢、误码率高、需要复杂路由**。

**按拓扑结构分：** 总线型、星型、环形、树型、网状型。
- **星型**：中心节点故障则全网瘫痪；易于管理和扩展。
- **总线型**：广播式、争用信道；总线断裂影响全网。
- **环形**：令牌传递、无冲突；节点故障影响全网。
- **网状型**：可靠性高、布线复杂、广域网常用。

**按使用者分：** 公用网、专用网。
**按传输技术分：** 广播式网络、点对点网络。

### 3. 网络功能

数据通信、资源共享、分布式处理、负载均衡、提高可靠性。

---

## 二、分层体系结构

### 1. OSI 七层模型 vs TCP/IP 四层模型 vs 五层协议模型

| OSI 七层 | TCP/IP 四层 | 五层模型 | PDU（协议数据单元） | 典型协议 |
|---------|------------|---------|------------------|---------|
| 7 应用层 | 应用层 | 应用层 | 报文 | HTTP、FTP、SMTP、DNS、Telnet |
| 6 表示层 | ↑ | | 报文 | JPEG、ASCII、加密 |
| 5 会话层 | ↑ | | 报文 | RPC、SQL |
| 4 传输层 | 传输层 | 传输层 | **报文段/数据报** | TCP、UDP |
| 3 网络层 | 网际层 | 网络层 | **数据报/分组** | IP、ICMP、ARP、OSPF |
| 2 数据链路层 | 网络接口层 | 数据链路层 | **帧** | PPP、HDLC、以太网 |
| 1 物理层 | ↑ | 物理层 | **比特** | RJ45、光纤 |

**记忆口诀（自下而上）：** 物—数—网—传—会—表—应。英文首字母：**P**hysical **D**ata **N**etwork **T**ransport **S**ession **P**resentation **A**pplication，记忆：**Please Do Not Touch Steve's Pet Alligator**。

### 2. 各层主要功能

- **物理层**：在物理媒体上**透明传输比特流**，规定接口机械、电气、功能、过程特性。
- **数据链路层**：封装成帧、透明传输、差错检测、流量控制、介质访问控制（MAC子层）。
- **网络层**：路由选择与分组转发、拥塞控制、逻辑寻址（IP）。
- **传输层**：端到端**可靠**（TCP）/不可靠（UDP）传输，分段与重组、流量控制、拥塞控制。
- **会话层**：建立、管理、终止会话，同步点。
- **表示层**：数据格式转换、数据加密/解密、数据压缩。
- **应用层**：为应用程序提供网络服务。

### 3. 协议与服务的概念

- **实体**：任何可发送或接收信息的硬件或软件进程。
- **协议**：**对等层实体**之间通信规则的集合，由**语法、语义、同步**三要素组成。水平方向。
- **服务**：**下层**为**上层**提供的功能调用，垂直方向。下层是服务提供者，上层是服务使用者。
- **服务访问点 SAP**：相邻层交换信息的逻辑接口（如传输层端口、网络层 IP 地址、链路层 MAC 地址）。
- **服务数据单元 SDU、协议数据单元 PDU、接口数据单元 IDU** 的关系：PDU = PCI + SDU。

### 4. OSI 失败与 TCP/IP 成功的原因

OSI 不得不失败的几个原因：实现过分复杂、层次划分不合理（会话/表示层过薄）；TCP/IP 简洁实用，先有协议后有模型，已在 Internet 中广泛部署。

---

## 三、性能指标（高频考点）

### 1. 速率与带宽

- **带宽（Bandwidth）**：原本指信号频带宽度（Hz），计算机网络中指信道能通过的**最高数据率**（bps、kbps、Mbps、Gbps）。
- **速率**：单位时间内传输的比特数。"10M 带宽"指最高 10 Mbps。

### 2. 时延（必考计算）

**总时延 = 发送时延 + 传播时延 + 处理时延 + 排队时延**

| 时延类型 | 计算公式 | 说明 |
|---------|---------|------|
| 发送时延（传输时延） | 数据长度 / 信道带宽 | 将所有比特推向信道的时间 |
| 传播时延 | 信道长度 / 电磁波传播速率 | 信号在媒体中传播的时间 |
| 处理时延 | —— | 路由器检错、找路由的时间 |
| 排队时延 | —— | 在路由器队列中等待的时间 |

- 电磁波在**自由空间**传播速率 = 3 × 10⁸ m/s；在**铜线**中约 2.3 × 10⁸ m/s；在**光纤**中约 2.0 × 10⁸ m/s。
- **典型例题**：发送 1000 bit 数据，带宽 1 Mbps，链路长 1000 km，光纤传播速率 2 × 10⁸ m/s：
  - 发送时延 = 1000 / 10⁶ = 1 ms
  - 传播时延 = 10⁶ / (2 × 10⁸) = 5 ms
  - 总时延 ≈ 1 + 5 + 处理 + 排队（ms）

### 3. 时延带宽积

**时延带宽积 = 传播时延 × 带宽**

- 物理意义：信道中正在传输的比特数（链路上可容纳的比特数），又称**链路长度（以比特为单位）**。
- 例：传播时延 5 ms，带宽 1 Mbps，时延带宽积 = 5000 bit。

### 4. 往返时延 RTT

- 从发送方发送数据开始，到发送方收到接收方确认所经历的时间。
- RTT ≈ 2 × 传播时延 + 中间节点的处理与排队时延。
- TCP 中 RTT 用于设置重传超时时间 RTO。

### 5. 吞吐量与利用率

- **吞吐量**：单位时间内通过某个网络的数据量，受瓶颈链路限制。
- **信道利用率**：某信道有数据通过的时间百分比，过高会导致时延急剧增大。
- **网络利用率**：全网络信道利用率的加权平均。
- 时延与利用率关系：**D = D0 / (1 - U)**，其中 D0 为空闲时延，U 为利用率。U → 1 时 D → ∞。

---

## 四、物理层基本概念

### 1. 物理层任务

在通信线路上**透明地传送比特流**。物理层协议又称**物理层规程**（Procedure），主要任务是为数据端设备（DTE）到数据电路端接设备（DCE）的接口提供规范。

### 2. 物理层接口特性（必背四特性）

| 特性 | 内容 |
|------|------|
| **机械特性** | 接口形状、尺寸、引脚数目、排列、固定装置（如 RJ-45 8 脚） |
| **电气特性** | 电压范围、传输速率、距离限制（如 ±5V 表示 1，0V 表示 0） |
| **功能特性** | 某根引脚出现的电平电压的意义（如发送数据、接收数据、信号地） |
| **过程特性（规程特性）** | 不同功能的各种可能事件的出现顺序 |

### 3. 数据通信系统模型

- **源系统**：源点（信源）+ 发送器（编码/调制）。
- **传输系统**：信号在物理媒体中传输。
- **目的系统**：接收器（解码/解调）+ 终点（信宿）。
- **通信方式**：单工（单向）、半双工（双向但不能同时）、全双工（双向同时）。

---

## 五、数据通信基础

### 1. 信号分类

- **模拟信号**：连续变化；**数字信号**：离散取值。
- **码元**：代表不同离散数值的固定时长的波形信号，一个码元可携带多个比特。
- **波特（Baud）**：码元传输速率，单位 time/s；**比特率** = 波特 × log₂ V（V 为每个码元可能状态数）。

### 2. 信噪比与极限传输速率

- **信噪比（dB）** = 10 × log10(S/N)，其中 S 为信号功率，N 为噪声功率。
- **奈奎斯特（Nyquist）定理**（理想无噪声、带限信道）：
  - **C = 2W × log2 V**
  - W 为信道带宽（Hz），V 为每个码元的离散状态数。
  - 含义：若信道频带宽度为 W，码元状态数为 V，则最大数据率为 2W log2 V。
- **香农（Shannon）定理**（有噪声信道）：
  - **C = W × log2(1 + S/N)**
  - 含义：带宽 W、信噪比 S/N 决定信道的极限信息传输速率。
- **结合使用**：取两者最小值作为信道最大数据率。

**例题**：信道带宽 3000 Hz，信噪比 30 dB（即 S/N = 1000），求最大数据率：
- 香农：C = 3000 × log2(1 + 1000) ≈ 3000 × 9.97 ≈ 30 kbps
- 若采用 16 进制传输，奈奎斯特上限 C = 2 × 3000 × log2 16 = 24000 bps = 24 kbps
- 实际最大取 min(30 kbps, 24 kbps) = 24 kbps

### 3. 编码与调制

**基带信号**：信源直接产生的原始信号，含有低频甚至直流成分，适合**有线近距离**传输。
**带通信号**：将基带信号调制后形成的频带信号，适合**远距离无线/有线**传输。

**常用编码（数字数据 → 数字信号）：**
- **不归零 NRZ**：高 1 低 0，无法同步（无跳变）。
- **曼彻斯特编码**：每个比特中间跳变，上跳为 0/下跳为 1（或反之），自带时钟，**以太网采用**。
- **差分曼彻斯特编码**：每比特中间必有跳变，比特边界有跳变=0，无跳变=1，**令牌环网采用**，抗干扰更好。

**调制方式（数字数据 → 模拟信号）：**
- **调幅 ASK**：振幅变化。
- **调频 FSK**：频率变化。
- **调相 PSK**：相位变化。
- **正交振幅调制 QAM**：振幅 + 相位联合调制，频带利用率高。

**模拟数据 → 数字信号**：PCM 脉冲编码调制，包括**采样、量化、编码**三步。采样频率 ≥ 2 × 信号最高频率（采样定理）。

---

## 六、信道复用技术

### 1. 频分复用 FDM

- 整个传输频带划分为若干**互不交叠**的子频段，每路信号占用一个子频段。
- 各路信号**同时**传输，**频域**上分开。
- 适合**模拟信号**，如广播电视、电话。

### 2. 时分复用 TDM

- 时间划分为等长的**TDM 帧**，每帧中各路信号占用固定时隙。
- 各路信号**轮流**传输，**时域**上分开。
- 适合**数字信号**。
- **统计时分复用 STDM**：按需动态分配时隙，提高利用率。

### 3. 波分复用 WDM

- 光纤通信中复用，一根光纤同时传输多个**不同波长**的光信号。
- **密集波分复用 DWDM**：波长间隔很小，可同时传输数十路光信号。
- 本质是**光的频分复用**。

### 4. 码分复用 CDMA

- 各用户使用**相同频率、相同时间**，靠**不同码型**区分。
- 每个用户分配一个唯一的**码片序列**（chip sequence），各码片序列相互**正交**。
- 发送 1 → 发送自身码片序列；发送 0 → 发送码片序列的反码。
- 接收端用相同码片序列与接收信号做**内积**，结果为 1 → 接收到 1，为 -1 → 接收到 0，为 0 → 无该用户数据。

**例**：S 站码片序列为 (-1 -1 -1 +1 +1 -1 +1 +1)，接收端收到叠加信号 (-1 +1 -3 +1 -1 -3 +1 +1)，求 S 站发送数据：
- 内积 = [(-1)(-1) + (-1)(1) + (-1)(-3) + (1)(1) + (1)(-1) + (-1)(-3) + (1)(1) + (1)(1)] / 8
- = (1 -1 +3 +1 -1 +3 +1 +1) / 8 = 8 / 8 = 1
- S 站发送比特为 **1**。

---

## 七、物理层设备

### 1. 中继器（Repeater）

- **作用**：对信号进行**再生和还原**，延长网络段长度，工作在**物理层**。
- **特点**：不进行存储转发，**无法隔离冲突域和广播域**；两端使用相同协议和速率。
- **5-4-3 规则**：5 个网段、4 个中继器、3 个网段可挂接节点（10Base5 以太网限制）。
- 不能连接两种不同速率的以太网，不能无限制延长网络。

### 2. 集线器（Hub）

- 实质是**多端口中继器**，物理层设备。
- **作用**：将多个节点连接到同一共享介质，对信号进行放大和转发。
- **特点**：
  - 使用集线器的以太网在逻辑上仍是一个总线网，**共享带宽**。
  - **广播**方式转发：从一个端口接收信号，从其他所有端口转发出去。
  - **不能隔离冲突域**：集线器整个网络是一个冲突域；**不能隔离广播域**。
  - 带宽平分：n 台主机共享带宽，每台仅得到 1/n。
- **与交换机对比**：交换机工作在链路层，可隔离冲突域，每个端口独占带宽。

### 3. 设备对比总结

| 设备 | 工作层次 | 隔离冲突域 | 隔离广播域 | 主要功能 |
|------|---------|----------|----------|---------|
| 中继器 | 物理层 | 否 | 否 | 信号再生延长距离 |
| 集线器 | 物理层 | 否 | 否 | 多端口中继 |
| 交换机 | 链路层 | 是 | 否 | 帧转发、自学习 |
| 路由器 | 网络层 | 是 | 是 | 路由选择、隔离广播 |

---

## 八、典型考点速记

- **时延计算四要素**：发送 + 传播 + 处理 + 排队；公式必须记牢。
- **奈奎斯特 vs 香农**：理想无噪声用奈奎斯特 C = 2W log2 V；有噪声用香农 C = W log2(1+S/N)；综合题取最小。
- **编码识别**：曼彻斯特（中间跳变）、差分曼彻斯特（边界跳变判断）。
- **复用区分**：FDM 同时间不同频率；TDM 不同时间相同频率；WDM 光波长；CDMA 同频同时不同码。
- **设备层次**：中继器/集线器=物理层；交换机/网桥=链路层；路由器=网络层。
- **集线器 vs 交换机**：集线器共享带宽、广播式、单冲突域；交换机独占带宽、按 MAC 转发、每端口一冲突域。
`,
    timestamp: "2026-08-06 18:30",
    embed: ``
};

export default [note1];
