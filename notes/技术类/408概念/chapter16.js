export const note1 = {
    title: "计算机网络（四）：传输层与应用层",
    contentmd: `
# 计算机网络 · 传输层与应用层

> 408 高频考点：TCP 首部与可靠传输、TCP 流量控制与拥塞控制（cwnd/ssthresh 计算）、三次握手四次挥手及状态转换、UDP 特点、DNS 递归/迭代查询、HTTP/SMTP/POP3/IMAP 协议。

---

## 一、传输层功能与端口

### 1. 传输层作用

- **端到端通信**：为应用进程之间提供**逻辑通信**（网络层是主机到主机）。
- **复用与分用**：
  - **复用**：发送方不同应用进程的数据可使用同一传输层协议封装后通过 IP 发送。
  - **分用**：接收方传输层根据端口号将数据交付给对应应用进程。
- **差错检测**：UDP/TCP 首部含校验和字段，对首部和数据均校验。
- **面向连接与无连接**：TCP 提供面向连接的可靠传输；UDP 提供无连接不可靠传输。

### 2. 端口

- **端口（Port）**：16 位，范围 0~65535。
- **三类端口**：
  - **熟知端口**（0~1023）：HTTP 80、HTTPS 443、FTP 21（控制）、20（数据）、SMTP 25、POP3 110、IMAP 143、DNS 53、Telnet 23。
  - **登记端口**（1024~49151）：需向 IANA 登记。
  - **客户端口/短暂端口**（49152~65535）：客户端临时使用。

### 3. 常用协议端口对照

| 协议 | 端口 | 传输层协议 |
|------|------|----------|
| HTTP | 80 | TCP |
| HTTPS | 443 | TCP |
| FTP 控制/数据 | 21/20 | TCP |
| SMTP | 25 | TCP |
| POP3 | 110 | TCP |
| IMAP | 143 | TCP |
| DNS | 53 | UDP（也可 TCP） |
| Telnet | 23 | TCP |
| SSH | 22 | TCP |
| DHCP | 67/68 | UDP |
| TFTP | 69 | UDP |
| SNMP | 161 | UDP |

---

## 二、UDP 协议

### 1. UDP 特点

- **无连接**：发送前不需要建立连接，减少开销和时延。
- **不可靠**：不保证可靠交付，不使用流量控制、拥塞控制、重传机制。
- **面向报文**：应用层交下来的报文 UDP 不合并也不拆分，保留报文边界。每个报文独立发送。
- **首部开销小**：仅 8 字节。
- **支持一对多通信**：可多播和广播。
- **无拥塞控制**：适用于实时应用（如视频、语音），即使拥塞也不降低发送速率。

### 2. UDP 首部格式（8 字节）

| 字段 | 长度 | 说明 |
|------|------|------|
| 源端口 | 16 bit | 发送方端口，可省略（0） |
| 目的端口 | 16 bit | 接收方端口 |
| 长度 | 16 bit | UDP 用户数据报长度（首部+数据），最小 8 |
| 校验和 | 16 bit | 检测整个 UDP 数据报（含伪首部） |

### 3. UDP 校验和

- 计算时添加 **12 字节伪首部**（源 IP、目的 IP、0、协议号 17、UDP 长度），仅在计算时使用，不发送。
- 包含伪首部是为了检测 IP 数据报是否错误地交付给 UDP。
- 校验和为可选字段（IPv4 中），置 0 表示不计算。

### 4. UDP 适用场景

- 实时多媒体（VoIP、视频会议）。
- DNS 查询、SNMP、DHCP、TFTP 等短报文。
- 需要高效、容忍少量丢包的应用。

---

## 三、TCP 协议

### 1. TCP 特点

- **面向连接**：通信前需三次握手建立连接，通信结束四次挥手释放。
- **点对点**：仅支持一对一通信，不支持多播和广播。
- **可靠传输**：通过序号、确认、重传、流量控制、拥塞控制等机制保证。
- **全双工**：双方可同时发送和接收数据。
- **面向字节流**：把应用层数据看作无结构的字节流，不保留报文边界。可分段发送、合并接收。
- **首部最小 20 字节**，可扩展到 60 字节。

### 2. TCP 首部格式（必考）

| 字段 | 长度 | 说明 |
|------|------|------|
| 源端口 | 16 bit | 发送方端口 |
| 目的端口 | 16 bit | 接收方端口 |
| 序号 seq | 32 bit | 本报文段数据中第一个字节的序号 |
| 确认号 ack | 32 bit | 期望收到对方下一个字节的序号 |
| 数据偏移 | 4 bit | 首部长度，单位 4B，最小 5（20B） |
| 保留 | 6 bit | 保留 |
| 控制位 URG | 1 bit | 紧急指针有效 |
| 控制位 ACK | 1 bit | 确认号有效（建立连接后 ACK=1） |
| 控制位 PSH | 1 bit | 推送，接收方尽快交付应用 |
| 控制位 RST | 1 bit | 复位连接 |
| 控制位 SYN | 1 bit | 建立连接同步序号 |
| 控制位 FIN | 1 bit | 释放连接 |
| 窗口 | 16 bit | 接收窗口 rwnd，告诉对方允许发送的字节数 |
| 校验和 | 16 bit | 检验首部+数据（含伪首部） |
| 紧急指针 | 16 bit | URG=1 时有效，指出紧急数据末尾 |
| 选项 | 0~40B | MSS、窗口扩大、时间戳、SACK |

### 3. 序号与确认号

- **序号 seq**：标识本报文段数据字段第一个字节的序号。
  - TCP 把数据看作字节流，每个字节都有序号。
  - 初始序号 ISN 随机选择（防预测攻击）。
- **确认号 ack**：期望收到对方下一个字节的序号。
  - 即 ack 之前的数据均已正确接收。
  - **累计确认**机制：确认号 N 表示 N-1 及之前的数据均已收到。
- 若接收方收到序号 1~1000 的数据，则回复 ACK=1001。

### 4. TCP 可靠传输机制

- **序号**：保证按序交付，识别重复数据。
- **确认**：接收方对收到的数据进行确认。
- **重传**：发送方超时未收到 ACK 则重传。
  - **超时时间 RTO**：基于 RTT 动态计算（加权平均 RTT + 偏差）。
- **冗余 ACK**：收到 3 个相同冗余 ACK 触发**快重传**。

---

## 四、TCP 流量控制

### 1. 滑动窗口机制

- **接收窗口 rwnd**：接收方在 ACK 中告知自己的接收窗口大小，限制发送方发送量。
- **发送窗口 = min(rwnd, cwnd)**：取接收窗口和拥塞窗口中的较小值。
- 窗口动态变化：随确认到达向前滑动。

### 2. 流量控制过程

- 接收方根据自己缓存情况调整 rwnd。
- 若 rwnd = 0，发送方停止发送；但发送方持续发送**零窗口探测报文**，防止死锁（窗口更新报文丢失）。
- 发送窗口随时间逐步调整：rwnd=0 → 停止；rwnd=N → 发送 N 字节。

### 3. 坚持计时器

- 发送方收到 rwnd=0 后启动坚持计时器，超时则发送零窗口探测报文，避免死锁。

---

## 五、TCP 拥塞控制（必考计算）

### 1. 概念

- **拥塞**：网络中某段时间内对资源需求超过可用资源，导致性能急剧下降。
- **拥塞控制 vs 流量控制**：流量控制是端到端的接收能力限制；拥塞控制是网络全局性的资源限制。
- **拥塞窗口 cwnd**：发送方根据网络拥塞程度动态调整。

### 2. 四种算法

#### 慢开始（Slow Start）

- 连接建立或超时后，**cwnd 从 1 开始**（实际现代 TCP 用初始 cwnd=10，408 一般按 1 计）。
- 每收到一个 ACK，cwnd **加倍**（即每个 RTT 翻倍）。
- 增长曲线：1 → 2 → 4 → 8 → 16 → ...（指数增长）。
- 目的：快速探测网络容量。

#### 拥塞避免（Congestion Avoidance）

- 当 cwnd 达到**慢开始门限 ssthresh** 时，进入拥塞避免阶段。
- 每经过一个 RTT，cwnd **加 1**（线性增长）。
- 增长曲线：缓慢线性上升，避免过快导致拥塞。

#### 快重传（Fast Retransmit）

- 接收方每收到一个失序报文段，立即发送**冗余 ACK**。
- 发送方连续收到 **3 个冗余 ACK** → 立即重传丢失报文段，不等超时。
- 目的：提高性能，减少等待。

#### 快恢复（Fast Recovery）

- 收到 3 个冗余 ACK 时，**不进入慢开始**，而是：
  - **ssthresh = cwnd / 2**。
  - **cwnd = ssthresh**（即减半，不下到 1）。
  - 进入拥塞避免阶段线性增长。
- **超时事件**则不同：
  - **ssthresh = cwnd / 2**。
  - **cwnd = 1**。
  - 进入慢开始阶段。

### 3. 拥塞控制规律总结

| 事件 | ssthresh 变化 | cwnd 变化 | 阶段 |
|------|--------------|----------|------|
| 慢开始阶段 | 不变 | 每收到 ACK 翻倍 | 慢开始 |
| 达到 ssthresh | 不变 | 改为线性增长 | 拥塞避免 |
| 收到 3 个冗余 ACK | = cwnd/2 | = ssthresh | 快恢复后拥塞避免 |
| 超时 | = cwnd/2 | = 1 | 慢开始 |

### 4. 拥塞窗口典型例题

**例**：cwnd 初始 = 1，ssthresh = 16，每个 RTT 后变化，求经过若干 RTT 的 cwnd。
- RTT 1：1 → 2
- RTT 2：2 → 4
- RTT 3：4 → 8
- RTT 4：8 → 16（达到 ssthresh，进入拥塞避免）
- RTT 5：16 → 17
- RTT 6：17 → 18
- RTT 7：18 → 19
- RTT 8：19 → 20（假设此时收到 3 个冗余 ACK）
  - ssthresh = 20/2 = 10
  - cwnd = 10，进入拥塞避免
- RTT 9：10 → 11
- RTT 10：11 → 12
- ...

**关键技巧**：慢开始阶段成倍增长，达到 ssthresh 改线性；3 冗余 ACK 减半继续线性；超时归 1。

---

## 六、TCP 连接管理（必考）

### 1. 三次握手（建立连接）

**状态转换**：客户端从 CLOSED → SYN_SENT → ESTABLISHED；服务端从 LISTEN → SYN_RCVD → ESTABLISHED。

1. **第一次握手**：客户端发送 **SYN=1, seq=x** 的连接请求报文。客户端进入 **SYN_SENT**。
2. **第二次握手**：服务端收到后，发送 **SYN=1, ACK=1, seq=y, ack=x+1** 的确认报文。服务端进入 **SYN_RCVD**。
3. **第三次握手**：客户端发送 **ACK=1, seq=x+1, ack=y+1** 确认。客户端进入 **ESTABLISHED**。服务端收到后进入 **ESTABLISHED**。

**为什么三次而不是两次？**
- 防止已失效的连接请求报文段突然又传到服务端，导致服务端错误建立连接、浪费资源。
- 两次握手无法确认客户端的接收能力正常。
- 第三次握手可以携带数据。

### 2. 四次挥手（释放连接）

**状态转换**：客户端从 ESTABLISHED → FIN_WAIT_1 → FIN_WAIT_2 → TIME_WAIT → CLOSED；服务端从 ESTABLISHED → CLOSE_WAIT → LAST_ACK → CLOSED。

1. **第一次挥手**：客户端发送 **FIN=1, seq=u**，停止发送数据。客户端进入 **FIN_WAIT_1**。
2. **第二次挥手**：服务端收到后发送 **ACK=1, ack=u+1**，服务端进入 **CLOSE_WAIT**。客户端收到后进入 **FIN_WAIT_2**。此时服务端还可继续发送剩余数据（半关闭状态）。
3. **第三次挥手**：服务端数据发送完毕，发送 **FIN=1, seq=w**，服务端进入 **LAST_ACK**。
4. **第四次挥手**：客户端发送 **ACK=1, ack=w+1**，客户端进入 **TIME_WAIT**，等待 **2MSL** 后进入 CLOSED。服务端收到 ACK 后进入 CLOSED。

### 3. TIME_WAIT 状态（必考）

- **客户端**在四次挥手后进入 **TIME_WAIT**，需等待 **2MSL**（Maximum Segment Lifetime，最长报文段寿命）才能进入 CLOSED。
- **MSL**：报文段最大寿命，RFC 793 建议 2 分钟，实际实现常为 30 秒、1 分钟或 2 分钟。
- **2MSL 原因**：
  1. **保证最后一个 ACK 能到达服务端**：若 ACK 丢失，服务端会重发 FIN，客户端在 2MSL 内还能收到并重发 ACK。
  2. **让本连接的所有报文段从网络中消失**：防止旧连接的报文段影响新连接。

### 4. 重要 TCP 状态

| 状态 | 含义 |
|------|------|
| CLOSED | 初始关闭状态 |
| LISTEN | 服务端监听端口等待连接 |
| SYN_SENT | 客户端发送 SYN 后等待确认 |
| SYN_RCVD | 服务端收到 SYN 发送 SYN+ACK 后 |
| ESTABLISHED | 连接建立，可以传输数据 |
| FIN_WAIT_1 | 主动关闭方发送 FIN 后 |
| FIN_WAIT_2 | 主动关闭方收到 ACK 后（半关闭） |
| TIME_WAIT | 主动关闭方发送最后 ACK 后，等待 2MSL |
| CLOSE_WAIT | 被动关闭方收到 FIN 发送 ACK 后 |
| LAST_ACK | 被动关闭方发送 FIN 后等待 ACK |
| CLOSING | 双方同时关闭（少见） |

### 5. 半关闭状态

- FIN_WAIT_2 状态下，客户端不能再发送数据但还能接收数据。
- 服务端可以继续发送未传完的数据，发送完毕后第三次挥手。

### 6. SYN 洪泛攻击

- 攻击者发送大量伪造源 IP 的 SYN 报文，服务端为每个 SYN 分配资源进入 SYN_RCVD，导致资源耗尽。
- 防御：SYN Cookie、连接限制、防火墙过滤。

---

## 七、应用层协议

### 1. HTTP 超文本传输协议

- **作用**：万维网客户程序与服务器程序之间的传输协议。
- **基于 TCP**，默认端口 80。
- **无状态协议**：服务器不记录客户状态。可通过 **Cookie** 维持状态。
- **HTTP 报文**：
  - **请求报文**：请求行（方法 URL 版本）+ 首部 + 空行 + 实体主体。
  - **响应报文**：状态行（版本 状态码 短语）+ 首部 + 空行 + 实体主体。
- **请求方法**：GET（获取）、POST（提交）、PUT（更新）、DELETE（删除）、HEAD（请求首部）、OPTIONS。
- **状态码**：
  - 1xx：信息性
  - 2xx：成功（**200 OK**）
  - 3xx：重定向（301 永久重定向、302 临时重定向、304 未修改）
  - 4xx：客户端错误（**404 Not Found**、403 禁止）
  - 5xx：服务器错误（**500 Internal Server Error**、503 服务不可用）

**HTTP 1.0 / 1.1 / 2.0 区别：**
- HTTP 1.0：每次请求建立新 TCP 连接（短连接）。
- HTTP 1.1：默认**持久连接**（keep-alive），支持**流水线**，可以复用 TCP 连接。
- HTTP 2.0：多路复用、二进制分帧、头部压缩。
- HTTPS = HTTP + SSL/TLS，端口 443，提供加密和身份认证。

### 2. DNS 域名系统

- **作用**：将**域名解析为 IP 地址**。
- **基于 UDP**，端口 53。查询报文小、要求快速响应。
- **DNS 域名层次结构**：层次树状，从右到左层级递减。
  - 根域名（.）→ 顶级域名 TLD（com、org、cn、edu）→ 二级域名 → 三级域名 → ...
- **域名服务器类型**：
  - **根域名服务器**：13 套（A~M），全球分布，知道所有顶级域名服务器。
  - **顶级域名服务器**：负责 com、org、cn 等顶级域。
  - **权威域名服务器**：负责具体域的解析。
  - **本地域名服务器**：每个 ISP 一个，主机配置的默认 DNS。
  - **中间域名服务器**：转发查询时使用。
- **解析方式**：

| 查询类型 | 客户端→本地 | 本地→其他 |
|---------|----------|---------|
| **递归查询** | 本地域名服务器代替主机查询 | 本地请求根、根请求 TLD... 逐级代查 |
| **迭代查询** | 本地返回最终结果或失败 | 本地依次向根、TLD、权威查询，每级返回下一级地址 |

- **实际中**：主机到本地域名服务器通常是**递归查询**；本地域名服务器向根、TLD、权威服务器通常是**迭代查询**。
- **资源记录 RR**：每条 DNS 记录包含（域名、类型、值、TTL）。
  - 类型 A：主机名 → IPv4 地址。
  - 类型 AAAA：主机名 → IPv6 地址。
  - 类型 NS：域 → 该域权威服务器。
  - 类型 CNAME：别名 → 规范名。
  - 类型 MX：邮件服务器。
  - 类型 PTR：IP → 域名（反向解析）。

### 3. 电子邮件协议

**邮件系统三大组成**：用户代理 UA、邮件服务器、协议（SMTP/POP3/IMAP）。

**SMTP（简单邮件传输协议）**：
- **基于 TCP**，端口 25。
- **推送协议**：发送方主动推送邮件到接收方邮件服务器。
- **ASCII 文本协议**，命令少（HELO、MAIL FROM、RCPT TO、DATA、QUIT）。
- **不认证**（原始版本），现代扩展 SMTP ESMTP 支持 AUTH。
- **传输过程**：发送方 UA → 发送方邮件服务器（SMTP）→ 接收方邮件服务器（SMTP）。

**POP3（邮局协议第 3 版）**：
- **基于 TCP**，端口 110。
- **拉取协议**：用户从邮件服务器下载邮件到本地。
- **工作模式**：
  - 下载并删除：邮件下载后从服务器删除（默认）。
  - 下载并保留：邮件下载后仍保留在服务器。
- **特点**：简单但功能少，不支持服务器端文件夹管理。

**IMAP（互联网邮件访问协议）**：
- **基于 TCP**，端口 143。
- **比 POP3 更复杂**：邮件保留在服务器，支持服务器端文件夹管理。
- 用户可在不同设备上访问相同邮件，状态同步。
- 支持搜索、部分下载（先看头再决定是否下载正文）。

**邮件发送流程**：
- 发送方 UA → SMTP → 发送方邮件服务器 → SMTP → 接收方邮件服务器 → POP3/IMAP → 接收方 UA。

### 4. FTP 文件传输协议

- **基于 TCP**，**控制连接端口 21，数据连接端口 20**。
- **双连接**：
  - **控制连接**：贯穿整个会话，传输命令。
  - **数据连接**：传输文件数据，每次传输建立一次。
- **两种模式**：
  - **主动模式 PORT**：服务器从 20 端口主动连接客户端。
  - **被动模式 PASV**：服务器开放随机端口等待客户端连接。
- **特点**：有状态协议，传输效率高，支持断点续传。
- **TFTP**：简单文件传输协议，基于 UDP 端口 69，无认证，仅用于小型嵌入式场景。

### 5. 其他应用层协议

- **Telnet**：远程登录协议，端口 23，明文传输，不安全。
- **SSH**：安全 shell，端口 22，加密传输，替代 Telnet。
- **SNMP**：简单网络管理协议，基于 UDP，端口 161（agent）/162（trap）。
- **MIME**：多用途互联网邮件扩展，扩展 SMTP 以支持非 ASCII 内容（图像、视频等）。

---

## 八、万维网 WWW 与 URL

### 1. WWW 万维网

- **WWW（World Wide Web）**：大规模、联机式的信息存储访问系统。
- **三大组成部分**：
  - **统一资源定位符 URL**：标识万维网上的资源。
  - **HTTP 协议**：传输超文本文档。
  - **HTML 超文本标记语言**：定义文档格式。
- **客户/服务器模式**：浏览器（客户端）请求，Web 服务器响应。

### 2. URL 统一资源定位符

- **格式**：**<协议>://<主机>:<端口>/<路径>/<文件名>**
- 例：http://www.example.com:80/index.html
- **协议**：HTTP、HTTPS、FTP、mailto、telnet 等。
- **主机**：域名或 IP 地址。
- **端口**：可省略（HTTP 默认 80，HTTPS 默认 443）。
- **路径**：服务器上资源的位置。

### 3. Cookie 与 Session

- **Cookie**：服务器发送、保存在客户端的小段数据，用于跟踪会话。每次请求同域时自动携带。
- **Session**：保存在服务器端的会话信息，通过 Session ID（通常存储在 Cookie 中）关联客户端。

---

## 九、典型考点速记

- **传输层 vs 网络层**：传输层端到端（进程间），网络层主机到主机。
- **UDP**：无连接、不可靠、面向报文、首部 8B、支持一对多、用于实时应用。
- **TCP**：面向连接、可靠、面向字节流、全双工、首部 20~60B、点对点。
- **TCP 端口**：HTTP 80、FTP 21/20、SMTP 25、POP3 110、IMAP 143、DNS 53、Telnet 23、HTTPS 443。
- **TCP 序号**：字节流编号；**确认号**：期望下一字节序号（累计确认）。
- **流量控制**：滑动窗口，rwnd 由接收方通告，窗口为 0 时启动坚持计时器。
- **拥塞控制 4 算法**：慢开始（指数翻倍）、拥塞避免（线性+1）、快重传（3 冗余 ACK）、快恢复（ssthresh=cwnd/2，cwnd=ssthresh）。
- **超时 vs 3 冗余 ACK**：超时 cwnd=1 进慢开始；3 冗余 ACK cwnd 减半进快恢复。
- **三次握手**：SYN_SENT→SYN_RCVD→ESTABLISHED；**四次挥手**：FIN_WAIT_1→FIN_WAIT_2→TIME_WAIT(2MSL)→CLOSED。
- **TIME_WAIT 2MSL**：保证最后 ACK 到达 + 让旧报文消失。
- **HTTP**：无状态、Cookie 维持、1.0 短连接、1.1 持久连接+流水线。
- **DNS**：UDP 53，递归查询（客户端到本地）+ 迭代查询（本地到根/TLD/权威）。
- **邮件协议**：SMTP 推送（端口 25）、POP3 拉取（端口 110）、IMAP 在线管理（端口 143）。
- **FTP**：双连接，控制 21 + 数据 20，主动 PORT / 被动 PASV。
- **URL 格式**：协议://主机:端口/路径/文件名。
`,
    timestamp: "2026-08-06 18:30",
    embed: ``
};

export default [note1];
