export const note1 = {
    title: "数据结构速通复习",
    contentmd: `
# 数据结构 · 408考试速通复习

> 定位：概念+高频考点速记，适合考前快速过一遍。重点掌握：复杂度分析、二叉树性质、图算法、查找与排序对比表。

---

## 一、基本概念与算法分析

- **数据**：信息的载体，能被计算机识别和处理的符号集合。
- **数据元素**：数据的基本单位（一个结点/记录）。
- **数据项**：构成数据元素的不可分割的最小单位。
- **数据对象**：性质相同的数据元素的集合。

**数据结构三要素：** 逻辑结构、存储结构、数据的运算。

- **逻辑结构（与存储无关，面向问题）：**
  - 集合结构（无序、互斥）
  - 线性结构（一对一）
  - 树形结构（一对多）
  - 图状/网状结构（多对多）
- **存储结构（物理结构，面向计算机）：**
  - 顺序存储：逻辑相邻→物理相邻，随机存取，但插入删除需移动元素。
  - 链式存储：指针表示逻辑关系，便于插入删除，但占用额外空间、不支持随机存取。
  - 索引存储：建立索引表，加快查找。
  - 散列存储：由关键字直接计算存储地址。

**算法特性：** 有穷性、确定性、可行性、有输入、有输出。

- **时间复杂度**：最坏情况下的基本操作次数（大O表示）。
- **空间复杂度**：算法运行所需额外内存（不含输入本身）。

> 常见复杂度排序：O(1) < O(log n) < O(n) < O(nlog n) < O(n²) < O(2ⁿ) < O(n!)

---

## 二、线性表

**顺序表 vs 链表对比：**

| 项目 | 顺序表 | 链表 |
|------|--------|------|
| 存储 | 连续空间 | 不连续，指针相连 |
| 存取方式 | 随机存取 | 顺序存取 |
| 插入/删除 | 平均移动 n/2 个元素，O(n) | 只需改指针，O(1)（查找仍需 O(n)） |
| 空间 | 需预分配，可能浪费 | 动态申请，灵活 |
| 适用 | 查找多、元素少变动 | 增删频繁 |

- **头结点作用：** 使空表与非空表操作统一，便于在表头插入/删除。
- **单链表**：只能从头到尾遍历；**双链表**：每个结点有前驱+后继指针，可双向访问；**循环链表**：尾指针指向头，可从任意点出发遍历全表。

---

## 三、栈、队列与串

**栈（Stack）**：后进先出 **LIFO**。只能在栈顶插入/删除。
- 栈的应用：括号匹配、表达式求值（后缀/中缀转后缀）、递归调用、函数调用栈、浏览器后退。

**队列（Queue）**：先进先出 **FIFO**。队尾插入、队头删除。
- **循环队列**（顺序实现）：
  - 入队：\`rear = (rear + 1) % MaxSize\`，出队：\`front = (front + 1) % MaxSize\`
  - 判空：\`front == rear\`
  - 判满：\`(rear + 1) % MaxSize == front\`（牺牲一个存储单元）
  - 元素个数：\`(rear - front + MaxSize) % MaxSize\`
- 双端队列：两端均可插入删除；输入受限/输出受限。

**串（String）**：子串、主串、模式匹配。
- **KMP算法**：利用已匹配信息避免回溯，时间 O(m+n)。
- **next数组**：\`next[j]\` 表示失配时模式串应滑动的位置，前缀与后缀的最长公共部分 +1；失配时 \`j = next[j]\`。

---

## 四、树与二叉树

**树的基本概念：** 结点的度（孩子个数）、树的度、叶子/终端结点、深度（层次）、森林。

**二叉树性质（必背）：**
1. 第 i 层最多有 **2^(i-1)** 个结点。
2. 深度为 k 的二叉树最多有 **2^k - 1** 个结点。
3. 叶子结点数 n0 与度为2的结点数 n2：**n0 = n2 + 1**。
4. 具有 n 个结点的完全二叉树深度为 **⌊log₂n⌋ + 1**。
5. 完全二叉树中编号 i 的结点：左孩子 2i，右孩子 2i+1，父结点 ⌊i/2⌋。

- **满二叉树**：每层都满；**完全二叉树**：只有最后两层不满且叶子集中在左侧。
- **顺序存储**适合完全二叉树；一般二叉树用**链式存储**（二叉链表：data + lchild + rchild）。

**遍历（重点）：**
- 先序：根→左→右；中序：左→根→右；后序：左→右→根；层序：逐层从左到右。
- 由**中序 + 先序**或**中序 + 后序**可唯一确定二叉树；先序+后序不能唯一确定。

**线索二叉树**：利用空指针域指向前驱/后继，加快遍历。线索化类型：先序、中序、后序线索树。

**哈夫曼树（最优二叉树）：**
- 带权路径长度 **WPL = Σ(权值 × 路径长度)** 最小的二叉树。
- 构造：每次选两个最小权值结点合并为新结点（权值相加）。
- 哈夫曼树特点：**没有度为1的结点**，n个叶子结点共 2n-1 个结点。
- 哈夫曼编码：前缀编码，用于无损压缩。

**二叉排序树 BST：** 左<根<右，中序遍历得到递增序列；查找/插入/删除效率 O(log n)~O(n)。
**平衡二叉树 AVL：** 左右子树高度差≤1；平衡因子 = 左高-右高 ∈ {-1,0,1}；失衡调整用 LL、RR、LR、RL 旋转。

---

## 五、图

**图的基本概念：** 有向图/无向图、完全图（无向 n(n-1)/2 条边，有向 n(n-1) 条）、连通图/强连通图、生成树、权、度。

**存储结构对比：**

| 项目 | 邻接矩阵 | 邻接表 |
|------|---------|--------|
| 空间 | O(n²)，适合稠密图 | O(n+e)，适合稀疏图 |
| 判断两点是否相邻 | O(1) | O(度) |
| 求度 | 简单 | 有向图需分入度/出度 |

**遍历：**
- **BFS（广度优先）**：队列实现，类似于树的层序遍历。
- **DFS（深度优先）**：栈/递归实现。

**最小生成树（MST）：**
- **Prim**：从顶点出发，每次选"连接已选集合与未选集合"的最小权边，适合稠密图 O(n²)。
- **Kruskal**：按边权从小到大选边（不成环），用并查集判环，适合稀疏图 O(elog e)。

**最短路径：**
- **Dijkstra**：单源最短路径，**不能处理负权边**，O(n²)。
- **Floyd**：所有顶点对之间最短路径，允许负权（不能有负环），O(n³)。

**拓扑排序（AOV网）：** 有向无环图，输出入度为0的顶点并删除其出边；存在拓扑排序⇔图中无环。
**关键路径（AOE网）：** 事件最早/最迟发生时间，活动最早/最迟开始时间；**关键路径 = 最长的路径**，其长度决定工期，关键活动时间余量为0。

---

## 六、查找

- **平均查找长度 ASL** = Σ(查找概率 × 比较次数)，是衡量查找算法效率的主要指标。
- **顺序查找**：O(n)，ASL=(n+1)/2。
- **折半查找（二分）**：仅适用于**有序**的顺序存储；ASL≈log₂(n+1)-1；判定树为平衡二叉树。
- **分块查找**：块间有序、块内无序；ASL = 索引表查找 + 块内查找。

**B树/B+树（多路平衡查找树）：**
- m阶B树：每个结点最多 m-1 个关键字、m 棵子树；所有叶子在同一层。
- B+树：关键字与数据分离，所有数据都在叶子结点，叶子结点有序链表相连，适合数据库索引。

**散列表（哈希表）：**
- 散列函数：直接定址法、除留余数法（\`H(key) = key % p\`）、平方取中法、数字分析法。
- **冲突处理：**
  - **开放定址法**：线性探测（堆积现象）、平方探测、再散列。
  - **拉链法（链地址法）**：同义词用链表串起来，最常用。
- **装填因子 α = 表中记录数/表长**，α 越大冲突越多。
- 影响散列表查找效率的因素：散列函数、冲突处理方法、装填因子。

---

## 七、排序（必背对比表）

| 排序算法 | 平均时间 | 最坏时间 | 空间 | 稳定性 |
|---------|---------|---------|------|--------|
| 直接插入 | O(n²) | O(n²) | O(1) | 稳定 |
| 希尔排序 | O(n^1.3) | O(n²) | O(1) | 不稳定 |
| 冒泡排序 | O(n²) | O(n²) | O(1) | 稳定 |
| 快速排序 | O(nlog n) | **O(n²)** | O(log n) | 不稳定 |
| 简单选择 | O(n²) | O(n²) | O(1) | 不稳定 |
| 堆排序 | O(nlog n) | O(nlog n) | O(1) | 不稳定 |
| 归并排序 | O(nlog n) | O(nlog n) | **O(n)** | 稳定 |
| 基数排序 | O(d(n+r)) | O(d(n+r)) | O(r) | 稳定 |

**记忆要点：**
- 一趟就能确定一个元素最终位置的：冒泡、快速（枢轴）、简单选择、堆（堆顶）。
- **堆**：大根堆（父≥子）/小根堆（父≤子），建堆 O(n)，调整 O(log n)。
- 快速排序：平均最快，最坏情况为基本有序时退化为 O(n²)；**每趟确定一个枢轴最终位置**。
- 归并排序：稳定、需要额外 O(n) 空间，适合外部排序。
- 排序趟数与初始序列无关的：简单选择、基数；快速排序趟数依赖初始序列。

---

## 八、速通口诀

- 数据结构三要素：**逻辑 + 存储 + 运算**。
- 二叉树性质记三条：**2^(i-1)、2^k-1、n0=n2+1**。
- 图算法各司其职：**求最小生成树（Prim点/Kruskal边），最短路径（Dijkstra单源正权/Floyd全源），拓扑判环，关键路径求工期**。
- 排序稳定性："**插冒归基**稳"，其余不稳；快排最坏 O(n²) 是唯一"退步"的 nlogn 算法。
`,
    timestamp: "2026-08-06 09:00",
    embed: ``
};

export default [note1];
