export const note1 = {
    title: "数据结构（一）：基本概念、线性表、栈队列与串",
    contentmd: `
# 数据结构（一）：基本概念、线性表、栈队列与串

> 定位：408考研数据结构第一章。重点掌握：数据结构三要素、时间复杂度分析、顺序表与链表对比、循环队列计算、KMP算法与next数组。

---

## 一、数据结构基本概念

### 1. 基本术语
- **数据**：信息的载体，能被计算机识别、存储和处理的符号集合。
- **数据元素**：数据的基本单位，在程序中作为一个整体考虑（一个结点/记录）。
- **数据项**：构成数据元素的不可分割的最小单位。
- **数据对象**：性质相同的数据元素的集合，是数据的子集。
- 包含关系：数据 > 数据元素 > 数据项。

### 2. 数据结构三要素
**数据结构 = 逻辑结构 + 存储结构 + 数据的运算**

**逻辑结构（与存储无关，面向问题）：**
- 集合结构：元素间除"同属一个集合"外无其他关系。
- 线性结构：一对一关系，除首尾外每个元素有唯一前驱和后继。
- 树形结构：一对多关系。
- 图状/网状结构：多对多关系。

**存储结构（物理结构，面向计算机）：**
- **顺序存储**：逻辑相邻元素物理相邻，支持随机存取，但插入删除需移动元素。
- **链式存储**：用指针表示逻辑关系，便于插入删除，但不支持随机存取，占用额外指针空间。
- **索引存储**：建立附加索引表，索引项形式（关键字，地址）。
- **散列存储**：由关键字直接计算存储地址（哈希）。

**数据的运算：** 定义在逻辑结构上，实现在存储结构上。基本运算：插入、删除、查找、更新、排序等。

> 逻辑结构是唯一的，但同一种逻辑结构可以用多种存储结构实现。

### 3. 算法与算法分析
**算法特性：**
1. **有穷性**：有限步骤后必须结束。
2. **确定性**：每条指令有明确含义，相同输入得到相同输出。
3. **可行性**：操作可通过基本运算实现。
4. **输入**：0个或多个输入。
5. **输出**：1个或多个输出。

**算法设计目标：** 正确性、可读性、健壮性、高效性（时间+空间）。

**时间复杂度**：算法中基本运算执行次数用大O表示，取最坏情况。
**空间复杂度**：算法运行所需额外内存空间（不含输入本身）。

> 常见复杂度排序：O(1) < O(log n) < O(n) < O(nlog n) < O(n²) < O(2ⁿ) < O(n!)

**复杂度计算规则：**
- 加法规则：T(n) = T1(n) + T2(n) = O(max(f(n), g(n)))
- 乘法规则：T(n) = T1(n) × T2(n) = O(f(n) × g(n))

---

## 二、线性表

### 1. 线性表定义
线性表是具有相同数据类型的 n（n≥0）个数据元素的有限序列。n=0 为空表。
- 逻辑特征：除首元素外每个元素有唯一前驱，除尾元素外每个元素有唯一后继。
- 元素之间的关系由序号决定，位序从1开始。

### 2. 顺序表（顺序存储）
- 用一组地址连续的存储单元依次存储线性表中的元素。
- **随机存取**特性：通过下标直接访问，O(1)。
- 元素地址计算：LOC(ai) = LOC(a1) + (i-1) × sizeof(ElemType)

**顺序表基本操作复杂度：**
- 按下标访问：O(1)
- 按值查找：O(n)
- 插入（第 i 个位置）：平均移动 n/2 次，O(n)
- 删除（第 i 个位置）：平均移动 (n-1)/2 次，O(n)

### 3. 链表（链式存储）

**单链表**：每个结点含 data + next 指针。
- **头结点作用**：使空表与非空表操作统一；便于在表头位置插入/删除，无需特殊处理。
- 建立链表：头插法（逆序生成）、尾插法（顺序生成）。

**双链表**：每个结点含 data + prior + next，可双向访问，删除结点 O(1)。
**循环链表**：尾结点指针指向头结点，可从任意点遍历全表。
**静态链表**：用数组实现，借助游标（cursor）代替指针。

**链表基本操作复杂度：**
- 按序号查找：O(n)
- 按值查找：O(n)
- 插入/删除（已知结点位置）：单链表插入 O(1)，但删除需先找前驱 O(n)；双链表插入删除均 O(1)。

### 4. 顺序表 vs 链表对比

| 项目 | 顺序表 | 链表 |
|------|--------|------|
| 逻辑/物理 | 逻辑相邻=物理相邻 | 不一定相邻，用指针联系 |
| 存取方式 | 随机存取 O(1) | 顺序存取 O(n) |
| 插入/删除 | 需移动元素 O(n) | 修改指针 O(1)（查找仍 O(n)） |
| 空间分配 | 静态预分配，可能溢出/浪费 | 动态分配，按需申请 |
| 存储密度 | 高（无额外指针） | 低（每个结点存指针） |
| 适用场景 | 表长稳定、查询多 | 频繁增删、长度变化大 |

---

## 三、栈

### 1. 栈的定义
**栈（Stack）**：后进先出（LIFO）的线性表，只能在栈顶进行插入（入栈 push）和删除（出栈 pop）。
- 栈底固定，栈顶浮动。
- n 个不同元素入栈，可能的合法出栈序列数为**卡特兰数** C(n) = (2n)! / ((n+1)! × n!)

### 2. 栈的存储结构
**顺序栈**：用数组实现，top 指针指向栈顶元素。
- 栈空：top == -1（约定实现）
- 栈满：top == MaxSize - 1
- 入栈：stack[++top] = x；出栈：x = stack[top--]

**共享栈**：两个栈共享一片空间，栈底分别在数组两端，栈顶相向生长，提高空间利用率，栈满条件：top0 + 1 == top1。

**链栈**：用链表实现，头结点作为栈顶，不存在栈满溢出问题。

### 3. 栈的应用
- **括号匹配**：左括号入栈，右括号与栈顶匹配则出栈。
- **表达式求值**：中缀转后缀、后缀表达式计算。
- **递归调用**：每层调用入栈，返回时出栈；递归深度过大会栈溢出。
- **函数调用栈**：保存返回地址、参数、局部变量。
- **浏览器后退/前进**、**编辑器撤销/重做**。

### 4. 中缀转后缀（重点）
**规则：**
1. 操作数直接输出。
2. 运算符入栈，入栈前将栈中优先级 ≥ 当前运算符的依次弹出输出。
3. 左括号直接入栈，遇右括号则弹出至左括号为止（括号不输出）。
4. 输入结束，栈中剩余运算符依次弹出。

**优先级**：乘除 > 加减；括号内优先计算。

例：A+B*(C-D)-E/F → 后缀：ABCD-*+EF/-

### 5. 后缀表达式求值
- 遇操作数入栈，遇运算符弹出两个操作数计算后结果入栈。
- 注意：减法/除法的操作数顺序，先弹出的是右操作数。

---

## 四、队列

### 1. 队列的定义
**队列（Queue）**：先进先出（FIFO）的线性表，队尾入队（rear）、队头出队（front）。

### 2. 队列的顺序存储——循环队列（重点）
将数组视为环形，front 指向队头元素，rear 指向队尾元素的下一个位置。
- 入队：Q[rear] = x; rear = (rear + 1) % MaxSize
- 出队：x = Q[front]; front = (front + 1) % MaxSize
- **判空：front == rear**
- **判满（牺牲一个单元）：(rear + 1) % MaxSize == front**
- **元素个数：(rear - front + MaxSize) % MaxSize**

> 另一种判满方式：
> 1. 增设 size 变量记录元素个数，size==MaxSize 时满，size==0 时空。
> 2. 增设 tag 标记最后一次操作（入队 tag=1，出队 tag=0），front==rear 且 tag=1 时满。

### 3. 链式队列
用带头结点的单链表实现，front 指向头结点，rear 指向尾结点。不存在队满问题（除非内存耗尽）。空队条件：front == rear 且都指向头结点。

### 4. 双端队列
两端均可进行入队、出队操作的队列。
- **输入受限的双端队列**：一端只能入队，另一端可入可出。
- **输出受限的双端队列**：一端只能出队，另一端可入可出。

> 输出序列判断：栈的输出序列是双端队列输出序列的子集；双端队列的能力介于栈和队列之间。

---

## 五、串

### 1. 串的定义
**串（String）**：由零个或多个字符组成的有限序列。n=0 为空串。
- **子串**：串中任意连续字符组成的子序列。
- **主串**：包含子串的串。
- **位置**：字符在串中的序号。
- **串相等**：长度相等且对应位置字符相同。
- 空串 ≠ 空格串：空串长度为0，空格串长度为空格数。

### 2. 串的存储结构
- **定长顺序存储**：固定长度数组，超出截断。
- **堆分配存储**：动态分配的连续空间。
- **块链存储**：链表存储，每个结点存多个字符，最后结点可能不满。

### 3. 串的模式匹配
在主串中查找子串（模式串）的位置。

**朴素模式匹配（BF算法）：**
- 主串长 n，模式串长 m。
- 思路：从主串每个位置开始与模式串逐字符比较，失配则主串指针回退到下一个起点。
- 最坏时间复杂度 **O(n×m)**。

### 4. KMP算法（重点）
**核心思想**：利用已匹配信息，主串指针不回退，模式串指针根据 next 数组滑动。

**时间复杂度**：O(n + m)。

**next数组求法（手算重点）：**
- 约定下标从1开始。
- next[1] = 0，next[2] = 1（固定值）。
- next[j]：模式串第 j 个字符之前（不含 j）的串 P[1..j-1] 中，最长相等前后缀长度 k，则 next[j] = k + 1。
- 即求真前缀集合与真后缀集合的最长公共元素长度。

**求法示例**：模式串 "ababaaab"

| j | 1 | 2 | 3 | 4 | 5 | 6 |
|---|---|---|---|---|---|---|
| 模式串 | a | b | a | b | a | a |
| next[j] | 0 | 1 | 1 | 2 | 3 | 4 |

**匹配过程：**
- 主串指针 i 不回退。
- 失配时：j = next[j]，若 next[j]=0 则 i++, j++（即 j=1 重新开始）。

### 5. nextval数组（next数组优化）
若 next[j] 指向的字符与第 j 个字符相同，则 nextval[j] = nextval[next[j]]；否则 nextval[j] = next[j]。

改进后的 KMP 用 nextval 替代 next，减少不必要的比较。

### 6. KMP常考点
- KMP 主串指针不回退，仅模式串指针滑动。
- next 数组仅与模式串有关，与主串无关。
- next[1]=0, next[2]=1 是固定值（从1编号）。
- 最坏比较次数 O(n+m)。
- next 数组求解是高频考点，务必手算熟练。

---

## 六、考点速记

- 数据结构三要素：**逻辑 + 存储 + 运算**。
- 顺序表随机存取 O(1)，链表顺序存取 O(n)。
- 顺序表插入平均移动 n/2，删除平均移动 (n-1)/2。
- 栈 LIFO，队列 FIFO；n 个元素入栈合法出栈序列数为卡特兰数。
- 循环队列元素个数 = (rear - front + MaxSize) % MaxSize。
- 循环队列判满用 (rear+1)%MaxSize==front（牺牲一个单元）。
- KMP 时间 O(n+m)，next[1]=0, next[2]=1，主串不回退。
- 头结点作用：统一空表非空表操作，便于表头增删。
- 双链表删除已知结点 O(1)，单链表需 O(n) 找前驱。
`,
    timestamp: "2026-08-06 18:30",
    embed: ``
};

export default [note1];
