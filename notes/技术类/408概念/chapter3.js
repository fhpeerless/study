export const note1 = {
    title: "数据结构（三）：图",
    contentmd: `
# 数据结构（三）：图

> 定位：408考研数据结构第三章。重点掌握：图的存储结构对比、BFS/DFS遍历、最小生成树（Prim/Kruskal）、最短路径（Dijkstra/Floyd）、拓扑排序、关键路径。

---

## 一、图的基本概念

### 1. 图的定义
**图（Graph）**：G = (V, E)，V 是顶点集（非空有限集），E 是边集。
- **有向图**：边有方向，称为弧，用 <v,w> 表示（v 为弧尾，w 为弧头）。
- **无向图**：边无方向，用 (v,w) 表示。
- **简单图**：无自环、无重边。408 默认讨论简单图。

### 2. 基本术语
**完全图：**
- 无向完全图：任意两顶点间有边，共 **n(n-1)/2** 条。
- 有向完全图：任意两顶点间有方向相反两条弧，共 **n(n-1)** 条。

**度：**
- 无向图顶点的度 = 与之相连的边数。
- 有向图分**入度**（以该点为头的弧数）和**出度**（以该点为尾的弧数），度=入度+出度。
- **所有顶点度数之和 = 2×边数**（无向）；有向图入度和=出度和=边数。

**其他术语：**
- **子图、生成子图**。
- **路径、路径长度（边数）、回路/环、简单路径（不重复）、简单环**。
- **权、网**：带权图称为网。

### 3. 连通性
- **连通**：无向图两顶点间有路径则连通。
- **连通图**：无向图任意两顶点连通。
- **连通分量**：无向图的极大连通子图。
- **强连通图**：有向图任意两顶点相互可达。
- **强连通分量**：有向图的极大强连通子图。
- **生成树**：连通图的极小连通子图，含 n 个顶点 n-1 条边。
- **生成森林**：非连通图各连通分量的生成树构成。

### 4. 图的重要性质
- 无向连通图：边数 ≥ n-1；若边数 > n-1，必有环。
- 无向连通图所有顶点度数之和 = 2×边数。
- n 个顶点的连通图最少 n-1 条边，最多 n(n-1)/2 条。
- n 个顶点的强连通图最少 n 条边（构成环）。

---

## 二、图的存储结构

### 1. 邻接矩阵
用二维数组 A[n][n] 存储：
- 无向图：A[i][j]=1 表示有边；矩阵**对称**。
- 有向图：A[i][j]=1 表示有 <i,j> 弧。
- 带权图：用权值或 ∞ 表示。

**特点：**
- 空间 O(n²)，适合**稠密图**。
- 判断两点是否相邻：O(1)。
- 求度：无向图第 i 行/列和；有向图第 i 行和为出度，第 i 列和为入度。
- 矩阵 A 的 k 次幂 A^k[i][j] 表示 i 到 j 长度为 k 的路径数。

### 2. 邻接表
为每个顶点建一个单链表，存储与该顶点相邻的顶点。

**特点：**
- 空间 O(n+e)，适合**稀疏图**。
- 无向图：每条边存两次，需 2e 个边结点。
- 有向图：每条弧存一次，e 个边结点；便于求**出度**，求入度需建逆邻接表。
- 判断两点是否相邻：O(度)。

### 3. 邻接矩阵 vs 邻接表

| 项目 | 邻接矩阵 | 邻接表 |
|------|---------|--------|
| 空间 | O(n²) | O(n+e) |
| 适用 | 稠密图 | 稀疏图 |
| 判邻接 | O(1) | O(度) |
| 求度 | O(n) | 无向易；有向分入/出度 |
| 找所有邻接点 | O(n) | O(度) |

### 4. 十字链表与邻接多重表
- **十字链表**：有向图的链式存储，同时表示入边和出边，结合邻接表与逆邻接表。
- **邻接多重表**：无向图的链式存储，每条边只存一次，便于边的操作（删除等）。

---

## 三、图的遍历

### 1. 广度优先搜索 BFS
**思想**：类似树的层序遍历，从某顶点出发，访问其所有未访问邻接点，再依次以这些邻接点出发。

**实现**：**队列**辅助。
- 访问起点，入队。
- 出队一个顶点，访问其所有未访问邻接点并入队。
- 重复直到队空。

**性能：**
- 时间复杂度：邻接矩阵 O(n²)，邻接表 O(n+e)。
- 空间复杂度：O(n)（队列+访问标记）。

**应用：**
- BFS 生成树的边集称为**广度优先生成树**。
- **BFS 可求无权图单源最短路径**（层序即距离）。
- 非连通图需多次 BFS，得**广度优先生成森林**。

### 2. 深度优先搜索 DFS
**思想**：类似树的先序遍历，沿一条路径深入到底，再回溯。

**实现**：**递归/栈**。
- 访问当前顶点，标记已访问。
- 对其未访问邻接点依次 DFS。

**性能：**
- 时间复杂度：邻接矩阵 O(n²)，邻接表 O(n+e)。
- 空间复杂度：O(n)（递归栈深）。

### 3. 遍历应用
- **连通性判断**：一次 BFS/DFS 能访问所有顶点 ⇔ 图连通。
- **连通分量**：对每个未访问顶点做一次 BFS/DFS，次数=连通分量数。
- **生成树/森林**：遍历经过的边构成生成树；非连通图得生成森林。
- BFS 生成树高度 ≤ DFS 生成树高度（BFS 更"矮"）。
- 有向图 DFS 可判断是否有环。

---

## 四、最小生成树（MST）

**定义**：带权连通无向图中，权值之和最小的生成树。MST 不一定唯一，但权值和唯一。

### 1. Prim 算法（点贪心）
**思想**：从某顶点开始，每次选择连接"已选顶点集合 U"和"未选顶点集合 V-U"的**权值最小边**，将对应顶点加入 U，直到 U=V。

**步骤：**
1. 初始 U={任意顶点}。
2. 在所有 (u,v) 中（u∈U, v∈V-U）选权值最小边，v 加入 U。
3. 重复直到 U=V。

**性能：**
- 时间复杂度：O(n²)。
- 适合**稠密图**。

### 2. Kruskal 算法（边贪心）
**思想**：将所有边按权值升序排列，依次选最小边加入 MST，若加入后不形成环则保留，否则跳过，直到选够 n-1 条边。

**步骤：**
1. 所有边按权值升序排序。
2. 依次选最小边，若两端不在同一连通分量（用**并查集**判环），则加入 MST。
3. 重复直到选够 n-1 条边。

**性能：**
- 时间复杂度：O(e log e)（主要在排序）。
- 适合**稀疏图**。

### 3. MST 性质
- 最小生成树唯一 ⇔ 所有边权值均不相同（充分不必要）。
- 若图中有权值相同的边，MST 可能不唯一，但 MST 的权值和唯一。
- Prim 适合稠密图（点少边多），Kruskal 适合稀疏图（边少）。

---

## 五、最短路径

### 1. Dijkstra 算法（单源最短路径）
**思想**：贪心，逐步扩展已确定最短路径的顶点集合 S。

**步骤：**
1. 初始化：源点 v0 距离 0，其他 ∞，S={v0}。
2. 选当前 dist 最小的未加入 S 的顶点 u 加入 S。
3. 以 u 为中转更新所有未在 S 中顶点的 dist：dist[w] = min(dist[w], dist[u] + 权(u,w))。
4. 重复 2-3 直到所有顶点加入 S。

**特点：**
- 时间复杂度：O(n²)。
- **不能处理负权边**（贪心策略失效，加入 S 后不再修改）。
- 每次加入一个顶点确定其最短路径，不可修改。
- 适合稠密图（邻接矩阵）。

### 2. Floyd 算法（各顶点对间最短路径）
**思想**：动态规划，逐步允许中间顶点。

**状态转移**：A[k][i][j] = min(A[k-1][i][j], A[k-1][i][k] + A[k-1][k][j])
- 即允许经过顶点 0~k 时 i 到 j 的最短路径。

**步骤：**
- 初始化 A[0][i][j] = 权值或 ∞。
- 对每个中间顶点 k，更新所有 A[i][j]。

**特点：**
- 时间复杂度：O(n³)，空间 O(n²)。
- **允许负权边**，但**不能有负权回路**。
- 代码简单，三重循环。

### 3. 对比

| 项目 | Dijkstra | Floyd |
|------|---------|-------|
| 问题 | 单源最短路径 | 所有顶点对 |
| 时间 | O(n²) | O(n³) |
| 负权边 | 不支持 | 支持 |
| 负权回路 | 不支持 | 不支持 |
| 思想 | 贪心 | 动态规划 |

### 4. 其他最短路径注意
- BFS 可求无权图最短路径 O(n+e)。
- DAG 可用拓扑排序+动态规划求最短路径，可处理负权。

---

## 六、拓扑排序（AOV网）

### 1. AOV网
**AOV网（Activity On Vertex）**：用顶点表示活动、边表示活动间先后关系的有向无环图（DAG）。

### 2. 拓扑排序
将 AOV 网中所有顶点排成线性序列，使得对任意有向边 <u,v>，u 在序列中位于 v 之前。

**算法：**
1. 选择一个**入度为0**的顶点输出。
2. 删除该顶点及以其为尾的所有弧（即将其后继顶点入度减1）。
3. 重复 1-2 直到全部输出或无入度为0的顶点。
4. 若输出顶点数 < 总顶点数，说明图中**有环**。

**特点：**
- 拓扑排序序列不唯一（除非每步只有一个入度为0的顶点）。
- **存在拓扑排序 ⇔ 图为有向无环图（DAG）**。
- 用邻接表存储，时间复杂度 O(n+e)。
- 入度为0的顶点可存入栈/队列，不影响合法性。

### 3. 逆拓扑排序
- 输出出度为0的顶点，删除其入边。
- 也可用 DFS 完成逆拓扑排序（出栈时输出）。

---

## 七、关键路径（AOE网）

### 1. AOE网
**AOE网（Activity On Edge）**：用边表示活动、顶点表示事件（活动完成时刻）的带权有向无环图。权为活动持续时间。
- 只有一个源点（入度为0，开始点）和一个汇点（出度为0，结束点）。
- AOE 网用于估算工程最短工期。

### 2. 相关概念
- **事件 v 的最早发生时间 ve(v)**：从源点到 v 的最长路径长度。
- **事件 v 的最迟发生时间 vl(v)**：在不延误工期的前提下，v 最迟发生时间。
- **活动 a 的最早开始时间 e(a)**：a 的弧尾事件的 ve 值。
- **活动 a 的最迟开始时间 l(a)**：a 的弧头事件的 vl 值 - a 的持续时间。
- **活动时间余量 d(a) = l(a) - e(a)**。

### 3. 关键路径
**关键路径**：从源点到汇点的**最长路径**，其长度为工程最短工期。
- **关键活动**：d(a) = 0 的活动，即时间余量为0，必须按时完成。
- 关键路径上的所有活动都是关键活动。

**求解步骤：**
1. 拓扑排序求各事件 ve（正序：ve(v) = max{ve(u)+w(u,v)}）。
2. 逆拓扑排序求各事件 vl（逆序：vl(u) = min{vl(v)-w(u,v)}）。
3. 对每条边（活动）求 e 和 l，d = l - e。
4. d=0 的活动为关键活动，连接起来为关键路径。

### 4. 注意事项
- 关键路径可能不止一条。
- 缩短关键活动时间可缩短工期，但只有缩短**所有关键路径共有的关键活动**才有效。
- 缩短某关键活动时间到一定程度后，它可能不再是关键活动，关键路径可能改变。
- 关键活动延长则工期延长；非关键活动在余量内延长不影响工期。

---

## 八、考点速记

- 完全图边数：无向 n(n-1)/2，有向 n(n-1)。
- 度数之和=2×边数（无向）；入度和=出度和=边数（有向）。
- n 顶点连通图最少 n-1 边；强连通图最少 n 边。
- 邻接矩阵适合稠密图 O(n²)，邻接表适合稀疏图 O(n+e)。
- BFS 用队列、DFS 用栈/递归；时间均为 O(n²) 或 O(n+e)。
- Prim O(n²) 适稠密图、Kruskal O(e log e) 适稀疏图。
- Dijkstra 单源 O(n²) 不能负权；Floyd 全源 O(n³) 可负权不可负环。
- 拓扑排序判环：输出顶点数 < 总顶点数则有环。
- 关键路径 = 最长路径，关键活动时间余量 d = l - e = 0。
- AOV网用顶点表示活动，AOE网用边表示活动。
`,
    timestamp: "2026-08-06 18:30",
    embed: ``
};

export default [note1];
