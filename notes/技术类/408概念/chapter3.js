export const note1 = {
    title: "操作系统速通复习",
    contentmd: `
# 操作系统 · 408考试速通复习

> 定位：概念+高频考点速记。重点掌握：进程状态与同步（PV操作）、调度算法、死锁与银行家算法、分页/虚拟内存与页面置换、磁盘调度。

---

## 一、操作系统概述

**定义：** 管理计算机硬件与软件资源的系统软件，是用户与计算机硬件之间的接口。

**四大特征（必背）：**
1. **并发**：多个事件在同一时间间隔内发生（宏观并行、微观交替）。
2. **共享**：资源供多个并发进程共同使用（互斥共享 / 同时共享）。
3. **虚拟**：把物理实体变为逻辑上的多个（虚拟内存、虚拟设备SPOOLing）。
4. **异步**：进程执行不是一贯到底，走走停停。

> 并发与共享是操作系统最基本的两个特征，二者互为存在条件。

**五大功能：** 处理机管理（进程调度）、存储器管理、设备管理、文件管理、提供用户接口。

**操作系统分类：** 批处理、分时（时间片轮转，交互性）、实时（及时响应，硬/软实时）、网络式、分布式、嵌入式。

---

## 二、进程与线程

### 1. 进程

- **进程**：程序的一次执行过程，是**资源分配**的基本单位。
- **进程控制块 PCB**：进程存在的唯一标志，包含进程标识符、状态、程序计数器、寄存器、内存信息等。

**五状态模型（必背）：** 创建 → 就绪 ↔ 运行 → 终止；运行 → 阻塞（等待事件）→ 就绪（事件完成）。
- 就绪→运行：被调度；运行→就绪：时间片到/被抢占；运行→阻塞：主动等待事件。

### 2. 进程同步与互斥

- **临界资源**：一次只允许一个进程使用的资源；**临界区**：访问临界资源的代码段。
- 同步（直接制约）：有序推进；互斥（间接制约）：不能同时使用同一资源。
- **信号量**：整型变量 + 队列；**PV操作**（原语，不可中断）：
  - \`P(S)\`：S减1，若 S<0 则进程阻塞进队列。
  - \`V(S)\`：S加1，若 S≤0 则唤醒队列中一个进程。
- **管程**：把共享资源及其操作封装起来，同一时间仅一个进程调用。

**经典同步问题（必考）：**
- **生产者-消费者**：两个信号量 empty（空缓冲数）、full（满缓冲数），再加互斥信号量 mutex。
- **读者-写者**：读者可同时读，写者互斥；用 count 计数器 + mutex 保护 count。
- **哲学家进餐**：互斥取筷子 + 避免死锁（最多允许4人同时拿筷子 或 只拿一只再判断）。

### 3. 进程通信

- 低级通信：信号量（同步互斥）。
- 高级通信：**共享存储**（直接共享内存区）、**消息传递**（send/receive）、**管道通信**（半双工，一端写一端读）。

### 4. 线程

- **线程**：轻量级进程，是**CPU调度**的基本单位；进程仍是资源分配的基本单位。
- 引入线程后：进程内线程共享进程的资源（地址空间、打开的文件）。
- 线程不拥有独立资源（只有运行必需的一点资源：栈、寄存器）。
- 用户级线程（内核不感知）/ 内核级线程（由内核管理）。

---

## 三、处理机调度与死锁

### 1. 调度算法（必背）

| 算法 | 原则 | 特点 | 是否抢占 |
|------|------|------|---------|
| FCFS 先来先服务 | 到达顺序 | 公平，但长作业拖短作业 | 非抢占 |
| SJF 短作业优先 | 运行时间最短 | 平均等待时间最小，但可能饥饿 | 可抢占（SRTF） |
| 优先级调度 | 优先级高低 | 高优先级先执行，可能饥饿 | 可/不可 |
| RR 时间片轮转 | 轮流给时间片 | 响应快，时间片大小影响性能 | 抢占 |
| 多级反馈队列 | 多队列+动态优先级 | 综合型，兼顾响应与吞吐 | 抢占 |

> 判平均等待时间/平均周转时间：**周转时间 = 完成时间 - 到达时间**；带权周转时间 = 周转/运行。

### 2. 死锁

**死锁产生必须同时满足4个必要条件：**
1. **互斥**：资源不可共享。
2. **占有并等待**：持有资源又申请新资源。
3. **不可剥夺**：资源不能被强行抢走。
4. **循环等待**：存在循环等待链。

**处理策略：**
- **预防**：破坏4个必要条件之一（如资源一次性分配破坏"占有并等待"）。
- **避免**：**银行家算法**——每次分配前判断系统是否安全（存在安全序列则安全）。
- **检测**：资源分配图化简，检测是否存在死锁。
- **解除**：撤销进程、资源剥夺、回退。

---

## 四、内存管理

### 1. 连续分配

- 动态分区分配算法：
  - **首次适应**：按地址递增找第一个够大的空闲分区（利用率较好，最常用）。
  - **最佳适应**：找最小的够用分区（产生最多小碎片）。
  - **最坏适应**：找最大的分区。
- 碎片：外部碎片（不连续空闲块）→ 用紧凑（拼接）解决。

### 2. 非连续分配（重点）

| 方式 | 特点 | 地址变换 |
|------|------|---------|
| **分页** | 固定大小页，物理上离散，逻辑上连续 | 页号→页表→物理块号 + 页内偏移 |
| **分段** | 按程序逻辑划分，段长可变 | 段号→段表→段基址 + 段内偏移 |
| **段页式** | 先分段再分页 | 段表+页表两级查 |

- 页大小 = 2^k，页内偏移占 k 位，页号 = 地址 >> k。
- 多级页表：解决页表过大；快表 TLB：缓存页表项加速。
- 分段易实现共享与保护（段是逻辑单位）；分页不易。

### 3. 虚拟内存

- 依据**局部性原理**（时间局部性：刚访问的会再访问；空间局部性：邻近地址会被访问）。
- **请求分页**：页面只装入用到的部分，用到才调入（缺页中断）。
- 页面置换算法：
  - **OPT 最佳**：置换未来最久不用的页（理论最优，不可实现）。
  - **FIFO**：先进先出，可能产生**Belady异常**（页框增多反而缺页率升高）。
  - **LRU 最近最久未使用**：置换最久没用的页，性能好，用栈/计数器实现。
  - **CLOCK（时钟）**：近似LRU，页面加访问位，一次循环清除。
- **抖动（颠簸）**：页框过少导致频繁缺页，应通过增加内存、调整工作集解决。
- **工作集**：某时间段内实际访问的页面集合。

---

## 五、文件管理

- **文件逻辑结构**：无结构（流式）、有结构（记录式：顺序/索引）。
- **文件物理结构：**
  - 连续分配：随机存取快，但产生外部碎片、文件难扩展。
  - 链接分配：无碎片，只能顺序存取（隐式/显式FAT表）。
  - 索引分配：为每个文件建索引块，随机存取，可动态扩展。
- **目录结构**：单级、两级、树形（最常用）、无环图（支持共享）。
- **文件控制块 FCB**：文件名、类型、物理地址、权限等；目录项即FCB。
- **空闲空间管理：** 空闲表法、空闲链表法、**位示图法**、成组链接法。
- **磁盘调度算法（必背）：**
  - **FCFS**：按请求先后。
  - **SSTF 最短寻道优先**：优先最近的磁道（可能饥饿）。
  - **SCAN 电梯算法**：单方向移动并服务沿途请求，到达一端再反向。
  - **C-SCAN 循环扫描**：只单向服务，返回时快速回端。

---

## 六、I/O 管理

- **I/O设备分类：** 块设备（磁盘，以块为单位，可寻址）/ 字符设备（键盘、打印机，以字符为单位）。
- **I/O控制方式：** 程序直接控制（查询）、中断驱动、**DMA**（直接存储器存取，成块传送）、通道控制。
- **缓冲技术：** 缓和CPU与I/O速度矛盾；单缓冲、双缓冲、循环缓冲、缓冲池。
- **SPOOLing（假脱机）技术：** 用磁盘模拟独占设备 → 变为共享的"虚拟设备"（如打印机）。
- **设备独立性：** 应用程序使用逻辑设备名，与物理设备无关，由OS映射。

> **易错点：** 通道是专门执行I/O指令的处理器，CPU只需发出I/O指令，其余交给通道。
`,
    timestamp: "2026-08-06 09:00",
    embed: ``
};

export default [note1];
