export const note1 = {
    title: "计算机组成原理（三）：存储系统",
    contentmd: `
# 计算机组成原理（三）· 存储系统

> 定位：408考研复习第三章。重点掌握：存储器层次结构、SRAM vs DRAM对比、主存扩展（字/位/字位同时）、Cache映射方式与计算、替换算法、写策略、多级Cache层次（VIPT/PIPT/包含策略）、Cache性能优化（Victim Cache/Non-blocking/硬件预取）、虚拟存储器（多级页表/TLB/倒排页表/段页式/大页机制/内存保护）、三级存储层次。

---

## 一、存储器层次结构

### 1. 存储器分类

| 分类维度 | 类型 |
|---------|------|
| 按存储介质 | 半导体存储器、磁表面存储器、光存储器 |
| 按存取方式 | 随机存取（RAM）、只读（ROM）、顺序存取（磁带）、直接存取（磁盘） |
| 按可读写性 | RAM、ROM |
| 按易失性 | 易失性（RAM）、非易失性（ROM、磁盘） |
| 按所处位置 | 主存（内存）、辅存（外存）、Cache、寄存器 |

### 2. 存储器层次结构（必考）

**层次（自上而下，速度递减、容量递增、价格递减）：**

    寄存器（CPU内）
       ↓
    Cache（高速缓存）
       ↓
    主存（内存）
       ↓
    辅存（外存：硬盘、U盘）

| 层次 | 速度 | 容量 | 价格 | 是否在CPU内 |
|------|------|------|------|------------|
| 寄存器 | 最快 | 最小 | 最贵 | 是 |
| Cache | 快 | 小 | 贵 | 部分集成 |
| 主存 | 中 | 中 | 中 | 否 |
| 辅存 | 慢 | 大 | 便宜 | 否 |

### 3. 局部性原理（程序访问的局部性）

- **时间局部性**：近期访问过的信息，不久可能再次访问（如循环变量、热点代码）。
- **空间局部性**：访问某地址后，其临近地址也可能被访问（如顺序执行的指令、数组）。
- **作用**：局部性原理是Cache和虚拟存储器工作的基础。

### 4. 层次间的数据传递单位

| 层次 | 传递单位 |
|------|---------|
| 寄存器-Cache | 字（机器字长） |
| Cache-主存 | 块（Block，几十字节） |
| 主存-辅存 | 页/段（几KB） |

---

## 二、半导体存储器

### 1. SRAM vs DRAM（高频考点）

| 对比项 | SRAM（静态随机存储器） | DRAM（动态随机存储器） |
|--------|----------------------|----------------------|
| 存储原理 | 触发器 | 电容电荷 |
| 是否需刷新 | 否 | 是（电容漏电，需定期刷新） |
| 集成度 | 低 | 高 |
| 速度 | 快 | 慢 |
| 功耗 | 大 | 小 |
| 成本 | 高 | 低 |
| 易失性 | 易失 | 易失 |
| 主要用途 | **Cache** | **主存** |

### 2. DRAM刷新

- **刷新原因**：电容存储的电荷会随时间泄漏，需定期补充。
- **刷新方式**：
  - **集中刷新**：在固定时间段内集中刷新所有行，存在"死时间"（不能读写）。
  - **异步刷新**：每隔一段时间刷新一行，刷新分散在各段时间，死时间短。
  - **分散刷新**：每个存取周期中前半段读写、后半段刷新，无死时间但存取周期变长。
- **刷新周期**：通常 2ms。
- **按行刷新**：以行为单位，使用行地址计数器自动刷新。

### 3. ROM类型

| 类型 | 说明 | 用途 |
|------|------|------|
| **掩膜ROM** | 出厂写入，不可改 | 固件 |
| **PROM** | 用户可写一次（熔丝） | 出厂后定制 |
| **EPROM** | 紫外线擦除后可重写 | 早期BIOS |
| **EEPROM** | 电可擦除 | 现代BIOS、小容量存储 |
| **Flash（闪存）** | 电可擦除，按块擦除 | U盘、SSD、嵌入式 |

- ROM特点：**非易失、随机读取、不能随机写入**。

---

## 三、主存储器

### 1. 主存基本组成

**主存 = 存储体 + MAR + MDR + 时序控制逻辑**

- **存储体**：由若干存储单元组成，每个存储单元存放一个存储字。
- **MAR**：存放CPU要访问的存储单元地址。
- **MDR**：存放从存储器读出或要写入存储器的数据。
- **地址译码器**：将MAR中的地址翻译为字线选择信号。

### 2. 主存编址

- **按字节编址**：每个存储单元8位，地址最小单位为字节。
- **按字编址**：每个存储单元为一个机器字长。
- **MAR位数**：决定可寻址存储单元数，2^MAR位 = 单元数。
- **MDR位数**：等于存储字长。

### 3. 存储单元寻址

- 存储器地址译码方式：**线选法**（每根字线选一个单元）、**重合法**（行+列译码，省地址线）。
- DRAM采用地址复用：行地址、列地址分两次送入（地址线减半）。

### 4. 主存扩展（高频考点）

| 扩展方式 | 说明 | 适用场景 |
|---------|------|---------|
| **位扩展** | 增加存储字长，多片并联，地址线共用 | 字长不足，如用4片8K×1组成8K×4 |
| **字扩展** | 增加存储字数，多片串联，片选译码 | 字数不足，如用2片1K×8组成2K×8 |
| **字位同时扩展** | 既有字扩展又有位扩展 | 实际工程常用 |

**字扩展片选方法：**
- **线选法**：高位地址线直接作为片选信号，简单但浪费地址空间。
- **译码片选法**：用译码器将高位地址译为多个片选信号，地址空间连续。

### 5. 主存扩展计算示例

- 用 16K×4 位芯片组成 64K×8 位存储器：
  - 位扩展：2片并联（4位 → 8位）
  - 字扩展：4组（16K → 64K）
  - 共需 2 × 4 = 8 片
  - 片选：64K/16K = 4 组，需 2 位高位地址译码

---

## 四、Cache高速缓存

### 1. Cache基本原理

**核心思想**：利用局部性原理，把主存中活跃的数据块复制到Cache，CPU直接访问Cache，减少访存时间。

- **命中率**：CPU要访问的信息在Cache中的比例。
- **缺失率** = 1 - 命中率
- **平均访问时间**：t = h × t_cache + (1 - h) × t_main，其中 h 为命中率
- **加速比** = t_main / t

### 2. Cache-主存映射方式（必考重点）

| 映射方式 | 规则 | 特点 |
|---------|------|------|
| **直接映射** | 主存块只能放在Cache固定位置：**块号 mod Cache总块数** | 实现简单、冲突多、空间利用率低 |
| **全相联映射** | 主存块可放在Cache任意位置 | 冲突少、查找慢、需并行比较 |
| **组相联映射** | 主存块放在固定组内任意位置：**块号 mod Cache组数** | 折中方案，最常用 |

### 3. 主存地址结构

| 映射方式 | 地址结构 |
|---------|---------|
| **直接映射** | 主存字块标记 Tag + Cache字块号 + 块内地址 |
| **全相联映射** | 主存字块标记 Tag + 块内地址 |
| **组相联映射** | 主存字块标记 Tag + 组地址 + 块内地址 |

**Tag位数计算**：主存块数位数 - Cache块数位数（直接/组相联），或 = 主存块数位数（全相联）。

### 4. 三种映射对比表

| 对比项 | 直接映射 | 全相联映射 | 组相联映射 |
|--------|---------|-----------|-----------|
| 块放置位置 | 固定1个位置 | 任意位置 | 固定组内任意位置 |
| 查找方式 | 按位置查找 | 全部比较 | 组内并行查找 |
| 命中速度 | 最快 | 最慢 | 中等 |
| 冲突率 | 高 | 低 | 中等 |
| 实现复杂度 | 简单 | 复杂 | 中等 |
| 适用 | 大容量Cache | 小容量Cache | 折中 |

### 5. Cache替换算法

当Cache满时，需替换出某块：

| 算法 | 思想 | 优缺点 |
|------|------|--------|
| **FIFO（先进先出）** | 替换最早进入Cache的块 | 简单，但可能替换热点块 |
| **LRU（最近最少使用）** | 替换最久未访问的块 | 命中率高，但需记录使用时间 |
| **LFU（最不经常使用）** | 替换访问次数最少的块 | 适合长期访问模式 |
| **RAND（随机）** | 随机替换 | 简单但命中率不稳定 |

- **LRU**是Cache最常用的替换算法。
- 命中率最高的是LRU，但实现最复杂；FIFO简单但有"抖动"现象。

### 6. Cache写策略（重点）

| 写策略 | 方式 | 特点 |
|--------|------|------|
| **写直达（全写法）** | 写Cache同时写主存 | 简单，但写速度慢 |
| **写回法** | 只写Cache，被替换时才写回主存 | 速度快，需设脏位 |
| **写分配法** | 写缺失时先把块调入Cache再写 | 配合写回法 |
| **不按写分配法** | 写缺失时直接写主存 | 配合写直达法 |

**写策略搭配**：
- 写直达 + 不按写分配
- 写回法 + 写分配

### 7. Cache设计要点

- **Cache容量** = 行数 × 行大小
- **行大小** = 块大小（主存块大小，通常是字或双字的整数倍）
- **Tag位** = 主存地址位数 - Cache块地址位数 - 块内地址位数
- **有效位**：每行设1位，标记该块是否有效。
- **脏位**（写回法）：标记块是否被修改过，被替换时需写回主存。

### 8. 多级Cache

- **L1 Cache**：CPU内，速度最快，容量小（几KB~几十KB），分指令Cache和数据Cache（哈佛结构）。
- **L2 Cache**：通常统一存储，几百KB~几MB。
- **L3 Cache**：多核共享，几MB~几十MB。

---

## 五、虚拟存储器

### 1. 虚拟存储器概念

- **虚拟存储器**：把主存和辅存统一编址，给用户提供一个比实际主存大得多的虚拟地址空间。
- **作用**：解决主存容量不足问题，实现多道程序并发、内存保护。
- **理论基础**：局部性原理（与Cache类似）。

### 2. 虚拟存储器与Cache对比

| 对比项 | Cache-主存 | 主存-辅存 |
|--------|-----------|-----------|
| 解决问题 | 速度 | 容量 |
| 实现者 | 硬件 | 软硬件结合（OS+硬件） |
| 对程序员透明度 | 全透明 | 部分透明 |
| 数据单位 | 块 | 页/段 |
| 替换算法 | 硬件实现 | OS实现 |
| 失效处理 | 硬件自动 | 缺页中断，OS处理 |

### 3. 页式虚拟存储器

- **页式**：将虚拟地址空间和主存都划分为固定大小的页。
- **页表**：记录虚页号与实页号的映射关系，存放在主存中。
- **虚拟地址**：虚页号 + 页内偏移
- **物理地址**：实页号 + 页内偏移（页内偏移不变）

**地址转换过程**：
1. CPU给出虚拟地址：虚页号 + 页内偏移。
2. 由虚页号查页表（PTBR指向页表基地址）。
3. 取出实页号，与页内偏移拼接得到物理地址。
4. 访问主存取得数据。

### 4. TLB（Translation Lookaside Buffer，快表）

- **作用**：页表的Cache，存放近期使用的页表项，加快地址转换。
- **TLB结构**：标记Tag + 牺牲存储项，由硬件维护。
- **TLB命中率**：高（局部性原理），通常>99%。

**TLB + Cache + 主存访问流程**：
1. 由虚拟地址查TLB，命中则直接得到实页号。
2. TLB未命中，查页表（在主存中）：
   - 页表命中：得到实页号，并更新TLB。
   - 页表未命中：缺页中断，OS从辅存调入页面，更新页表。
3. 由物理地址查Cache，命中则取得数据。
4. Cache未命中，从主存取数据到Cache再到CPU。

### 5. 段式虚拟存储器

- **段式**：按程序的逻辑结构分页（如代码段、数据段、栈段），每段大小可变。
- **段表**：记录段号、段起点、段长。
- **优点**：便于共享、保护；**缺点**：易产生外部碎片。

### 6. 段页式虚拟存储器

- **段页式**：先分段，每段再分页，结合段式和页式优点。
- **虚拟地址**：段号 + 页号 + 页内偏移
- **地址转换**：查段表 → 查页表 → 物理地址。

### 7. 页表项结构

页表项通常包括：
- **实页号**：物理页框号
- **有效位（装入位）**：该页是否在主存
- **修改位（脏位）**：是否被修改过，替换时是否写回辅存
- **访问位（引用位）**：用于替换算法（如LRU）
- **保护位**：读写执行权限

### 8. 缺页中断

- 当访问的页不在主存时，产生**缺页中断**。
- OS响应中断：从辅存读入该页、更新页表、重新执行被中断的指令。
- 缺页中断属于**内部异常**（程序性中断），在执行过程中产生。

---

## 六、Cache-主存-辅存三级存储层次

### 1. 三级层次结构

           CPU
            ↓
        寄存器  ←——— 速度最快，容量最小
            ↓
         Cache（L1/L2/L3） ←——— 硬件管理
            ↓
         主存  ←——— OS参与
            ↓
         辅存  ←——— 文件系统

### 2. 三级层次的工作流

1. CPU要访问数据，先查Cache。
2. Cache命中 → 直接返回数据。
3. Cache未命中 → 查主存，将数据所在块调入Cache，再返回CPU。
4. 主存未命中 → 缺页中断，从辅存读入主存，再调入Cache。

### 3. 层次间的对应关系

| 层次 | 实现方式 | 失效时 |
|------|---------|--------|
| Cache-主存 | 硬件 | 自动调块 |
| 主存-辅存 | 软硬结合 | 缺页中断，OS调页 |
| TLB-页表 | 硬件 | 自动查页表 |

### 4. 命中率与平均访问时间

- **平均访问时间** = 命中Cache时间 + (1 - Cache命中率) × (查主存时间 + 主存缺失时查辅存时间)
- 三级层次需综合考虑各级命中率和访问时间。

---

## 七、RAID 磁盘阵列（Redundant Array of Independent Disks）

### 1. 基本概念

**RAID** 将多个独立物理磁盘组合成一个逻辑磁盘，提供**数据冗余、高性能、大容量**。由**RAID 控制器**管理。

**核心思想**：利用多磁盘并行访问提高 I/O 性能，利用冗余信息提高可靠性。

### 2. 各级 RAID 对比

| RAID 级别 | 原理 | 最少盘数 | 容量利用率 | 冗余 | 性能特点 | 适用场景 |
|----------|------|---------|-----------|------|---------|---------|
| **RAID 0** | 条带化（Striping），数据分块连续写入多盘，无冗余 | 2 | 100% | 无 | 读写并行，速度快，但一块坏全坏 | 追求性能、数据不重要 |
| **RAID 1** | 镜像（Mirroring），数据完全复制到另一盘 | 2 | 50% | 100% 镜像 | 读可并行（两盘各读一半），写需写两盘 | 重要数据，可靠性优先 |
| **RAID 2** | 位级条带 + 海明码纠错 | 3+ | ≈(n-1)/n | 海明码 | 理论化，需多个校验盘，实际不用 | 已不实用 |
| **RAID 3** | 字节级条带 + 单校验盘 | 3 | (n-1)/n | 奇偶校验 1 盘 | 大块顺序读写快，小随机 I/O 差 | 视频流等大文件 |
| **RAID 4** | 块级条带 + 单校验盘 | 3 | (n-1)/n | 奇偶校验 1 盘 | 读并行好，写需更新校验盘（瓶颈） | 读多写少场景 |
| **RAID 5** | 块级条带 + **校验块分散**在各盘 | 3 | (n-1)/n | 分布式奇偶校验 | 读并行好，写校验盘不再成瓶颈 | **最常用**，兼顾性能与可靠性 |
| **RAID 6** | 块级条带 + **双校验块分散** | 4 | (n-2)/n | 双重奇偶校验 | 可靠（允许 2 盘同时坏），写性能略差于 RAID5 | 高可靠性企业环境 |
| **RAID 10** | 先镜像(RAID1)再条带(RAID0) | 4 | 50% | 每盘有镜像 | 读写高性能 + 高可靠 | 数据库、金融系统 |

### 3. RAID 关键机制

**数据分块方式：**
- **位/字节级分条**（RAID 2/3）：以位/字节为单位散布多盘，需各盘同步旋转。
- **块级分条**（RAID 4/5/6）：以数据块为单位，各盘可独立处理请求，适合小 I/O。

**校验算法：**
- 奇偶校验：P = D0 ⊕ D1 ⊕ D2 ⊕ ... （异或），任一数据丢失可通过其他数据和 P 恢复。例：若 D1 丢失，D1 = P ⊕ D0 ⊕ D2。
- 写在 RAID 5：新校验块 = 旧数据 ⊕ 新数据 ⊕ 旧校验块（避免读所有其他盘重算）。

**写惩罚（Write Penalty）：** RAID 5/6 每次写操作需：读旧数据 → 读旧校验 → 计算新校验 → 写新数据 → 写新校验。一次写操作实际产生 4 次磁盘访问（读-改-写）。

### 4. RAID 常考点
- RAID 0 无冗余但最快；RAID 1 镜像可靠性高但空间效率最低（50%）。
- **RAID 5 是最常用折中方案**：校验块分散，读写性能好，允许坏 1 盘。
- RAID 6 允许坏 2 盘，写惩罚更大。
- RAID 10（RAID 1+0）兼顾 RAID 0 性能 + RAID 1 可靠性。
- RAID 卡和磁盘阵列柜用于硬件级 RAID 管理。

---

## 八、现代多级 Cache 层次结构（第3版强化）

### 1. 现代处理器的 Cache 层次

现代处理器普遍采用 **3 级 Cache 层次**：

| Cache 级别 | 位置 | 典型容量 | 典型延迟 | 组织方式 | 共享范围 |
|-----------|------|---------|---------|---------|---------|
| **L1 I-Cache** | CPU 核内 | 32~64 KB | ~1-4 周期 | VIPT（虚索引实标记） | 每核私有 |
| **L1 D-Cache** | CPU 核内 | 32~64 KB | ~1-4 周期 | VIPT | 每核私有 |
| **L2 Cache** | 核内或紧邻 | 256 KB~2 MB | ~10-20 周期 | PIPT | 每核私有或小规模共享 |
| **L3 Cache（LLC）** | 所有核共享 | 4~64 MB | ~30-50 周期 | PIPT | 所有核共享 |

- L1 采用 **哈佛结构**（指令和数据分 Cache），L2/L3 通常为 **统一 Cache**。
- **VIPT**（Virtually Indexed, Physically Tagged）：用虚拟地址做索引、物理地址做标记并比较，可并行访问 TLB 和 Cache。
- **PIPT**（Physically Indexed, Physically Tagged）：索引和标记都用物理地址，无别名问题但需 TLB 完成后才能访问 Cache。

### 2. Cache 包含策略

| 策略 | 说明 | 优缺点 |
|------|------|--------|
| **多级包含（Inclusive）** | L1 中的数据一定也在 L2 中，L2 是 L1 的超集 | 利于 Cache 一致性（只需查 L2 就知是否在 L1）；但浪费容量，L1 替换时需通知 L2 |
| **多级不包含（Exclusive）** | L1 和 L2 中数据不重叠，被替换时数据逐级下移 | 有效 Cache 总容量 = L1+L2，空间利用率高；但一致性实现复杂 |
| **非包含非排斥（NINE）** | L1 和 L2 之间没有严格的包含/排斥关系 | Intel Core 系列常用此策略，兼顾灵活与性能 |

### 3. Cache 性能优化技术

**（1）Victim Cache（牺牲缓存）**
- 在 Cache 和主存之间加一个小容量全相联 Cache，专门存储被替换出的块。
- 再次访问时先在 Victim Cache 查找，命中率高时可减少冲突缺失。

**（2）Non-blocking Cache（非阻塞 Cache）**
- 常规 Cache 在缺失时会阻塞后续访存请求。
- 非阻塞 Cache 允许多个缺失请求同时处理（通过 MSHR —— 缺失状态保持寄存器记录未完成的缺失请求）。
- 对乱序执行处理器意义重大。

**（3）硬件预取（Hardware Prefetch）**
- 根据访问模式自动将可能用到的数据块提前调入 Cache。
- 常见预取器：顺序预取（检测连续访问模式）、步长预取（检测固定步长访问模式）。
- 过度预取会浪费带宽并污染 Cache（Cache Pollution）。

**（4）Cache 压缩**
- 对 Cache 行数据进行压缩，使同一 Cache 行能存放更多数据块（如 IBM POWER 系列）。

### 4. 写合并与写缓冲区

- **写缓冲区（Write Buffer）**：在 Cache 和主存之间插入 FIFO 缓冲区暂存待写入主存的数据，CPU 写完 Cache 后可立即继续工作。
- **写合并（Write Combining）**：将多个写到同一 Cache 行的部分写操作合并为一个完整的写操作，减少总线事务。
- 写缓冲区对写直达 Cache 尤为重要。

### 5. Cache 行大小的影响

| 行大小 | 优点 | 缺点 |
|--------|------|------|
| 小（如 16B） | 空间局部性差的程序受益；替换代价小 | 标记位开销大（Tag 占比高）；预取效果差 |
| 大（如 128B） | 空间局部性好的程序受益；预取效果好 | 可能带入大量无用数据（Cache 污染）；缺失时传输时间长 |

- 现代处理器典型 Cache 行大小为 **64 字节**，是主存总线一次突发传输（Burst Transfer）的大小。

---

## 九、虚拟存储器进阶（第3版强化）

### 1. 多级页表

单级页表在 64 位虚拟地址下过大（如 4KB 页、48 位虚拟地址需 2^36 个页表项），因此采用**多级页表**：

**x86-64 四级页表结构（48 位虚拟地址）：**

    虚拟地址 = PML4索引(9位) + PDP索引(9位) + PD索引(9位) + PT索引(9位) + 页内偏移(12位)

| 级别 | 名称 | 说明 |
|------|------|------|
| 第1级 | PML4（Page Map Level 4） | 页映射第4级表，每个进程一个 |
| 第2级 | PDP（Page Directory Pointer） | 页目录指针表 |
| 第3级 | PD（Page Directory） | 页目录表 |
| 第4级 | PT（Page Table） | 页表，最终指向物理页框 |

- 每级页表大小为 4KB，每项 8 字节，每表可容纳 512 项。
- **优点**：只有实际使用的地址范围才分配中间级页表，大幅节省内存。
- **缺点**：地址转换需多次访存（4 级需 4 次访存），TLB 命中可避免。

### 2. 倒排页表（Inverted Page Table）

| 对比项 | 传统页表 | 倒排页表 |
|--------|---------|---------|
| 索引方式 | 按虚页号索引 | 按实页框号索引 |
| 项数 | = 虚拟地址空间页数 | = 物理地址空间页数 |
| 查找方式 | 直接索引 | 需哈希查找 |
| 空间开销 | 大（每个进程一个全量页表） | 小（仅与物理内存成正比） |
| 适用 | 通用系统 | 物理内存大的系统（如 PowerPC、IA-64） |

- 倒排页表以实页框号为索引，每项记录该物理页框当前被哪个进程的哪个虚页占用。
- 地址转换时对虚页号哈希，查找对应的实页框，需处理哈希冲突。

### 3. TLB 详细机制

**TLB 是专门用于地址转换的 Cache**，结构类似 Cache：

| TLB 类型 | 说明 |
|---------|------|
| **指令 TLB（iTLB）** | 用于取指时的地址转换 |
| **数据 TLB（dTLB）** | 用于数据访存时的地址转换 |
| **统一 TLB** | 指令和数据共用 |

**TLB 多级层次**（类似 Cache）：
| 级别 | 容量 | 延迟 |
|------|------|------|
| L1 TLB | 几十项，全相联 | 1 周期 |
| L2 TLB | 几百~几千项 | 几~十几周期 |

**TLB 缺失处理：**
- **硬件页表遍历（Hardware Page Table Walk）**：x86 中，TLB 缺失后硬件自动按 CR3 寄存器从多级页表中查找，找到后更新 TLB。
- **软件 TLB 填充**：MIPS/RISC-V 等架构中，TLB 缺失触发异常，由 OS 软件查询页表并填入 TLB。
- **TLB 击落（TLB Shootdown）**：多核系统中，一个核心修改页表后需通知其他核心使对应 TLB 项失效，通过 IPI（核间中断）实现。

### 4. 虚拟地址转换总流程（含 TLB + Cache）

    CPU发出虚拟地址
        ↓
    查TLB（虚拟地址→物理地址）
        ├─ TLB命中 → 得到物理地址
        └─ TLB缺失 → 硬件/软件页表遍历 → 得到物理地址 → 更新TLB
        ↓
    查L1 Cache（VIPT：虚拟索引+物理标记比较）
        ├─ L1命中 → 返回数据
        └─ L1缺失 → 查L2 Cache
                       ├─ L2命中 → 返回数据 → 填入L1
                       └─ L2缺失 → 查L3/主存
                                       ├─ 主存命中 → 返回数据 → 依次填入L2/L1
                                       └─ 主存缺失（缺页）→ 缺页中断 → OS从辅存调入页
                                                          → 更新页表 → 重新执行指令

### 5. 内存保护机制

| 机制 | 说明 |
|------|------|
| **页表保护位** | 每页设 R（读）、W（写）、X（执行）权限位。违反权限触发异常（Segmentation Fault） |
| **特权级保护** | 页表项中 U/S 位区分用户页和内核页，用户模式不能访问内核页 |
| **NX/XD 位** | 不可执行位，防止数据页被当作代码执行（抵御缓冲区溢出攻击） |
| **ASLR** | 地址空间布局随机化，每次加载时随机化栈/堆/库位置，增加攻击难度 |
| **SMAP/SMEP** | 内核模式禁止访问用户空间页/禁止执行用户空间代码（x86 新增安全特性） |

### 6. 大页（Huge Page）机制

| 页大小 | TLB 覆盖范围 | 优点 | 缺点 |
|--------|------------|------|------|
| 4KB（标准页） | 小（64个TLB项覆盖256KB） | 管理精细、碎片小 | TLB 覆盖范围有限 |
| 2MB（大页） | 大（64个TLB项覆盖128MB） | TLB 命中率高、缺页少 | 内存浪费（内部碎片） |
| 1GB（巨页） | 极大 | 适合大数据库/虚拟机 | 严重内部碎片 |

- x86-64 支持 4KB、2MB、1GB 三种页大小。
- 应用场景：数据库（如 Oracle）、虚拟化（降低 TLB 缺失）。

### 7. 虚拟化环境中的地址转换

在虚拟机环境中，地址转换变为两次映射：
- **GVA（Guest Virtual Address）→ GPA（Guest Physical Address）→ HPA（Host Physical Address）**
- 传统的两次页表遍历效率极低，Intel/AMD 引入 **EPT/NPT**（扩展页表/嵌套页表）硬件加速。
- 又称 **二级地址转换（SLAT）**，由硬件一次性完成 GVA→HPA 的映射。

---

## 十、典型计算题套路

### 1. 主存扩展计算

- 给定芯片容量 a×b 和目标容量 A×B：
  - 位扩展片数 = B/b
  - 字扩展片数 = A/a
  - 总片数 = (A/a) × (B/b)
- 片选信号位数 = log2(字扩展片数)

### 2. Cache地址结构与命中计算

- 给定主存地址位数 M、Cache块大小 B、Cache行数 C：
  - 块内地址位数 = log2(B)
  - 直接映射：Cache块号位数 = log2(C)，Tag = M - log2(C) - log2(B)
  - 全相联：Tag = M - log2(B)
  - 组相联（每组 r 行）：组数 = C/r，组地址位数 = log2(C/r)，Tag = M - log2(C/r) - log2(B)
- 计算命中率、平均访问时间：t = h × t_c + (1-h) × t_m

### 3. 页式虚拟存储器地址转换

- 虚拟地址：虚页号 + 页内偏移
- 物理地址：实页号 + 页内偏移
- 由虚页号查页表得到实页号，拼接页内偏移得到物理地址。

### 4. 存储器带宽计算

- 带宽 = 数据宽度 / 存取周期
- 例：32位数据宽度，存取周期100ns → 带宽 = 32/100×10^-9 = 320 MB/s

---

## 十一、速通口诀

- **存储层次四级**：寄存器→Cache→主存→辅存，速度递减、容量递增、价格递减。
- **局部性两原理**：时间（重复访问）、空间（连续访问）。
- **SRAM做Cache、DRAM做主存**：SRAM快不刷新、DRAM慢需刷新。
- **三种扩展**：位扩展并联、字扩展串联、字位同时扩展常用。
- **Cache三映射**：直接固定、全相联任意、组相联组内任意（最常用）。
- **替换算法**：LRU最优常用、FIFO简单可能抖动。
- **Cache 写策略**：写直达简单、写回法高效；写分配配写回、不按写分配配写直达。
- **虚拟存储器三式**：页式固定大小、段式逻辑结构、段页式结合优点。
- **TLB是页表的Cache**：失效后查页表、再失效缺页中断。
- **三级层次工作流**：CPU→Cache→主存→辅存，缺失时自动调块/页。
- **RAID**：RAID0 条带快无冗余，RAID1 镜像安全但 50% 效率，RAID5 分布式校验最常用，RAID10 镜像+条带兼顾。
- **多级 Cache**：L1 哈佛结构、VIPT 虚实并行；L2/L3 PIPT、统一 Cache；包含/排斥/NINE 三种策略。
- **Cache 优化**：Victim Cache 存被替换块、Non-blocking Cache 允许并发缺失处理、硬件预取注意 Cache 污染。
- **多级页表**：x86 四级页表（PML4→PDP→PD→PT），页内 4KB，节省空间但多次访存，TLB 命中可免。
- **倒排页表**：按实页框索引，空间小但需哈希查找，PowerPC/IA-64 使用。
- **TLB 两级**：L1 TLB 全相联几十项 1 周期，L2 TLB 几百项几周期；缺失可硬件页表遍历或软件填充。
- **大页机制**：2MB/1GB 大页提高 TLB 命中率，适合数据库和虚拟化。
- **内存保护**：RWX 权限位、NX 防代码执行、ASLR 地址随机化、SMAP/SMEP 内核保护。
- **虚拟化地址转换**：GVA→GPA→HPA 两次映射，EPT/NPT 硬件加速二级地址转换 SLAT。
`,
    timestamp: "2026-08-06 22:55",
    embed: ``
};

export default [note1];
