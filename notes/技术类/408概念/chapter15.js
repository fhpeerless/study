export const note1 = {
    title: "计算机网络（三）：网络层",
    contentmd: `
# 计算机网络 · 网络层

> 408 高频考点：IP 子网划分与 CIDR 路由聚合、ARP 工作过程、IP 数据报格式、RIP/OSPF/BGP 协议对比、路由器工作原理、IPv6 地址表示。

---

## 一、网络层功能与服务

### 1. 网络层主要功能

- **路由选择与分组转发**：根据路由表选择最佳路径。
- **异构网络互联**：连接不同物理层和链路层技术的网络。
- **拥塞控制**：当网络中数据过多时，限制注入速率。

### 2. 两种服务模型

| 项目 | 数据报服务（无连接） | 虚电路服务（面向连接） |
|------|--------------------|---------------------|
| 连接建立 | 不需要 | 必须（呼叫建立） |
| 目的地址 | 每个分组携带完整目的地址 | 仅建立时携带，分组用虚电路号 |
| 路由选择 | 每个分组独立选路 | 同一虚电路走相同路径 |
| 分组顺序 | 不保证按序，可能乱序 | 按序到达 |
| 故障影响 | 节点故障不影响已发分组 | 虚电路经过故障节点则中断 |
| 可靠性 | 网络层不保证可靠，由传输层负责 | 网络层保证可靠 |
| 应用 | Internet（IP） | ATM、帧中继、X.25 |

- **Internet 采用数据报服务**：网络层只提供简单灵活、无连接、尽最大努力交付的数据报服务。可靠通信由**传输层 TCP** 完成。

---

## 二、IPv4 地址（必考核心）

### 1. IP 地址结构与分类

- **IPv4 地址 = 32 位（4 字节）**，由**网络号 + 主机号**组成。
- 表示方法：**点分十进制**，如 192.168.1.1。

**分类编址（A/B/C/D/E 类）：**

| 类别 | 第 1 字节范围 | 网络号位数 | 主机号位数 | 网络数 | 主机数/网 | 默认掩码 |
|------|-------------|----------|----------|-------|----------|---------|
| A 类 | 1~126 | 8 | 24 | 126 (2^7-2) | 2^24-2 = 16777214 | 255.0.0.0 |
| B 类 | 128~191 | 16 | 16 | 2^14 | 2^16-2 = 65534 | 255.255.0.0 |
| C 类 | 192~223 | 24 | 8 | 2^21 | 2^8-2 = 254 | 255.255.255.0 |
| D 类 | 224~239 | - | - | 组播地址 | - | - |
| E 类 | 240~255 | - | - | 保留未来使用 | - | - |

- **特殊地址**：
  - **网络号全 0**：本网络。
  - **主机号全 0**：网络本身。
  - **主机号全 1**：广播地址（受限广播 255.255.255.255，直接广播到某网络）。
  - **127.x.x.x**：环回地址 loopback，测试本机。
  - **0.0.0.0**：本网络本主机（DHCP 申请时作为源地址）。
  - **169.254.x.x**：链路本地地址（自动私有 IP）。
  - **私有地址**（ RFC 1918）：
    - A 类：10.0.0.0 ~ 10.255.255.255
    - B 类：172.16.0.0 ~ 172.31.255.255
    - C 类：192.168.0.0 ~ 192.168.255.255

### 2. 子网掩码与子网划分

- **子网划分**：从主机号中借用若干位作为**子网号**，将一个网络划分为多个子网。
- 子网划分后 IP 地址 = **网络号 + 子网号 + 主机号**。
- **子网掩码**：网络号和子网号对应位置 1，主机号对应位置 0。
- **将 IP 地址与子网掩码按位与运算 = 子网网络地址**。

**例题**：IP = 192.168.1.130，掩码 = 255.255.255.192，求子网地址：
- 130 = 10000010，192 = 11000000，按位与 = 10000000 = 128。
- 子网地址 = 192.168.1.128。
- 主机号位数为 6 位，每个子网可用主机数 = 2^6 - 2 = 62。
- 子网 192.168.1.128 的广播地址 = 192.168.1.191。

**子网划分公式**：若借用 n 位作为子网号，原主机号 h 位：
- 子网数 = 2^n（部分旧标准扣除全 0 全 1 子网，即 2^n - 2）。
- 每子网主机数 = 2^(h-n) - 2。

### 3. CIDR 无分类编址（重要）

- **CIDR（无类别域间路由）**：取消 A/B/C 类固定分类，使用**变长子网掩码 VLSM**。
- IP 地址 = **网络前缀 + 主机号**。
- 表示法：**斜线记法**，如 192.168.1.0/24 表示前 24 位为网络前缀。
- **地址块**：网络前缀相同的连续 IP 地址范围。
- **掩码**：前缀位为 1，主机位为 0。/20 → 掩码 255.255.240.0。

### 4. 路由聚合（构成超网）

- **路由聚合**（Route Aggregation）：将多个子网合并为一个更大的地址块，用更短的前缀表示。
- 方法：找出这些子网前缀的**最长公共前缀**。
- **例**：192.168.0.0/24、192.168.1.0/24、192.168.2.0/24、192.168.3.0/24
  - 转换为二进制看公共前缀：第 3 字节 0=00000000, 1=00000001, 2=00000010, 3=00000011，公共前缀 6 位。
  - 聚合后：192.168.0.0/22。
- **最长前缀匹配原则**：路由表中前缀越具体越优先。

### 5. 子网划分典型例题

**例**：某公司有 C 类网络 192.168.10.0/24，需划分为 6 个子网，每个子网至少容纳 25 台主机，如何划分？
- 需 6 个子网，需借 n 位使 2^n ≥ 6，n = 3。
- 借 3 位后主机号 5 位，每子网主机数 = 2^5 - 2 = 30 ≥ 25 ✓。
- 子网掩码 = 255.255.255.224（/27）。
- 各子网：192.168.10.0/27, 192.168.10.32/27, 192.168.10.64/27, ...

---

## 三、IP 数据报格式（必考）

IPv4 数据报首部由 **20 字节固定部分 + 可变部分**组成：

| 字段 | 长度 | 说明 |
|------|------|------|
| 版本 | 4 bit | IPv4=4，IPv6=6 |
| 首部长度 | 4 bit | 单位 4 字节，最小 5（即 20B），最大 15（60B） |
| 区分服务 | 8 bit | QoS 服务质量 |
| 总长度 | 16 bit | 首部+数据，单位字节，最大 65535 |
| 标识 | 16 bit | 同一数据报分片标记相同 |
| 标志 | 3 bit | MF=1 还有分片；DF=0 才能分片 |
| 片偏移 | 13 bit | 单位 8 字节，分片在原数据报中的相对位置 |
| 生存时间 TTL | 8 bit | 经过路由器数减 1，为 0 丢弃，防止环路 |
| 协议 | 8 bit | 上层协议：1=ICMP, 6=TCP, 17=UDP, 89=OSPF |
| 首部校验和 | 16 bit | 仅校验首部 |
| 源地址 | 32 bit | 发送方 IP |
| 目的地址 | 32 bit | 接收方 IP |
| 可变部分 | 0~40B | 选项字段 |

### 分片例题

- MTU = 1400 字节，IP 数据报总长 4000 字节（含 20B 首部），数据 3980 字节。
- 每个分片数据字段最大 = 1400 - 20 = 1380 字节，需为 8 的倍数 → 1376 字节。
- 分片数 = ⌈3980 / 1376⌉ = 3。
- 分片 1：偏移 0，MF=1，数据 1376B，总长 1396B。
- 分片 2：偏移 1376/8 = 172，MF=1，数据 1376B，总长 1396B。
- 分片 3：偏移 2752/8 = 344，MF=0，数据 1228B，总长 1248B。

---

## 四、ARP 地址解析协议

### 1. 作用

- **将 IP 地址解析为 MAC 地址**。
- 在同一局域网内，主机之间通信需要 MAC 地址；ARP 用于获取目的主机的 MAC。

### 2. 工作过程

1. 主机 A 想发送数据给同网段主机 B（已知 B 的 IP，不知 B 的 MAC）。
2. A 查询本地 **ARP 缓存表**，若有则直接使用。
3. 若无，A **广播 ARP 请求分组**：包含发送方 IP+MAC、目标 IP。
4. 局域网内所有主机收到请求，只有目标主机 B 单播回复 **ARP 响应分组**：包含 B 的 MAC。
5. A 将 B 的 IP-MAC 映射存入 ARP 缓存表，并设置**老化时间**（通常 10~20 分钟）。
6. 后续通信直接使用缓存表。

### 3. 跨网段 ARP

- A → B 跨网段时，A 通过 ARP 解析的是**默认网关路由器接口的 MAC**，而非远端主机 B 的 MAC。
- A 把数据发到网关，网关逐跳转发，最后由目的网关 ARP 解析 B 的 MAC。

### 4. ARP 缓存特点

- ARP 缓存是动态的，会定期更新。
- ARP 表项有老化时间，超时自动删除。
- ARP 是即插即用协议，自动运行，无需配置。

---

## 五、DHCP 动态主机配置协议

### 1. 作用

- **动态分配 IP 地址**给主机，提供"即插即用"联网。
- 还可分配：子网掩码、默认网关、DNS 服务器地址。

### 2. 工作过程（C/S 模式，基于 UDP）

DHCP 客户端（端口 68）与服务器（端口 67）通过 **4 个报文**完成租约：

1. **DHCP Discover（发现）**：客户端**广播**寻找 DHCP 服务器。源 IP = 0.0.0.0，目的 IP = 255.255.255.255。
2. **DHCP Offer（提供）**：DHCP 服务器**单播或广播**回应，提供 IP 配置参数。
3. **DHCP Request（请求）**：客户端**广播**请求，确认使用某服务器的配置（同时告知其他服务器 IP 可释放）。
4. **DHCP Ack（确认）**：服务器**单播或广播**确认，分配 IP 地址（含租约期）。

### 3. 租约与续租

- **租约期**（Lease Time）：通常 8 天，到期前可续租。
- 续租：到 50% 租约时单播请求续租，未响应则在 87.5% 时广播续租。
- 客户端可主动发送 **DHCP Release** 释放 IP。

---

## 六、ICMP 网际控制报文协议

### 1. 作用

- **报告差错和异常**，提供网络层控制信息。
- ICMP 是 IP 层协议，ICMP 报文**封装在 IP 数据报**中传输（协议字段=1）。

### 2. ICMP 报文类型

**差错报告报文（5 种）：**
- **终点不可达**：路由器或主机无法交付数据报。
- **源点抑制**：因拥塞丢弃数据报，通知源点降低速率。
- **时间超时**：TTL=0 丢弃数据报；或分片未全到。
- **参数问题**：首部字段有错。
- **改变路由（重定向）**：告诉主机下次发给更好的路由器。

**询问报文（2 种）：**
- **回送请求和回答**：ping 命令使用。
- **时间戳请求和回答**：用于时钟同步。

### 3. ICMP 应用

- **ping**：测试主机连通性，使用 ICMP 回送请求/回答。
- **traceroute（tracert）**：利用 **TTL 递增 + ICMP 超时报文**，逐跳探测路径。
  - 发送 TTL=1 的 IP 数据报，第一跳路由器返回 ICMP 超时。
  - 发送 TTL=2 的数据报，第二跳路由器返回 ICMP 超时。
  - 依此类推，直到到达目的主机返回 ICMP 终点不可达（端口不可达）。

---

## 七、IPv6（新一代 IP）

### 1. 产生背景

- IPv4 地址枯竭，且 IPv4 首部字段过多影响转发效率。
- IPv6 地址 **128 位（16 字节）**，地址空间约为 3.4 × 10^38 个。

### 2. IPv6 地址表示

- **冒号十六进制记法**：8 组 4 位十六进制数，组间用冒号分隔。
  - 例：2001:0DB8:0000:0000:0000:0000:0003:0001
- **零压缩**：连续的 0 可用 **::** 表示，但**只能压缩一次**。
  - 上例简化为：2001:0DB8::0003:0001 或 2001:DB8::3:1
- **IPv4 映射地址**：::ffff:192.168.1.1（前 96 位 0 + 16 位 1 + 32 位 IPv4）。

### 3. IPv6 地址分类

- **单播**：一对一通信。
- **组播**：一对多通信（FF00::/8）。
- **任播**：一对最近（新增，IPv4 没有）。
- **取消了广播**，广播功能用组播实现。

### 4. IPv4 vs IPv6 对比

| 项目 | IPv4 | IPv6 |
|------|------|------|
| 地址长度 | 32 位 | 128 位 |
| 首部长度 | 20~60B 可变 | 40B 固定 |
| 分片 | 路由器和源端都可分片 | **仅源端分片** |
| 首部校验和 | 有 | **取消**（提高转发效率） |
| 选项 | 在首部中 | **扩展首部** |
| 地址配置 | 手动/DHCP | **无状态自动配置** + DHCPv6 |
| 安全性 | IPsec 可选 | **IPsec 内置** |
| 广播 | 有 | **取消**，用组播代替 |
| ICMP | ICMPv4 | ICMPv6（含邻居发现 ND） |

### 5. IPv4 → IPv6 过渡技术

- **双栈协议**：主机同时运行 IPv4 和 IPv6 协议栈。
- **隧道技术**：将 IPv6 数据报封装在 IPv4 数据报中通过 IPv4 网络。
- **头部转换**：NAT-PT（已废弃，替代方案 NAT64）。

---

## 八、IP 组播（Multicast）

### 1. 组播基本概念

**组播**：源主机向一个**组播组**中所有成员发送数据的一对多通信。组播地址标识一个组播组，加入该组的主机接收发往该组的数据。

- **单播**：一对一一通信，发 n 份数据给 n 台主机。
- **广播**：一对所有，子网内每台主机都收到。
- **组播**：一对多，只有组内成员收到，**节省带宽**，适合视频会议、在线直播、分布式系统。

**IPv4 组播地址范围**：D 类地址 224.0.0.0 ~ 239.255.255.255。

**组播与以太网 MAC 地址映射**：
- 组播 MAC 地址前缀：01-00-5E-00-00-00 ~ 01-00-5E-7F-FF-FF。
- 映射方法：将 IP 组播地址的低 23 位复制到 MAC 地址 01-00-5E-00-00-00 的低 23 位。
- **注意**：并非一一映射（32:1），一个组播 MAC 对应多个 IP 组播组。

### 2. IGMP 协议（Internet Group Management Protocol）

**IGMP**：主机与本地组播路由器之间的协议，管理主机加入/离开组播组。

**IGMP 工作流程**：
1. 主机要加入组播组 G → 发送 IGMP 成员报告报文（Membership Report）。
2. 组播路由器定期（默认 125 秒）向 224.0.0.1（子网所有主机）发送 IGMP 成员查询报文。
3. 若某组在超时时间内（默认 250 秒）无成员响应，路由器停止转发该组数据。

**IGMP v1/v2/v3**：
- v1：基本加入/查询，无离开报文（靠超时退出）。
- v2：增加**离开报文**（Leave Group），路由器发送特定组查询快速确认。
- v3：增加**源过滤**，主机可指定接收来自特定源的组播数据。

### 3. 组播路由协议

组播路由建立一棵**组播树**，从源到所有组成员。

**组播路由协议分类：**
| 类型 | 协议 | 原理 | 特点 |
|------|------|------|------|
| **密集模式 DM** | DVMRP/PIM-DM | 先广播再剪枝（Push），适合组成员密集 | 定期泛洪+剪枝，不适用于稀疏场景 |
| **稀疏模式 SM** | PIM-SM | 显式加入（Pull），适合组成员稀疏 | 有汇聚点 RP（Rendezvous Point），主机主动加入 |
| **链路状态** | MOSPF | OSPF 扩展，链路状态数据库含组成员 | 适合单个域内 |

**PIM-SM（最常用）工作过程：**
1. 每个组播域有一个**汇聚点 RP**（Rendezvous Point）。
2. 组播源向 RP 注册（发送 PIM Register）。
3. 接收者向 RP 发送 PIM Join 加入组播树。
4. RP 在源和接收者间建立**共享树（RPT）**。
5. 可切换为**最短路径树（SPT）**以优化转发路径。

### 4. IPv6 组播

- IPv6 组播地址前缀：FF00::/8。
- IPv6 **取消了广播**，广播功能完全由组播替代。
- 常见 IPv6 组播地址：FF02::1（链路所有结点）、FF02::2（链路所有路由器）。

### 5. 组播常考点
- D 类地址 224~239 是组播地址；组播 MAC 前缀 01-00-5E。
- IGMP 在本地子网主机与路由器之间工作；IGMPv2 支持离开报文，v3 支持源过滤。
- PIM-SM 显式加入有 RP，适合稀疏组播；PIM-DM 广播剪枝适合密集组播。
- IPv6 取消广播，用组播替代（如 FF02::1 代替 ARP 广播）。

---

## 九、网络地址转换 NAT（重点）

### 1. NAT 产生背景
- IPv4 地址紧缺，**私有地址**在 Internet 上不可被路由。
- NAT 使使用私有地址的内网主机能通过公有地址访问 Internet。

### 2. NAT 工作原理
- **NAT 路由器**位于内网与公网边界，完成 **私有 IP ↔ 公网 IP** 的转换。
- 内网主机发出数据报时，NAT 将源 IP（私有地址）更换为公网 IP 并分配一个新端口号。
- 收到外网回复时，NAT 根据记录将公网 IP 和端口换回原始私有 IP 和端口。
- **NAPT（网络地址端口转换）**：最常用形式，使用"IP + 端口"对实现多台内网主机共用一个公网 IP。

### 3. NAT 转换表
NAT 维护一张**转换表**，记录：私有地址+端口 ↔ 公网地址+端口。
- 内 → 外：查找或创建表项，替换源 IP 和端口。
- 外 → 内：查找表项，恢复目的 IP 和端口。

### 4. NAT 穿透问题
- NAT 屏蔽了内网主机，外部无法**主动向内发起连接**。
- 解决方法：端口映射/UPnP、STUN/TURN/ICE 协议（如 WebRTC）、中继服务器。

### 5. NAT 优缺点
- **优点**：节省 IPv4 地址、隐藏内网拓扑（一定安全性）、内网地址变更不影响公网。
- **缺点**：违反 IP 端到端原则（修改 IP 数据报）；增加时延；某些应用层协议（FTP、SIP）需特殊处理；无法对 NAT 后的主机进行直接访问。

---

## 十、网络安全基础

### 1. 网络安全威胁
- **被动攻击**：窃听（截获通信内容，不干扰通信，难以检测）。
- **主动攻击**：篡改、伪造、拒绝服务 DoS、重放、恶意程序。
- **分布式拒绝服务 DDoS**：多台主机同时向目标发送大量请求使其瘫痪。

### 2. 密码学基础
- **对称密钥加密**：加密解密使用**相同密钥**，如 DES、AES。加解密速度快，适合大数据量，但密钥分发困难。
- **公钥加密（非对称加密）**：加密使用**公钥**，解密使用**私钥**，如 RSA、ECC。公钥可公开，密钥分发方便，但加解密速度慢，适合加密会话密钥和数字签名。
- **混合加密**：用公钥加密对称密钥，再用对称密钥加密数据（如 HTTPS）。

### 3. 数字签名与认证
- **数字签名**：发送方用自己的**私钥**对报文摘要（Hash）签名，接收方用发送方**公钥**验证。签名提供**不可否认性**和**完整性**。
- **报文摘要（Hash）**：MD5（128 位，已不安全）、SHA-1（160 位，已淘汰）、SHA-256。特性：单向、防碰撞、雪崩效应。
- **CA（认证中心）**：签发数字证书，绑定公钥与实体身份。用户信任 CA，由 CA 担保公钥真实性。

### 4. 防火墙
- **防火墙**：在内外网边界上部署，根据预定义的安全策略**过滤进出数据**。
- **包过滤防火墙**：基于 IP 数据报首部字段（源/目的 IP、端口、协议）过滤，工作在网络层/传输层。简单高效但粒度粗。
- **应用网关（代理防火墙）**：在应用层代理服务（如 HTTP 代理），检查应用层内容，粒度细但效率低。
- **状态检测防火墙**：记录连接状态，结合包过滤 + 状态表，比纯包过滤更安全。

### 5. 入侵检测系统 IDS
- 基于特征（规则匹配）或基于异常（行为偏离基线），检测网络攻击行为。
- **HIDS**（基于主机）、**NIDS**（基于网络）。

### 6. IPsec 安全体系

**IPsec（IP Security）**：在 IP 层提供安全通信的协议族，为 IPv4 可选、IPv6 内置。

**IPsec 两大协议：**
- **AH（认证首部）**：提供数据完整性、数据源认证，**不提供加密**。协议号 51。
- **ESP（封装安全载荷）**：提供完整性、认证 + **加密**（对称加密）。协议号 50。

**IPsec 两种工作模式：**
| 模式 | 保护范围 | 应用场景 | 特点 |
|------|---------|---------|------|
| **传输模式** | 仅保护 IP 数据报载荷（传输层及以上） | 端到端通信（主机之间） | 原 IP 首部不变，AH/ESP 插在原 IP 首部之后 |
| **隧道模式** | 保护整个原始 IP 数据报（含原首部） | VPN 网关之间 | 原始 IP 数据报整体封装为新 IP 数据报的载荷，新首部由安全网关生成 |

**安全关联 SA（Security Association）：**
- SA 是通信双方对安全参数的约定，单向（发送方到接收方），由三元组标识：**安全参数索引 SPI + 目的 IP + 安全协议（AH/ESP）**。
- 双向通信需要一对 SA。
- 通过 **IKE（Internet 密钥交换协议）** 自动协商建立 SA（基于 Diffie-Hellman 密钥交换）。

**IPsec 常考点：**
- AH 协议号 51，仅认证不加密；ESP 协议号 50，认证 + 加密。
- 传输模式：端到端，仅保护载荷；隧道模式：网关之间，保护整个原始 IP 数据报。
- SA 由 SPI + 目的 IP + 协议三元组唯一确定，单向。
- IPv6 内置 IPsec 支持（原 IPv4 为可选扩展）。

---

## 十一、路由算法与协议

### 1. 路由算法分类

**静态路由 vs 动态路由：**

| 项目 | 静态路由 | 动态路由 |
|------|---------|---------|
| 配置方式 | 手工配置 | 路由协议自动学习 |
| 适应性 | 差，拓扑变化需手动改 | 好，自动适应 |
| 开销 | 小 | 大 |
| 安全性 | 高 | 低 |
| 适用场景 | 小型网络、 stub 网络 | 大型网络 |

**动态路由算法分类：**
- **距离向量算法**：RIP，邻居交换路由表。
- **链路状态算法**：OSPF，全网络拓扑信息，Dijkstra 算法。

### 2. RIP 路由信息协议（基于距离向量）

- **基于 UDP**，端口 520。
- **距离度量**：**跳数**（hop count），每经过一个路由器 +1。
- **最大跳数 15**，16 跳视为不可达。**仅适用于小型网络**。
- **路由表交换**：每 **30 秒**与邻居交换**完整路由表**。
- **算法**：Bellman-Ford 距离向量算法。
  - 收到邻居路由表后，更新到各目的网络的距离（加 1 跳）。
  - 若新距离更小则更新；若原路由来源是该邻居，则无条件更新。
- **特点**：
  - **好消息传得快，坏消息传得慢**（计数到无穷问题）。
  - 容易产生**路由环路**。
  - 解决方法：水平分割、毒性逆转、触发更新、最大跳数限制。
- **RIP 版本**：RIPv1（仅支持分类地址）、RIPv2（支持 VLSM 和 CIDR）、RIPng（IPv6）。

### 3. OSPF 开放最短路径优先（基于链路状态）

- **基于 IP**，协议号 89（不使用传输层）。
- **度量**：**链路代价**（带宽、延迟、负载等综合），无最大跳数限制。
- **算法**：**Dijkstra 最短路径算法**。
- **工作过程**：
  - 每个路由器**向所有路由器**泛洪链路状态通告 **LSA**。
  - 每个路由器建立完整的**链路状态数据库 LSDB**（全网拓扑）。
  - 用 Dijkstra 算法计算到每个网络的最短路径。
- **更新触发**：链路状态变化时立即触发更新；同时每 **30 分钟**刷新一次。
- **特点**：
  - **收敛速度快**，无计数到无穷问题。
  - 适合**大型网络**。
  - **支持区域划分**，减少开销（骨干区域 0 + 非骨干区域）。
  - **支持等价多路径负载均衡**。
- **OSPF 报文类型**：Hello（发现邻居）、DD（数据库描述）、LSR（链路状态请求）、LSU（链路状态更新）、LSAck（确认）。

### 4. BGP 边界网关协议（路径向量）

- **基于 TCP**，端口 179。
- **作用**：**自治系统 AS 之间**的路由协议（外部网关协议 EGP）。
- **度量**：**路径属性**，使用**策略路由**，不仅考虑最短路径，还考虑商业策略。
- **AS_PATH 属性**：记录经过的 AS 序列，**防止环路**。
- **BGP-4 是事实标准**，支持 CIDR。
- **路由交换**：BGP 邻居间交换**完整的 AS 路径**信息（不是链路状态）。
- **报文类型**：OPEN、UPDATE、NOTIFICATION、KEEPALIVE。
- **特点**：
  - **可扩展性强**，互联网骨干使用。
  - 路由收敛较慢。
  - 基于策略，可以人为干预路由选择。

### 5. 三种路由协议对比

| 协议 | RIP | OSPF | BGP |
|------|-----|------|-----|
| 类型 | 内部网关 IGP | 内部网关 IGP | 外部网关 EGP |
| 算法 | 距离向量 | 链路状态 | 路径向量 |
| 度量 | 跳数（≤15） | 链路代价 | 策略 + AS 路径 |
| 传输层 | UDP 520 | IP 协议 89 | TCP 179 |
| 更新 | 30s 整表 | 触发 + 30min | 触发 |
| 适用 | 小型网络 | 大型网络 | 自治系统间 |
| 收敛 | 慢 | 快 | 慢 |

---

## 十二、路由器与三层交换

### 1. 路由器工作原理

- 路由器是**网络层设备**，根据 IP 数据报的**目的 IP 地址**转发。
- **路由器组成**：
  - **输入端口**：物理层 → 链路层 → 网络层处理，查路由表。
  - **交换结构**：将分组从输入端口转到输出端口（共享内存、纵横式、总线式）。
  - **输出端口**：排队调度、链路层封装、物理层发送。
  - **路由处理器**：运行路由协议，维护路由表。
- **转发流程**：
  1. 接收 IP 数据报，校验首部。
  2. 提取目的 IP 地址，按**最长前缀匹配**查路由表。
  3. TTL - 1，若为 0 则丢弃并发 ICMP 超时。
  4. 重新计算首部校验和。
  5. 通过输出端口转发（封装为链路层帧）。
- **隔离广播域**：路由器不转发广播帧。

### 2. 三层交换机

- **三层交换机 = 二层交换 + 三层路由**，能在硬件中同时完成 MAC 转发和 IP 路由。
- **特点**：
  - 第一次跨 VLAN 通信通过路由（软件）。
  - 后续通过硬件（ASIC）"一次路由，多次交换"，性能高于传统路由器。
  - 适合 VLAN 间大量并发的局域网通信。

### 3. 设备对比总结

| 设备 | 工作层次 | 转发依据 | 隔离冲突域 | 隔离广播域 |
|------|---------|---------|----------|----------|
| 集线器 | 物理层 | 信号放大 | 否 | 否 |
| 网桥/交换机 | 链路层 | MAC 地址 | 是 | 否 |
| 路由器 | 网络层 | IP 地址 | 是 | 是 |
| 三层交换机 | 网络层 | MAC + IP | 是 | 是 |

---

## 十三、SDN 软件定义网络

### 1. SDN 基本概念

- **SDN（Software Defined Networking）**：一种新型网络架构，核心思想是将网络设备的**控制面与数据面分离**。
- **控制面**：负责路由决策、拓扑管理、策略配置，由集中式**SDN 控制器**运行。
- **数据面**：仅负责根据**流表**匹配转发分组，不进行独立决策。
- 传统网络中每台路由器/交换机独立运行路由协议做决策；SDN 中这些决策集中到控制器，网络设备成为"哑"转发设备。

### 2. SDN 三层架构

| 层次 | 功能 | 典型组件 |
|------|------|---------|
| **应用层** | 网络应用（路由、访问控制、负载均衡、流量工程） | SDN App（如防火墙应用、负载均衡应用） |
| **控制层** | 全局网络视图，决策转发规则并下发流表 | SDN 控制器（Floodlight、OpenDaylight、ONOS、Ryu） |
| **基础设施层** | 根据流表执行分组转发 | OpenFlow 交换机、虚拟交换机（OVS） |

- **南向接口**（控制层 ↔ 基础设施层）：**OpenFlow** 协议为主流，也可用 NETCONF、OVSDB。
- **北向接口**（应用层 ↔ 控制层）：REST API，无统一标准。

### 3. OpenFlow 协议核心

- OpenFlow 交换机维护一个或多个**流表（Flow Table）**，每个流表包含多个**流表项**。
- **流表项结构**：匹配域 + 优先级 + 计数器 + 指令 + 超时 + Cookie。
- **匹配域（Match Fields）**：可匹配 12 元组信息（入端口、MAC 源/目的、以太网类型、IP 源/目的、IP 协议、TCP/UDP 源/目的端口等），从 L1 到 L4 均可匹配。
- **流水线处理**：分组从第一个流表开始匹配，匹配成功后执行指令（转发、丢弃、修改字段、转到下一流表）。
- **安全通道**：控制器与交换机之间通过 TLS 加密通信。
- 若分组未匹配任何流表项 → 发送 **Packet-In 消息**到控制器，由控制器决策后下发流表（**Packet-Out 消息**）。

### 4. SDN 与传统网络对比

| 项目 | 传统网络 | SDN |
|------|---------|-----|
| 控制面 | 分布式（每台设备独立运行路由协议） | 集中式（控制器统一管理） |
| 数据面 | 与控制面耦合在同一设备中 | 与控制面分离，标准化转发 |
| 网络可编程性 | 弱，每设备独立配置 | 强，软件可编程 |
| 网络可见性 | 仅本地视图 | 全局网络拓扑视图 |
| 新协议部署 | 需逐设备升级 | 控制器软件更新即可 |
| 转发依据 | 目的 IP 地址（最长前缀匹配） | 多字段流表匹配（L1~L4） |

### 5. SDN 常考点

- 核心：**数据面与控制面分离**，控制器集中管理。
- OpenFlow 交换机通过流表匹配转发，流表可匹配 L1~L4 多字段。
- 南向接口：OpenFlow；北向接口：REST API。
- SDN 使网络可编程，支持网络功能虚拟化 NFV。

---

## 十四、IPv6 深入

### 1. IPv6 地址类型详解

**全球单播地址（Global Unicast）**：
- 前缀：**2000::/3**（即二进制的 001 开头）。
- 相当于 IPv4 的公网地址，全球唯一、可在 Internet 上路由。
- 结构：全球路由前缀（n 位）+ 子网 ID（64-n 位）+ 接口 ID（64 位）。

**链路本地地址（Link-Local）**：
- 前缀：**FE80::/10**。
- 仅在同一链路上有效，路由器不转发。作用类似 IPv4 自动私有地址 169.254.x.x。
- 每台启用 IPv6 的设备自动生成，用于邻居发现和本地通信。

**唯一本地地址（Unique Local）**：
- 前缀：**FD00::/8**（此前曾为 FC00::/7，FG00::/8）。
- 在站点/组织内部使用，不可在 Internet 上路由。相当于 IPv4 私有地址。
- 比站点本地地址（FEC0::/10，已废弃）更规范。

**组播地址（Multicast）**：
- 前缀：**FF00::/8**。
- 替代 IPv4 广播，如 FF02::1（链路所有结点）、FF02::2（链路所有路由器）。
- 组播地址范围字段（Scope）：1=接口本地、2=链路本地、5=站点本地、8=组织本地、E=全球。

**任播地址（Anycast）**：
- 与单播地址格式相同，但分配给多个接口（通常是不同节点）。
- 数据报送往最近的接口（按路由协议的距离度量）。
- 取消广播，广播功能由组播替代。

**特殊地址：**
- ::/0：默认路由（同 IPv4 的 0.0.0.0/0）。
- ::/128：未指定地址。
- ::1/128：环回地址（loopback）。
- ::ffff:0:0/96（即 ::ffff:x.x.x.x）：IPv4 映射地址。

### 2. 无状态地址自动配置 SLAAC

- **SLAAC（Stateless Address Autoconfiguration）**：IPv6 主机无需 DHCP 服务器即可自动配置地址。
- **过程**：
  1. 主机生成 64 位接口 ID（基于 EUI-64 从 MAC 地址派生，或随机生成临时地址以保护隐私）。
  2. 主机向链路发送 **路由器请求 RS（Router Solicitation）** 报文（ICMPv6 类型 133）。
  3. 路由器回复 **路由器通告 RA（Router Advertisement）** 报文（ICMPv6 类型 134），包含网络前缀和前缀长度。
  4. 主机将前缀与接口 ID 拼接生成完整 IPv6 地址。
  5. 通过**重复地址检测 DAD**（Duplicate Address Detection）验证地址唯一性。
- 与 DHCPv6（有状态地址配置）互补：RA 中可设置标志位指示主机使用 SLAAC 或 DHCPv6。

### 3. ICMPv6 与邻居发现协议 NDP

- IPv6 不再使用 ARP，所有地址解析、邻居发现功能由 **ICMPv6** 承载的 **NDP（Neighbor Discovery Protocol）** 完成。
- NDP 使用 5 种 ICMPv6 报文（类型 133~137）：
  - **RS（Router Solicitation，133）**：主机请求路由器通告。
  - **RA（Router Advertisement，134）**：路由器周期性通告或响应 RS，包含前缀、跳数限制、MTU。
  - **NS（Neighbor Solicitation，135）**：替代 ARP 请求，解析邻居链路层地址 + 重复地址检测。
  - **NA（Neighbor Advertisement，136）**：响应 NS，通告链路层地址。
  - **Redirect（137）**：路由器通知主机使用更好的下一跳。
- **与 ARP 对比**：NDP 在 ICMPv6（网络层）运行，使用组播而非广播，效率更高。

### 4. IPv6 扩展首部

- IPv6 基本首部固定 40 字节，可选信息通过**扩展首部**实现。
- 扩展首部链：基本首部 → 扩展首部1（下一首部字段指向扩展首部2）→ ... → 上层协议数据。
- 常见扩展首部：
  - **逐跳选项（Hop-by-Hop Options）**：每个路由器都检查（协议号 0）。
  - **路由首部（Routing Header）**：指定源路由（协议号 43）。
  - **分片首部（Fragment Header）**：仅源端可执行分片（协议号 44）。
  - **目的选项首部（Destination Options）**：仅目的端检查（协议号 60）。
  - **认证首部 AH（51）**和 **ESP（50）**：IPsec 安全扩展。

---

## 十五、移动 IP

### 1. 移动 IP 基本概念

- **移动 IP（Mobile IP）**：使移动节点（如手机、笔记本）可在不同网络间漫游而**保持通信不中断**，上层连接不因 IP 地址改变而断开。
- 移动节点有两个地址：
  - **家乡地址**：永久 IP 地址，在归属网络（Home Network）中分配，通信对端始终用此地址。
  - **转交地址 CoA（Care-of Address）**：在访问网络（Foreign Network）中获得的临时 IP 地址。
- 三个关键实体：
  - **移动节点 MN**：漫游的设备。
  - **家乡代理 HA（Home Agent）**：归属网络中的路由器，代理移动节点接收数据，通过隧道转发给移动节点。
  - **外地代理 FA（Foreign Agent）**：访问网络中的路由器，协助移动节点与家乡代理通信（MIPv4 有，MIPv6 通常不需要，MN 直接与 HA 通信）。

### 2. 移动 IPv4 工作过程

1. **代理发现**：移动节点收到 HA 或 FA 的代理通告（Agent Advertisement），判断当前在归属网络还是外地网络。
2. **注册**：移动节点向 HA 注册转交地址 CoA。
3. **数据转发**：
  - 通信对端 CN 向移动节点的家乡地址发送数据。
  - HA 截获数据报，通过 **IP-in-IP 隧道**封装，发往 CoA。
  - 移动节点解封装获得原始数据报。
4. **反向隧道**：移动节点发出的数据经 FA 通过隧道发回 HA，再由 HA 发往通信对端（解决入口过滤问题）。
- **三角路由问题**：CN → HA → MN 路径比 CN → MN 直接路径长，效率低。MIPv6 通过**路由优化**（CN 和 MN 直接通信）解决。

### 3. 移动 IPv6 改进

- **不再需要外地代理**：移动节点直接使用自己的 CoA（通过 SLAAC 或 DHCPv6 获取），自行注册到 HA。
- **路由优化**：移动节点可向通信对端直接发送**绑定更新**消息，使 CN 直接将数据发往 CoA，避免经 HA 绕路。
- 移动 IPv6 利用 IPv6 的扩展首部（目的选项首部、路由首部）承载移动性管理信令。

### 4. 移动 IP 常考点

- 移动 IP 使上层连接不因 IP 地址改变而中断。
- 核心机制：HA 截获 + IP-in-IP 隧道转发到 CoA。
- MIPv4 有三角路由问题；MIPv6 引入路由优化解决。
- 家乡代理 HA、转交地址 CoA 是关键概念。

---

## 十六、MPLS 多协议标签交换

### 1. MPLS 基本概念

- **MPLS（Multi-Protocol Label Switching）**：在 IP 网络层之下、数据链路层之上插入一个**标签交换层**，基于短固定长度的**标签**转发，而不分析 IP 首部。
- "多协议"指 MPLS 可承载多种网络层协议（IPv4、IPv6、IPX、以太网帧等）。
- 位置：MPLS 报头夹在 L2 帧头与 L3 分组头之间，通常称为 **L2.5 层**。

### 2. MPLS 核心概念

| 概念 | 说明 |
|------|------|
| **LER（Label Edge Router）** | 标签边缘路由器，在 MPLS 域边缘负责添加/删除标签。入口 LER（Ingress）压入标签；出口 LER（Egress）弹出标签 |
| **LSR（Label Switching Router）** | 标签交换路由器，在 MPLS 域内部根据标签转发，不查 IP 路由表 |
| **LSP（Label Switched Path）** | 标签交换路径，从入口 LER 到出口 LER 的一条单向路径 |
| **FEC（Forwarding Equivalence Class）** | 转发等价类，将具有相同转发处理方式的分组合并为一类，映射为同一标签 |

### 3. MPLS 标签格式

- MPLS 标签头 **4 字节（32 位）**，位于 L2 帧头与 IP 分组头之间：
  - **Label（标签值）**：20 位，0~15 保留（0=IPv4 显式空标签，3=隐式空标签）。
  - **EXP（实验/CoS）**：3 位，用于 QoS 服务等级。
  - **S（栈底标志）**：1 位，1 表示该标签为标签栈底，支持多级标签嵌套。
  - **TTL**：8 位，类似 IP TTL，逐跳减 1 防止环路。

### 4. MPLS 转发过程

1. 入口 LER 收到 IP 分组，根据 FEC 分类**压入标签**（Push），从 LSP 端口发出。
2. 中间 LSR 收到带标签分组，查标签转发表（LFIB），**交换标签**（Swap），从对应端口发出。不查 IP 路由表。
3. 倒数第二跳 LSR 可以执行**倒数第二跳弹出 PHP（Penultimate Hop Popping）**，提前弹出标签以减轻出口 LER 负担。
4. 出口 LER **弹出标签**（Pop），恢复为纯 IP 分组，按 IP 路由转发到目的地。

### 5. MPLS 主要应用

- **MPLS VPN**：利用两层标签栈——外层标签用于在 MPLS 域内转发（LSP 隧道标签），内层标签标识 VPN 归属（VPN 标签）。实现不同 VPN 客户的流量隔离。
- **流量工程 TE**：通过显式指定 LSP 路径优化网络资源利用，避免拥塞，绕过故障链路。
- **QoS 保障**：利用 EXP 位标记服务等级，提供差分服务。

### 6. 分段路由 SR（Segment Routing）简介

- SR 是 SDN 时代对 MPLS 的简化演进：
  - 去除了 LDP/RSVP-TE 等复杂的标签分发协议，通过 IGP（OSPF/IS-IS）或控制器分发标签。
  - 标签由源节点计算路径，压入标签栈，中间节点只按栈顶标签转发。
  - **SR-MPLS**：复用 MPLS 标签机制；**SRv6**：利用 IPv6 扩展首部（SRH）承载段列表。

### 7. MPLS 常考点

- MPLS 是 L2.5 层，基于标签转发，标签 4 字节（20 位 Label + 3 位 EXP + 1 位 S + 8 位 TTL）。
- LER 压入/弹出标签，LSR 交换标签；LSP 是单向标签路径。
- MPLS VPN 使用两层标签栈：外层隧道 + 内层 VPN 标识。
- PHP（倒数第二跳弹出）减轻出口 LER 负担。

---

## 十七、典型考点速记

- **子网划分公式**：借用 n 位 → 2^n 个子网，每子网 2^(h-n) - 2 个主机。
- **CIDR**：IP/N，前缀位为网络号；**路由聚合**找最长公共前缀；**最长前缀匹配**越具体越优先。
- **特殊 IP**：127.x 环回、169.254 链路本地、私有地址 10/172.16~31/192.168；主机全 0 是网络地址，全 1 是广播。
- **IP 分片**：片偏移单位 8 字节，MF=1 还有分片，DF=0 才能分片。
- **ARP**：广播请求 + 单播响应，跨网段解析的是网关 MAC。
- **DHCP 4 报文**：Discover→Offer→Request→Ack，全程广播（除确认）。
- **ICMP 应用**：ping 用回送请求/回答；tracert 用 TTL 递增 + 超时报文。
- **RIP**：UDP、跳数≤15、30s 整表、坏消息传得慢。
- **OSPF**：IP、Dijkstra、链路状态 LSA、收敛快、支持区域。
- **BGP**：TCP、路径向量、AS_PATH 防环、策略路由。
- **IPv6**：128 位、零压缩仅一次、取消广播、内置 IPsec、首部固定 40B 无校验和。
- **路由器转发流程**：TTL-1、重算校验和、最长前缀匹配、隔离广播域。
- **NAT**：私有 IP ↔ 公网 IP 转换，NAPT 用 IP+端口复用公网 IP；转换表维护双向映射。
- **网络安全**：对称加密（DES/AES）快但密钥分发难；公钥加密（RSA/ECC）分发易但慢，适合数字签名。
- **防火墙**：包过滤（网络层/传输层）高效粗粒度；应用网关（代理）细粒度慢；状态检测折中。
- **数字签名**：私钥签名公钥验证，保证不可否认性和完整性；CA 绑定身份与公钥。
- **组播**：D类地址 224~239，MAC 前缀 01-00-5E；IGMP 管理组成员，PIM-SM 有 RP 稀疏加入、PIM-DM 广播剪枝。
- **SDN**：数据面与控制面分离，OpenFlow 流表 L1~L4 多字段匹配，南向 OpenFlow/北向 REST API。
- **IPv6 深入**：全球单播 2000::/3，链路本地 FE80::/10，唯一本地 FD00::/8。SLAAC 无状态自动配置，NDP（NS/NA/RS/RA）替代 ARP。
- **移动 IP**：HA 家乡代理 + CoA 转交地址，IP-in-IP 隧道，MIPv4 三角路由 → MIPv6 路由优化。
- **MPLS**：L2.5 层标签交换，LER 压入/弹出、LSR 交换，标签 4B，MPLS VPN 两层标签栈。
`,
    timestamp: "2026-08-06 23:00",
    embed: ``
};

export default [note1];
