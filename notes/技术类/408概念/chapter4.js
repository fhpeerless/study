export const note1 = {
    title: "数据结构（四）：查找与排序",
    contentmd: `
# 数据结构（四）：查找与排序

> 定位：408考研数据结构第四章。重点掌握：折半查找判定树、散列表冲突处理与ASL计算、八大排序算法对比表、堆排序、快速排序、归并排序。

---

## 一、查找基本概念

- **查找表**：由同一类型数据元素构成的集合，支持查询、检索操作。
- **静态查找表**：仅查找、检索（顺序、折半、分块、散列）。
- **动态查找表**：插入、删除（BST、AVL、B树、散列）。
- **关键字**：数据元素中某数据项的值，用于标识数据元素。
- **平均查找长度 ASL = Σ(Pi × Ci)**：Pi 为查找第 i 个元素的概率，Ci 为查找所需比较次数。是衡量查找效率的主要指标。
- 区分**查找成功 ASL** 与**查找失败 ASL**。

---

## 二、静态查找表

### 1. 顺序查找
- **思想**：从表的一端开始逐个比较。
- 适用：顺序表、链表，有序或无序均可。
- **ASL（成功）= (n+1)/2**（等概率）。
- 查找失败：比较 n+1 次（设哨兵）。
- 时间复杂度 O(n)。

**优化（有序表）**：设置哨兵可简化边界判断；有序顺序查找失败时可比无序早终止（查到比关键字大/小处即停）。

### 2. 折半查找（二分查找，重点）
**条件**：**有序 + 顺序存储**（链表不行）。

**思想**：与中间元素比较，小于则在前半部分找，大于则在后半部分找。

**算法：**
- low=1, high=n
- mid = (low+high)/2
- key < A[mid]：high = mid-1
- key > A[mid]：low = mid+1
- key == A[mid]：找到
- low > high：查找失败

**判定树（描述折半查找过程的二叉树）：**
- 判定树是**平衡二叉树**（形态由元素个数决定，与具体值无关）。
- 树高 ⌊log₂n⌋+1（不大于完全二叉树高度）。
- **ASL ≈ log₂(n+1) - 1**。
- 查找失败：从根走到判定树外部结点，比较次数不超过树高。

**注意**：折半查找判定树的形态仅与元素个数 n 有关，与具体关键字值无关。中间位置取 ⌊(low+high)/2⌋ 时判定树形态确定。

### 3. 分块查找（索引顺序查找）
**结构**：块间有序、块内无序。建立索引表（每块最大关键字 + 起始地址）。

**查找过程**：先在索引表中折半/顺序查找确定块，再在块内顺序查找。

**ASL = 索引表 ASL + 块内 ASL**。
- 设 n 个元素分 b 块，每块 s 个元素（n = b×s）：
  - 顺序查找索引：ASL = (b+1)/2 + (s+1)/2
  - 折半查找索引：ASL ≈ ⌈log₂(b+1)⌉ + (s+1)/2

---

## 三、B树/B+树（动态查找表）

详见"数据结构（二）：树与二叉树"第八节。要点：
- **m 阶 B 树**：每结点最多 m 棵子树 m-1 关键字，非根至少 ⌈m/2⌉ 子树，叶同层。
- 插入超限**分裂**（中间关键字上移），删除不足**借/合并**。
- **B+树**：数据在叶，叶链表相连，内部结点仅作索引，子树数=关键字数。

---

## 四、散列表（Hash Table，重点）

### 1. 基本概念
- **散列表（哈希表）**：通过散列函数将关键字映射到存储位置，实现 O(1) 平均查找。
- **散列函数 H(key)**：将关键字映射到地址。
- **冲突**：不同关键字映射到相同地址（同义词）。
- **装填因子 α = 表中记录数 / 表长**，α 越大冲突越严重，是影响散列表查找效率的**主要因素**。

### 2. 散列函数构造方法
- **直接定址法**：H(key) = a×key + b。无冲突但适用范围窄。
- **除留余数法**：**H(key) = key mod p**，最常用，p 选不大于表长的最大素数。
- **数字分析法**：取关键字中分布均匀的若干位。
- **平方取中法**：取关键字平方的中间几位。
- **折叠法**：关键字分段相加。

### 3. 冲突处理方法

**开放定址法**：H_i = (H(key) + d_i) mod m
- **线性探测法**：d_i = 1,2,3,...,m-1。简单但会产生**堆积（聚集）**现象，降低效率。
- **平方探测法**：d_i = 1²,-1²,2²,-2²,...。缓解堆积，但可能探测不到所有位置（要求表长为 4k+3 形素数）。
- **再散列法（双散列）**：d_i = i×H₂(key)。
- **伪随机序列法**：d_i 为伪随机数。

**拉链法（链地址法）**：将所有同义词存储在同一单链表中。无堆积，删除方便，适合经常增删场景。

> 开放定址法删除结点不能直接置空（会断链），只能标记为"已删除"；拉链法可直接删除。

### 4. 散列表查找性能
- 查找效率取决于：**散列函数、冲突处理方法、装填因子 α**。
- 平均查找长度 ASL 是 α 的函数，主要与装填因子有关。
- 线性探测法成功 ASL ≈ (1 + 1/(1-α))/2。
- 拉链法成功 ASL ≈ 1 + α/2。
- 查找失败 ASL 也需会算（手算常见考题）。

### 5. 散列表手算步骤
1. 用散列函数算各元素地址。
2. 冲突时按指定方法探测。
3. 画出散列表后，分别计算成功/失败 ASL。
- **成功 ASL**：每个元素查找时的比较次数之和 / 元素数。
- **失败 ASL**：从每个地址出发到空位置的比较次数之和 / 地址数（mod p 时按 p 个位置算）。

> 注意：线性探测法查找失败时需探测到空位置才算失败；拉链法查找失败时比较到链表末尾。

---

## 五、排序基本概念

- **排序**：将无序序列排成有序序列。
- **稳定性**：相同关键字排序后相对次序不变则稳定，否则不稳定。
- **内部排序**：全部在内存中进行。
- **外部排序**：数据量大需借助外存。
- 排序的稳定性由具体算法决定，与序列无关。

---

## 六、插入排序

### 1. 直接插入排序
**思想**：将待排序元素插入到已排序子序列的正确位置。

**过程**：从第2个元素开始，依次将其插入前面已排序序列。

**性能：**
- 最好（基本有序）：比较 n-1 次，O(n)。
- 最坏/平均：O(n²)。
- 空间 O(1)，**稳定**。
- 适用于顺序表和链表。

### 2. 折半插入排序
- 用折半查找确定插入位置，减少比较次数到 O(n log n)。
- 但移动次数仍 O(n)，总时间仍 O(n²)。
- **稳定**，仅适用于顺序表。

### 3. 希尔排序（缩小增量排序）
**思想**：按增量 d 将序列分组，组内直接插入排序，逐步缩小 d 到1。

**过程**：d=n/2, n/4, ..., 1，每趟对各组做插入排序。

**性能：**
- 增量序列影响效率，常用 d=n/2, n/4, ..., 1。
- 平均时间约 **O(n^1.3)**，最坏 O(n²)。
- 空间 O(1)，**不稳定**。

---

## 七、交换排序

### 1. 冒泡排序
**思想**：相邻元素两两比较，逆序则交换，每趟将最大/最小元素"冒泡"到端部。

**过程**：n-1 趟，每趟比较相邻元素并交换逆序对。

**性能：**
- 最好（有序）：O(n)（设置交换标志，无交换则提前结束）。
- 最坏/平均：O(n²)。
- 空间 O(1)，**稳定**。
- 每趟确定一个最终位置（最大或最小）。

### 2. 快速排序（重点）
**思想**：选枢轴（pivot），将小于枢轴的放左、大于的放右，对左右子序列递归。

**算法（划分过程）：**
1. 选枢轴（通常首元素），保存枢轴值。
2. 双指针 low 从左、high 从右向中间扫描。
3. high 向左找比枢轴小的元素移到 low 位置。
4. low 向右找比枢轴大的元素移到 high 位置。
5. 重复 3-4 直到 low==high，枢轴放入此位置（最终位置）。
6. 递归处理枢轴左右两部分。

**性能：**
- 最好/平均：O(n log n)。
- **最坏（基本有序/逆序）：O(n²)**，此时退化为冒泡。
- 空间：递归栈，最好 O(log n)，最坏 O(n)。
- **不稳定**。
- **每趟确定一个枢轴的最终位置**。
- 是**平均性能最好**的内部排序。

**优化**：枢轴选"三者取中"（首、中、尾中位数）减少最坏情况概率。

---

## 八、选择排序

### 1. 简单选择排序
**思想**：每趟选出剩余部分最小元素放到前面。

**过程**：第 i 趟从第 i 到 n 个元素中选最小，与第 i 个交换。

**性能：**
- 比较次数固定 O(n²)，与初始序列无关。
- 移动次数最多 O(n)。
- 空间 O(1)，**不稳定**。
- 每趟确定一个最终位置。

### 2. 堆排序（重点）
**堆定义**：完全二叉树，任一非叶结点值 ≤（小根堆）或 ≥（大根堆）其孩子值。

**建堆**：从最后一个非叶结点（⌊n/2⌋）开始，自下而上调整。建堆时间 **O(n)**。

**堆调整（下沉）**：比较结点与孩子，将较大/较小孩子上移，递归向下。每次调整 **O(log n)**。

**堆排序过程：**
1. 建大根堆（升序用大根堆）。
2. 将堆顶（最大值）与末尾交换，堆规模减1。
3. 调整堆顶使其重新满足堆性质。
4. 重复 2-3 直到堆规模为1。

**性能：**
- 时间：建堆 O(n) + n 次调整 O(n log n) = **O(n log n)**，最好最坏都是。
- 空间 O(1)，**不稳定**。
- 适合大数据量，不适合记录数少。

**堆的应用：**
- 优先队列。
- Top-K 问题：维护大小为 K 的堆。
- **插入元素**：放到末尾后**上浮**调整 O(log n)。
- **删除堆顶**：末尾移到堆顶后**下沉**调整 O(log n)。

> 堆的插入向上调整（与双亲比较），删除向下调整（与孩子比较）。

---

## 九、归并排序

**思想**：将两个或多个有序序列合并为一个有序序列，递归二路归并。

**过程：**
1. 将序列看作 n 个长度为1的有序子序列。
2. 两两归并为长度为2的有序序列。
3. 重复直到合并为一个序列。

**性能：**
- 时间：**O(n log n)**，最好最坏都是。
- 空间：**O(n)**（需辅助数组），是各内部排序中空间最大的。
- **稳定**。
- 适合**外部排序**（多路归并）。
- 归并趟数 ⌈log₂n⌉（n 个元素二路归并）。

---

## 十、基数排序

**思想**：借助"分配"和"收集"实现，按关键字各位依次排序（LSD 最低位优先 / MSD 最高位优先）。

**过程（LSD）：**
1. 按个位分配到 r 个队列，收集。
2. 按十位分配，收集。
3. 重复 d 次（d 为关键字位数）。

**性能：**
- 时间：**O(d×(n+r))**，d 为关键字位数，r 为基数（每位取值数）。
- 空间：O(r)（队列数）。
- **稳定**。
- 适合关键字位数少、取值范围小的情况。
- **不需要比较关键字**，是分配式排序。

---

## 十一、排序算法对比表（必背）

| 排序算法 | 平均时间 | 最好时间 | 最坏时间 | 空间复杂度 | 稳定性 |
|---------|---------|---------|---------|-----------|--------|
| 直接插入 | O(n²) | O(n) | O(n²) | O(1) | 稳定 |
| 折半插入 | O(n²) | O(n²) | O(n²) | O(1) | 稳定 |
| 希尔排序 | O(n^1.3) | O(n) | O(n²) | O(1) | 不稳定 |
| 冒泡排序 | O(n²) | O(n) | O(n²) | O(1) | 稳定 |
| 快速排序 | O(nlog n) | O(nlog n) | O(n²) | O(log n) | 不稳定 |
| 简单选择 | O(n²) | O(n²) | O(n²) | O(1) | 不稳定 |
| 堆排序 | O(nlog n) | O(nlog n) | O(nlog n) | O(1) | 不稳定 |
| 归并排序 | O(nlog n) | O(nlog n) | O(nlog n) | O(n) | 稳定 |
| 基数排序 | O(d(n+r)) | O(d(n+r)) | O(d(n+r)) | O(r) | 稳定 |

**记忆要点：**
- **稳定**：直接插入、折半插入、冒泡、归并、基数（口诀："**插冒归基**稳"）。
- **不稳定**：希尔、快速、简单选择、堆。
- **每趟确定一个元素最终位置**：冒泡、快速（枢轴）、简单选择、堆（堆顶）。
- **时间复杂度与初始序列无关**：简单选择、堆、归并、基数（这些最坏=平均）。
- **排序趟数与初始序列无关**：简单选择、基数、归并。
- **排序趟数与初始序列有关**：冒泡（设置交换标志）、快速、插入。
- 快速排序是**平均性能最好**的内部排序。
- 基本有序时**直接插入、冒泡**最优，可达 O(n)。
- 堆排序空间 O(1)，归并排序空间 O(n)，快排空间 O(log n)（递归栈）。

---

## 十二、外部排序（第2版重点扩充）

### 1. 外部排序基本思想

数据量大无法全部装入内存，需借助外存（磁盘/磁带）。外部排序通常采用**归并排序**的思想：
- 第一阶段（预处理）：将大文件分块读入内存，用内部排序对各块排序，产生**初始归并段**（有序子文件），写回外存。
- 第二阶段（归并）：通过**多路归并**将初始归并段逐步合并为一个有序文件。

### 2. 多路平衡归并与败者树

**问题**：k 路归并时，每选一个最小元素需比较 k-1 次，总比较次数 = (k-1)×(n-1)×趟数。k 增大虽然减少归并趟数，但每趟比较次数大幅增加。

**败者树（Loser Tree）** 正是解决此问题的数据结构：通过树形结构将每选最小元素的比较次数从 k-1 降为 **⌈log₂k⌉**。

**败者树的定义与构造：**
- 败者树是一棵**完全二叉树**，叶子结点是 k 个归并段的当前参与比较元素。
- 每个**内部结点记录"败者"**（即比较中较大者所在的归并段编号），胜者继续向上比较。
- 树根上方的结点记录最终**"胜者"**（最小值所在的归并段编号）。

**败者树的构造过程：**
1. 将 k 个归并段的当前最小元素分别放入叶子结点。
2. 自底向上两两比较，败者（较大者）记在父结点，胜者（较小者）继续上比。
3. 根结点上方的额外结点存放全局最小元素（胜者）的归并段编号。
4. 输出最小元素后，从该归并段取下一个元素放入对应叶子，然后沿该叶到根的路径重新调整（只需与沿途父结点记录的败者比较）。

**构造败者树的时间**：O(k)（初始建树需要 k-1 次比较）。**每次调整**：仅需沿路径比较 ⌈log₂k⌉ 次（因为只走一条从叶子到根的路径）。

**示例（4 路归并）**：4 个归并段当前最小值为 (5, 8, 3, 12)：
    - 叶 0: 段0=5, 叶 1: 段1=8, 叶 2: 段2=3, 叶 3: 段3=12
    - 内部结点 0: (叶0,叶1) 败者=段1(8), 胜者=段0(5)
    - 内部结点 1: (叶2,叶3) 败者=段3(12), 胜者=段2(3)
    - 内部结点 2: (内部0胜者=段0(5), 内部1胜者=段2(3)) 败者=段0(5), 胜者=段2(3)
    - 全局胜者 = 段2(3)，输出 3 后从段2取下一元素替换叶2，沿路径向上调整。

> 败者树中，每次调整仅调整叶子到根的一条路径，时间复杂度 O(log k)。k 路归并总比较次数 = (n-1)×⌈log₂k⌉ × 归并趟数。

### 3. 置换-选择排序（生成初始归并段）

**目标**：生成尽可能长的初始归并段，以减少归并趟数。

**思想**：利用大小为 w 的内存工作区，通过"当前段"与"下一段"的标记，尽量延长当前归并段。

**算法步骤：**
1. 从输入文件读 w 个记录到内存工作区 WA，建立**败者树**（或最小堆）。
2. 选出 WA 中最小关键字记录 minMax 输出到当前归并段，标记 minMax 为当前段的最小阈值。
3. 从输入文件再读一个记录替换已输出的记录。
4. 若新记录关键字 ≥ 阈值 minMax，该记录仍属于**当前归并段**。
5. 若新记录关键字 < 阈值 minMax，该记录属于**下一归并段**，暂时不参与当前段选最小。
6. 重复取 WA 中"属于当前段"的最小记录输出，直到 WA 中所有记录都属于下一段。
7. 此时当前段结束，将阈值复位，下一段记录变为当前段，继续处理。

**置换-选择排序效果**：
- 当输入文件随机排列时，产生的初始归并段平均长度约为 **2w**（w 为内存工作区大小）。
- 比固定长度排序（每段长度 w）的初始段数减少约一半，从而减少归并趟数。

> 置换-选择排序是"生成初始归并段"阶段的优化，配合多路归并使用，是完整外部排序方案的关键部分。

### 4. 最佳归并树（Optimal Merge Tree）

**问题**：使用置换-选择排序后，各初始归并段长度不相等。如何安排归并顺序，使总 I/O 次数最少？

**最佳归并树**：以各归并段长度为权值，构建一棵**k 叉哈夫曼树**（严格 k 叉树）。

**构造方法：**
1. 将所有初始归并段长度作为叶子权值。
2. 仿照哈夫曼树的构建，每次选 k 个最小权值合并。
3. 合并后的权值（长度之和）作为新结点。
4. 重复直到只剩一个结点。

**虚段（虚设归并段）**：若叶结点数不足 (k-1) 的倍数加 1，需补权值为 0 的虚段，使树为**严格 k 叉树**。

**严格 k 叉树条件**：设叶结点数为 m，归并路数为 k：
- 若 (m - 1) % (k - 1) == 0，无需虚段。
- 若 (m - 1) % (k - 1) = r ≠ 0，则需补充 **k - 1 - r** 个权值为 0 的虚段。

**示例**：8 个归并段长度分别为 2, 3, 6, 9, 12, 17, 18, 24，做 3 路归并：
- m=8, k=3, (8-1)%(3-1)=7%2=1≠0，需补 2-1=1 个虚段（权值 0）。
- 按哈夫曼思想每次选 3 个最小权值合并，得最佳归并树。
- 树的带权路径长度（WPL）即总 I/O 次数。

> 最佳归并树使得总归并读写次数最小。总 I/O 次数 = 2 × WPL（读写各一次），其中 WPL 为最佳归并树的带权路径长度。

### 5. 外部排序总 I/O 次数分析

- 设文件共有 m 个初始归并段，k 路归并，则归并趟数 s = ⌈log_k m⌉。
- 每趟归并需将全部 n 个记录读入一次、写出一次，即 2n 次 I/O。
- 总 I/O 次数 = **2n × s + 初始生成时的 2n** = 2n × (s+1)。
- 优化方向：增大 k（多路归并）减少趟数 → 用败者树降低每趟比较代价；增大归并段长度 w（置换-选择排序）减少段数 m → 用最佳归并树降低不等长段的总合并代价。

---

## 十三、考点速记

- 折半查找仅适用于**有序顺序表**，判定树为平衡二叉树，ASL≈log₂(n+1)-1。
- 分块查找：块间有序块内无序，ASL=索引ASL+块内ASL。
- 散列表三因素：散列函数、冲突处理、装填因子；α 越大冲突越多。
- 线性探测有堆积现象，平方探测缓解，拉链法无堆积。
- 散列表成功 ASL 看每个元素比较次数，失败 ASL 看每个地址到空的探测次数。
- 排序稳定性口诀："**插冒归基**稳"，其余不稳。
- 一趟定位置：冒泡、快排、选择、堆。
- 快排平均最快但最坏 O(n²)；归并稳定但费空间 O(n)；堆排序空间 O(1)。
- 排序趟数与初始序列无关：选择、基数、归并；有关：快排、冒泡、插入。
- 堆排序建堆 O(n)，每次调整 O(log n)，总 O(n log n)。
- 归并趟数 ⌈log₂n⌉，基数排序趟数 d（关键字位数）。
`,
    timestamp: "2026-08-06 18:30",
    embed: ``
};

export default [note1];
