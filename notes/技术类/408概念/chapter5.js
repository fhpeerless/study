export const note1 = {
    title: "计算机组成原理（一）：计算机系统概论与数据表示",
    contentmd: `
# 计算机组成原理（一）· 计算机系统概论与数据表示

> 定位：408考研复习第一章。重点掌握：冯·诺依曼架构、存储程序思想、计算机层次结构、性能指标（主频/CPI/MIPS）、RISC-V指令集基本概念、SoC片上系统组成、定点数四种机器数表示、IEEE754浮点数、海明码与CRC校验。

---

## 一、计算机发展历程与层次结构

### 1. 计算机发展四代

| 代次 | 元件 | 时间 |
|------|------|------|
| 第一代 | 电子管 | 1946-1957 |
| 第二代 | 晶体管 | 1958-1964 |
| 第三代 | 中小规模集成电路 | 1965-1971 |
| 第四代 | 大规模/超大规模集成电路 | 1972至今 |

- **摩尔定律**：集成电路上可容纳的晶体管数目约每18个月翻一番，性能也提升一倍。

### 2. 计算机层次结构（从下到上）

| 层级 | 名称 | 说明 |
|------|------|------|
| 第0层 | 微程序机器层 | 微指令组成的微程序，由硬件直接执行 |
| 第1层 | 传统机器语言层 | 机器指令执行 |
| 第2层 | 操作系统层 | 提供系统调用接口 |
| 第3层 | 汇编语言层 | 汇编指令 |
| 第4层 | 高级语言层 | C/Java等高级语言 |

- **翻译程序**：汇编程序（汇编语言→机器语言）、编译程序（高级语言→汇编/机器语言）、解释程序（逐句解释执行）。
- **透明性**：某一层用户看不到的下层细节称为对该用户透明。

---

## 一-附、RISC-V 指令集简介（第3版新增）

### 1. RISC-V 基本概念

**RISC-V**（读作"RISC-Five"）是一个**开源、免费**的精简指令集架构（RISC ISA），由加州大学伯克利分校于 2010 年开发。其名称中的"V"表示第五代 RISC 架构。

### 2. RISC-V 核心特点

| 特点 | 说明 |
|------|------|
| **开源免费** | 指令集规范完全开放，无需授权费用，任何人都可设计 RISC-V 处理器 |
| **模块化设计** | 采用基础指令集 + 标准扩展的模块化方式，可根据需求灵活裁剪 |
| **简洁规整** | 基本整数指令集 RV32I 仅约 40 条指令，编码格式规整 |
| **支持多种字长** | RV32（32位）、RV64（64位）、RV128（128位） |
| **可扩展性强** | 通过标准扩展支持乘法、浮点、原子操作、压缩指令、向量等 |

### 3. RISC-V 指令集模块

| 模块 | 名称 | 说明 |
|------|------|------|
| **I** | 基本整数指令集 | 必备基础模块，含整数运算、访存、分支等 |
| **M** | 整数乘除法扩展 | 硬件乘除法支持 |
| **A** | 原子操作扩展 | 支持多核同步的原子指令 |
| **F** | 单精度浮点扩展 | IEEE754 单精度浮点运算 |
| **D** | 双精度浮点扩展 | IEEE754 双精度浮点运算 |
| **C** | 压缩指令扩展 | 16 位压缩指令，提高代码密度 |
| **V** | 向量扩展 | SIMD 向量运算支持 |

- **RV32IMAFD** 表示：32位基本整数 + 乘除 + 原子 + 单精度浮点 + 双精度浮点（通用计算常用组合）。
- 最小完整实现是 **RV32I**，其他扩展均为可选。

### 4. RISC-V 寄存器与寻址

- **32 个通用寄存器**：x0~x31，其中 x0 硬连接到 0（永远为 0）。
- **寄存器-寄存器架构**：只有 LOAD/STORE 指令访问存储器，其他指令在寄存器间操作。
- 寻址方式简洁：寄存器寻址、立即数寻址、基址偏移寻址（用于访存）、PC 相对寻址（用于分支跳转）。

### 5. RISC-V 与 x86、ARM 对比

| 对比项 | x86（CISC） | ARM（RISC） | RISC-V（RISC） |
|--------|------------|------------|---------------|
| 授权 | 闭源，Intel/AMD 授权 | 闭源，ARM 公司授权 | **开源免费** |
| 指令数 | 多（数百条） | 中等 | 少（RV32I 约40条） |
| 指令长度 | 可变（1~15字节） | 固定32位（Thumb 16位） | 可变（基础32位，压缩16位） |
| 寻址复杂度 | 复杂 | 简单 | 极简 |
| 应用领域 | PC、服务器 | 移动设备、嵌入式 | IoT、嵌入式、AI加速器 |

### 6. RISC-V 的意义

- 打破了 x86 和 ARM 的指令集垄断，降低了处理器设计门槛。
- 学术界和工业界可自由研究、定制和优化处理器微架构。
- 国内大量芯片公司（如平头哥、芯来科技等）基于 RISC-V 设计处理器。

---

## 一-附、SoC 片上系统简介（第3版新增）

### 1. SoC 基本概念

**SoC（System on Chip，片上系统）**：将计算机系统的全部或大部分功能部件集成在**单一芯片**上，包括 CPU 核、GPU 核、存储器、I/O 接口、总线、专用加速器等。

### 2. SoC 典型组成

| 部件 | 说明 |
|------|------|
| **CPU 核** | 一个或多个处理器核心（如 ARM Cortex-A 系列、RISC-V 核心） |
| **GPU 核** | 图形处理单元，用于图形渲染或并行计算 |
| **NPU / AI 加速器** | 神经网络处理单元，用于人工智能推理加速 |
| **存储器** | 片内 SRAM、Cache（L1/L2/L3）、可能集成 DRAM 控制器 |
| **I/O 接口** | USB、HDMI、PCIe、SPI、I2C、UART 等 |
| **总线 / 互联** | 片内总线（如 AXI、AHB、NoC 片上网络）连接各部件 |
| **外设控制器** | 定时器、中断控制器、DMA 控制器、电源管理 |

### 3. SoC 与 CPU 的区别

| 对比项 | 传统 CPU 芯片 | SoC |
|--------|-------------|-----|
| 集成内容 | 主要为 CPU 核心 + Cache | CPU + GPU + 内存控制器 + I/O + 外设 |
| 系统完整度 | 需配合南北桥芯片组 | 单芯片即可组成完整系统 |
| 功耗 | 较高 | 较低（针对移动设备优化） |
| 代表产品 | Intel Core、AMD Ryzen | 高通骁龙、苹果 A/M 系列、华为麒麟 |

### 4. SoC 设计方法

- **IP 核复用**：SoC 设计大量使用可复用的 IP 核（Intellectual Property Core），如 CPU IP（ARM Cortex）、GPU IP（ARM Mali、Imagination PowerVR）等。
- **IP 核分类**：
  - **软核**：用 HDL（如 Verilog）描述的 RTL 代码，灵活可改。
  - **固核**：门级网表形式，经过部分优化。
  - **硬核**：版图级物理设计，性能确定但不可修改。
- **SoC 设计流程**：IP 核选型 → 集成与互联设计 → 功能验证 → 综合与布局布线 → 流片。

### 5. SoC 的应用领域

- **智能手机**：高通骁龙、苹果 A 系列、华为麒麟（集成 CPU+GPU+5G基带+NPU）。
- **平板/笔记本**：苹果 M1/M2/M3（基于 ARM 架构的桌面级 SoC）。
- **嵌入式/IoT**：ESP32、瑞芯微 RK 系列。
- **汽车电子**：智能座舱芯片、自动驾驶芯片（如 NVIDIA Orin、高通 Snapdragon Ride）。

---

## 二、冯·诺依曼架构

### 1. 冯·诺依曼思想核心

冯·诺依曼机的设计思想（存储程序概念）：
1. **指令和数据同等地位**，都存放在存储器中。
2. **存储程序**：程序和数据事先存入存储器，机器按存储器中的指令序列自动执行。
3. **由五大部件组成**：运算器、控制器、存储器、输入设备、输出设备。
4. 指令由**操作码**和**地址码**组成。
5. 以**运算器为中心**（现代计算机改为以存储器为中心）。

### 2. 五大部件功能

| 部件 | 功能 |
|------|------|
| 运算器（ALU） | 算术运算和逻辑运算 |
| 控制器（CU） | 指挥协调各部件工作 |
| 存储器 | 存放程序和数据 |
| 输入设备 | 将外部信息转换为计算机能识别的形式 |
| 输出设备 | 将计算机结果转换为人能识别的形式 |

### 3. 冯·诺依曼机与现代计算机区别

| 项目 | 冯·诺依曼机 | 现代计算机 |
|------|------------|-----------|
| 中心 | 运算器为中心 | 存储器为中心 |
| 总线 | 单总线 | 多总线 |
| I/O方式 | 程序查询 | 中断、DMA |

---

## 三、性能指标（高频考点）

### 1. 基本性能指标

| 指标 | 含义 | 关系 |
|------|------|------|
| **主频 f** | CPU时钟频率 | f = 1/T，T为时钟周期 |
| **时钟周期 T** | CPU最小时间单位 | T = 1/f |
| **CPI** | 执行一条指令所需平均时钟周期数 | CPI = 总时钟周期数 / 指令数 |
| **MIPS** | 每秒执行百万条指令 | MIPS = 指令数 / (执行时间 × 10^6) = 主频 / (CPI × 10^6) |
| **FLOPS** | 每秒浮点运算次数 | 用于科学计算衡量 |
| **CPU执行时间** | 程序运行总时间 | = 指令数 × CPI × 时钟周期 = 指令数 × CPI / 主频 |

### 2. 关键公式

- **CPU执行时间 = (指令数 × CPI) / 主频**
- **MIPS = 主频 / (CPI × 10^6)**
- 等效CPI：CPI = Σ(CPIi × fi)，其中 fi 为第 i 类指令使用频率

### 3. 其他指标

- **机器字长**：CPU一次能处理的二进制数据的位数（通常等于寄存器位数、ALU宽度）。
- **存储容量**：MAR位数决定可寻址单元数，MDR位数决定每个存储单元的位数。
  - 地址寄存器 MAR 宽度 = 24 位 → 可寻址 2^24 = 16MB（按字节编址）
- **总线宽度**：数据总线一次传输的位数。

---

## 四、数据表示基础

### 1. 机器数与真值

- **真值**：带正负号的数（如 +5、-3）。
- **机器数**：连同符号一起数字化表示的数（符号位0正1负）。
- **字长**：机器数位数，如8位、16位、32位。

### 2. 进制转换

- 二→十：按权展开求和
- 十→二：整数除2取余（逆序），小数乘2取整（顺序）
- 二→八：3位一组；二→十六：4位一组
- **常用：1字节(B)=8位(b)，1KB=1024B，1MB=1024KB，1GB=1024MB**

### 3. BCD码

- **8421BCD码**：4位二进制表示1位十进制，权值8421。如 9 = 1001。
- **余3码**：8421码 + 0011，即 +3。

---

## 五、定点数表示

### 1. 机器数四种表示

设字长为 n+1 位（1位符号位 + n位数值位），表示小数 X = ±0.x1x2...xn 或整数 X = ±x1x2...xn：

| 编码 | 正数 | 负数 | 0的表示 |
|------|------|------|---------|
| **原码** | 符号位0 + 真值绝对值 | 符号位1 + 真值绝对值 | +0 = 0...0；-0 = 1...0...0（两种） |
| **反码** | 同原码 | 符号位1 + 绝对值按位取反 | +0 = 0...0；-0 = 1...1（两种） |
| **补码** | 同原码 | 反码末位+1（模2运算） | 0 = 0...0（唯一） |
| **移码** | 补码符号位取反 | 补码符号位取反 | 0 = 1...0...0（唯一） |

### 2. 补码核心要点（重点）

- **补码表示范围（n+1位整数）**：-2^n 到 2^n - 1，多表示一个最小负数。
- **补码表示范围（n+1位小数）**：-1 到 1 - 2^-n。
- **补码求原码方法**：负数补码再次"取反加一"得到原码。
- **补码运算**：符号位参与运算，连同符号位一起取反加一。
- **变形补码（双符号位）**：00正、11负、01上溢、10下溢，便于判断溢出。

### 3. 原码与补码范围对比表

| 表示方式 | n+1位整数范围 | n+1位小数范围 | 0的个数 |
|---------|--------------|--------------|---------|
| 原码 | -(2^n-1) ~ 2^n-1 | -(1-2^-n) ~ 1-2^-n | 2个 |
| 反码 | 同原码 | 同原码 | 2个 |
| 补码 | -2^n ~ 2^n-1 | -1 ~ 1-2^-n | 1个 |
| 移码 | 同补码 | 同补码 | 1个 |

### 4. 移码用途

- **移码主要用于表示浮点数的阶码**，便于比较大小（移码按无符号数比较即可判断大小）。
- 移码 = 真值 + 偏置常数（如 2^n）。

---

## 六、浮点数表示

### 1. 浮点数格式

**浮点数 = 符号 S + 阶码 E + 尾数 M**

- **阶码 E**：决定数的范围（指数部分）。
- **尾数 M**：决定数的精度（小数部分）。
- 公式：**N = (-1)^S × 1.M × 2^(E-127)**（IEEE754单精度）。

### 2. IEEE754标准

| 类型 | 总位数 | 符号位 | 阶码位数 | 尾数位数 | 偏置常数 | 隐藏位 |
|------|--------|--------|---------|---------|---------|--------|
| 单精度 | 32 | 1 | 8 | 23 | 127 | 1 |
| 双精度 | 64 | 1 | 11 | 52 | 1023 | 1 |

- **隐藏位**：规格化数尾数默认前有1，存储时省略，节省1位精度。
- **真值公式**（单精度）：N = (-1)^S × 1.M × 2^(E-127)

### 3. IEEE754表示范围（单精度）

| 类型 | 阶码E | 尾数M | 真值 |
|------|-------|-------|------|
| 0 | 0 | 0 | ±0 |
| 非规格化 | 0 | ≠0 | (-1)^S × 0.M × 2^(-126) |
| 规格化 | 1~254 | 任意 | (-1)^S × 1.M × 2^(E-127) |
| 无穷 | 255 | 0 | ±∞ |
| NaN | 255 | ≠0 | 非数 |

- **规格化范围**：约 ±1.18×10^-38 ~ ±3.4×10^38
- **单精度最小规格化正数**：2^(-126) × 1.0
- **单精度最大规格化正数**：(2-2^-23) × 2^127

### 4. 浮点数规格化

- **IEEE754规格化**：隐含尾数前有1（即1.M形式）。
- **规格化目的**：保证尾数精度最高，提高表示精度。
- **对阶操作**：小阶向大阶看齐，尾数右移，每右移一位阶码加一。

### 5. 浮点数舍入方法

- 就近舍入（默认，0舍1入）
- 朝 +∞ 舍入
- 朝 -∞ 舍入
- 朝 0 舍入（截断）

---

## 七、数据校验码

### 1. 校验原理

- **码距**：两个合法编码之间不同的二进制位数的最小值。
- **检错能力**：码距 d 可检 d-1 位错。
- **纠错能力**：码距 d 可纠 ⌊(d-1)/2⌋ 位错。

### 2. 奇偶校验码

- **奇校验**：使1的个数为奇数。
- **偶校验**：使1的个数为偶数。
- **特点**：只能检测奇数个错误，不能纠错，码距=2。
- **应用**：内存条、串口通信。

### 3. 海明码（Hamming Code，重点）

**核心思想**：在数据位之间插入多个校验位，每个校验位负责一组特定位置，能检错且纠错一位错。

**校验位个数确定公式**：**2^k ≥ n + k + 1**（n为数据位，k为校验位）

**海明码分组规则**：校验位 P_i 位于第 2^(i-1) 位（第1、2、4、8、16...位），数据位填充其他位置。

**分组规则**：第 j 位若其二进制表示中第 i 位为1，则被第 i 个校验位校验。

**纠错过程**：
- 计算各校验组的检错值（偶校验应为0）。
- 将结果按 Pk...P2P1 组合得到错误位置二进制值。
- 若结果为0，无错；否则将对应位取反即可纠正。

**特点**：码距=3，能检错2位、纠错1位错误。增加一位总校验位后码距=4，可纠1位检2位。

### 4. CRC循环冗余校验码（重点）

**核心思想**：利用生成多项式 G(x) 对数据做模2除法，得到校验位附加在数据后。

**生成步骤**：
1. 数据后面追加 k 个 0（k = 生成多项式最高次幂，即多项式位数-1）。
2. 用追加0后的数据对生成多项式做**模2除法**（异或运算）。
3. 余数即为CRC校验码，替换掉后面的k个0。

**模2除法规则**：
- 不借位减法，即异或运算。
- 上商规则：首位是1商1，首位是0商0。

**检错与纠错**：
- 接收方用收到的整个数据对生成多项式做模2除法。
- 余数为0，传输正确；余数不为0，传输有错。
- **常用生成多项式**：CRC-16、CRC-CCITT、CRC-32。
- 能检测所有奇数位错、双位错、长度小于等于k的突发错误。

---

## 八、定点与浮点运算（重点）

### 1. 定点补码加减运算

**补码加法**：[X+Y]补 = [X]补 + [Y]补（符号位参与运算）。

**补码减法**：[X-Y]补 = [X]补 + [-Y]补 = [X]补 + [[Y]补]求补（[Y]补 全部位取反末位加1）。

**溢出判断（三种方法）：**
- **单符号位法**：正+正=负 或 负+负=正 → 溢出。
- **双符号位法（变形补码）**：结果双符号位为 01=正溢出（上溢），10=负溢出（下溢），00=正，11=负。运算时双符号位一同参与运算。
- **进位判断法**：符号位进位 Cn 与最高数值位进位 Cn-1 异或为1 → 溢出。即 **V = Cn ⊕ Cn-1**。

### 2. 原码一位乘法

- 符号位单独处理：乘积符号 = 被乘数符号 ⊕ 乘数符号。
- 数值部分为绝对值相乘，每次根据乘数末位（当前最低位）决定是否加被乘数。
- 部分积与乘数右移一位（逻辑移位），共 n 次加法、n 次移位。
- 运算器结构：**ACC（部分积高位）+ MQ（乘数/部分积低位）+ X（被乘数）**。
- 原码乘法 **n 位 × n 位 → 2n 位** 结果。

**步骤：**
1. 初始化：ACC=0，MQ=乘数绝对值，X=被乘数绝对值。
2. 若 MQ 最低位=1，则 ACC += X；否则加 0。
3. ACC 和 MQ **逻辑右移** 一位（高位补0）。
4. 重复 2-3 共 n 次。
5. 加上符号位，得到最终结果。

### 3. 补码一位乘法（Booth 算法）

- 符号位参与运算，结果直接为补码形式。
- 在乘数末尾增设附加位 y_(n+1)=0。
- **Booth 算法规则**（根据乘数当前位 yi 和附加位 y_(i+1)）：

| y_i | y_(i+1) | 操作 |
|-----|---------|------|
| 0 | 0 | 部分积 +0，算术右移一位 |
| 0 | 1 | 部分积 +[X]补，算术右移一位 |
| 1 | 0 | 部分积 +[-X]补，算术右移一位 |
| 1 | 1 | 部分积 +0，算术右移一位 |

- 算术右移：高位补符号位（保持补码性质）。
- 共 n+1 次加法、n+1 次移位。
- 初始：ACC=0，MQ=乘数补码 + 附加位 0。

**原码 vs 补码乘法对比：**

| 项目 | 原码乘法 | Booth 补码乘法 |
|------|---------|---------------|
| 符号位 | 单独处理 | 参与运算 |
| 右移方式 | 逻辑右移 | 算术右移 |
| 结果 | 原码 | 补码 |
| 步数 | n 次 | n+1 次 |

### 4. 原码除法（不恢复余数法 / 加减交替法）

- 符号单独处理（异或），数值为绝对值相除。
- 运算器：ACC（被除数/余数）+ MQ（商）+ X（除数）。
- 经典方法：**恢复余数法**（试商，不够减需恢复）+ **不恢复余数法（加减交替法）**（不够减时下一步加除数替代恢复）。
- **加减交替法步骤**：
  1. ACC = 被除数，MQ = 0，X = 除数。
  2. ACC -= X（第一次必做）。
  3. 余数为正：商上 1，ACC 和 MQ 左移一位，下一步 ACC -= X。
  4. 余数为负：商上 0，ACC 和 MQ 左移一位，下一步 ACC += X。
  5. 重复 n 次（n 位商），最后一步若余数为负则需恢复（+X）。
- 若被除数 ≥ 除数，除法溢出。

### 5. 补码除法（加减交替法）

- 符号位参与运算，商符在运算中形成。
- 第一步：若被除数与除数同号则 ACC -= X，异号则 ACC += X。
- 上商规则：余数与除数同号 → 商 1；异号 → 商 0。
- 下一步：**商 1 → ACC -= X**；商 0 → ACC += X。
- 末位商恒置 1（提高精度）。
- 共 n 步，最后一步后不再移位。

### 6. 浮点四则运算（重点）

**浮点加/减法步骤：**
1. **对阶（求阶差）**：小阶向大阶看齐，阶码小的尾数算术右移（每右移1位阶码+1），直到两数阶码相等。
   - 右移时可能丢失精度。
2. **尾数求和**：对阶后的两尾数按定点加减运算。
3. **规格化**：
   - **左规**：尾数溢出（如 01.xxx 或 10.xxx）→ 尾数算术右移1位，阶码+1。
   - **右规**：尾数绝对值 < 1/2（补码形式 00.0xxx 或 11.1xxx）→ 尾数左移直到规格化，阶码相应减少。
   - 规格化判断（补码）：双符号位最高数值位与符号位不同（如 00.1xxx 或 11.0xxx）。
4. **舍入**：对阶或右规时丢弃的低位需舍入：
   - **0 舍 1 入法**（就进舍入）：丢弃位最高位为 1 则进 1。
   - **恒置 1 法**：只要丢弃位非全 0，新的最低位置 1。
5. **判溢出**：阶码超出范围 → 浮点数溢出。阶码上溢 → 结果溢出；阶码下溢 → 结果为机器零。

**浮点乘法步骤：**
1. 阶码相加（注意偏置常数：E_结果 = E1 + E2 - 127）。
2. 尾数相乘（定点乘法）。
3. 规格化与舍入。
4. 判溢出。

**浮点除法步骤：**
1. 阶码相减（E_结果 = E1 - E2 + 127）。
2. 尾数相除（定点除法）。
3. 规格化与舍入。
4. 判溢出。

### 7. 算术逻辑单元 ALU 与进位链

**加法器：**
- **串行进位加法器（行波进位）**：每位进位依次传递，延迟 O(n)。速度慢但结构简单。
- **并行进位加法器（先行进位/超前进位 CLA）**：直接由输入产生各位进位，延迟 O(1)。速度快但电路复杂。
  - 进位产生函数 Gi = Ai·Bi（本位产生进位）。
  - 进位传递函数 Pi = Ai ⊕ Bi（本位将低位的进位传递到高位）。
  - 进位公式：C_i+1 = Gi + Pi·Ci。
  - 先行进位：CL = G3 + P3·G2 + P3·P2·G1 + P3·P2·P1·G0 + P3·P2·P1·P0·C0。
- **分组并行进位（组内并行、组间串行/并行）**：
  - 单级先行进位（组内并行、组间串行）。
  - 多级先行进位（组内并行、组间也并行），如 74181+74182 芯片组合。

**ALU 芯片**：
- 74181：4 位 ALU，可实现 16 种算术运算 + 16 种逻辑运算。
- 74182：先行进位产生器（CLA Generator）。
- 4 片 74181 + 1 片 74182 可实现 16 位并行进位 ALU。

---

## 九、典型计算题套路

### 1. 性能指标计算

- 已知主频 f 和 CPI，求 MIPS = f / (CPI × 10^6)
- 已知指令数和CPI求执行时间：T = N × CPI / f
- 多类指令混合CPI：CPI = Σ(CPIi × fi)

### 2. 补码范围与转换

- 判断溢出：变形补码双符号位 01 为正溢出、10 为负溢出。
- 求补码：原码 → 取反加1 → 补码（仅负数需要此步骤）。
- 已知补码求真值：负数补码"取反加1"得到绝对值。

### 3. 浮点数IEEE754转换

- 十进制 → IEEE754：先转为二进制 → 规格化（小数点移到第1个1后）→ 计算阶码（指数+127）→ 组合S+E+M。
- IEEE754 → 十进制：分解S、E、M → 计算真值 (-1)^S × 1.M × 2^(E-127)。

### 4. CRC计算步骤

1. 写出信息位和生成多项式二进制形式。
2. 信息位后补 k 个0。
3. 模2除法得到余数。
4. 余数替换掉补的0，得到发送数据。

---

## 十、速通口诀

- **冯诺依曼五部件**：存、算、控、入、出（"存算控入出"）。
- **存储程序**是核心：指令和数据同等地位、按地址顺序执行。
- **性能三件套**：主频决定速度上限、CPI决定指令代价、MIPS = 主频/(CPI×10^6)。
- **机器数四种**：原码直观、反码过渡、补码运算、移码比阶。
- **补码多一个负数**：-2^n（n+1位整数）或-1（n+1位小数）。
- **IEEE754公式**：(-1)^S × 1.M × 2^(E-127)，注意隐藏位1。
- **海明公式**：2^k ≥ n + k + 1，校验位在 2 的幂次位。
- **CRC**：补0、模2除（异或）、余数替换。
- **码距口诀**：检错 d-1、纠错 ⌊(d-1)/2⌋。
- **补码加减**：符号位参与运算；溢出判断三法：单符号位看正负、双符号位看 01/10、进位异或法 V=Cn⊕Cn-1。
- **原码乘法**：符号单独异或、绝对值相乘、逻辑右移、n 次加法 n 次移位。
- **Booth 补码乘法**：看 yi 与 y_(i+1)，01 加[X]补、10 加[-X]补、其余加0；算术右移；共 n+1 次。
- **除法**：加减交替法比恢复余数法更优；原码除看余数正负定商，补码除看余数与除数同异号定商。
- **浮点加减五步**：对阶（小向大看齐）→ 尾数求和 → 规格化（左规/右规）→ 舍入（0舍1入/恒置1）→ 判溢出。
- **ALU 进位链**：串行慢 O(n)、并行快（CLA，Gi/Pi 函数）、分组并行折中。
- **浮点乘除**：阶码加/减（注意偏置常数）、尾数乘/除、规格化舍入、判溢出。
- **RISC-V**：开源免费 ISA、模块化（I/M/A/F/D/C/V）、RV32I 约 40 条指令、32 通用寄存器（x0=0）。
- **SoC 片上系统**：CPU+GPU+存储器+I/O+总线 在单芯片集成，IP 核复用（软核/固核/硬核）是核心设计方法。
`,
    timestamp: "2026-08-06 22:00",
    embed: ``
};

export default [note1];
