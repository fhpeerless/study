export const note1 = {
    title: "操作系统（二）：处理机调度与死锁",
    contentmd: `
# 操作系统 · 处理机调度与死锁 · 408考试速通复习

> 定位：概念+高频考点速记。重点掌握：三级调度层次、五种调度算法对比与计算、死锁四条件、银行家算法安全性序列、信号量机制。

---

## 一、处理机调度

### 1. 调度的基本概念

**处理机调度**：从就绪队列中按一定算法选择一个进程，将处理机分配给它运行。

**调度的层次（三级调度）：**

| 层次 | 别名 | 调度对象 | 频率 | 目的 |
|------|------|---------|------|------|
| **高级调度** | 作业调度 | 作业（外存→内存） | 最低（几分钟一次） | 决定哪些作业调入内存，建立进程 |
| **中级调度** | 内存调度 | 挂起的进程（内存↔外存交换） | 中等 | 提高内存利用率，实现虚拟存储 |
| **低级调度** | 进程调度 | 进程（就绪→运行） | 最高（毫秒级） | 决定哪个就绪进程获得 CPU |

- **高级调度**：发生于批处理系统，每个作业只调入一次、调出一次。无→创建态→就绪态。
- **中级调度**：将暂不能运行的进程调至外存（挂起态），需要时再调回。用于内存紧张时。
- **低级调度**：最基本的调度，所有类型系统都有。就绪态→运行态。

**七状态模型（加入挂起）：** 在五状态基础上增加**就绪挂起**和**阻塞挂起**。
- 挂起：将进程从内存移到外存，处于静止状态。
- 激活：将进程从外存调回内存。

### 2. 调度的时机、切换与过程

**调度时机：**
- 引起调度的因素：进程完成、I/O 请求、时间片用完、原语操作、更高优先级进程到达、进程自我阻塞。
- **不能调度的情况**：处理中断过程中、进程在操作系统内核临界区中、其他需要完全屏蔽中断的原子操作过程中。

**进程调度方式：**
- **非抢占式（非剥夺式）**：一旦进程获得 CPU，就一直运行到完成或阻塞才释放。实现简单，系统开销小；适用于批处理系统，不适用于实时/分时系统。
- **抢占式（剥夺式）**：可根据原则（时间片、优先级）剥夺当前运行进程的 CPU。适用于分时/实时系统，调度灵活但开销大。
- 抢占原则：时间片原则、优先权原则、短作业优先原则。

**进程切换过程（开销）：**
1. 保存当前进程现场（寄存器、PC、PSW 等）到 PCB。
2. 修改被切换进程的 PCB（如运行→就绪）。
3. 将其 PCB 插入相应队列。
4. 选择新进程，恢复其现场。
5. 更新内存管理相关寄存器。

> **"调度"与"切换"的区别**：调度是决定谁运行，切换是实际转换。调度不一定会引起切换（如选中同一进程）。

### 3. 调度算法的评价指标

| 指标 | 定义 | 公式 |
|------|------|------|
| **CPU 利用率** | CPU 处于忙状态的时间占比 | 忙时间/总时间 |
| **系统吞吐量** | 单位时间内完成的作业数 | 完成作业数/总时间 |
| **周转时间** | 从作业提交到完成的时间 | 完成时间 - 提交时间 |
| **带权周转时间** | 周转时间与运行时间之比 | 周转时间/运行时间（≥1） |
| **平均周转时间** | 各作业周转时间的平均 | Σ周转时间/n |
| **平均带权周转时间** | 各作业带权周转时间的平均 | Σ带权周转/n |
| **等待时间** | 进程在就绪队列中等待的时间 | 周转时间 - 运行时间 - I/O 时间 |
| **响应时间** | 从用户提交请求到首次响应的时间 | - |

**关系：**
- 周转时间 = 等待时间 + 运行时间（+ I/O 时间）。
- 带权周转时间 ≥ 1，越接近 1 越好。
- 响应时间是分时系统和交互式系统的重要指标。

### 4. 经典调度算法（重点）

#### (1) 先来先服务 FCFS

- **规则**：按作业/进程到达顺序依次调度。
- **方式**：非抢占式。
- **优点**：公平、实现简单；对长作业有利。
- **缺点**：对短作业不利（"护航效应"），I/O 繁忙型进程等待时间长；有利于 CPU 繁忙型，不利于 I/O 繁忙型。
- **适用于**：批处理系统，长作业。

#### (2) 短作业优先 SJF / 短进程优先 SPF

- **规则**：选择估计运行时间最短的作业/进程优先调度。
- **方式**：非抢占式（SJF）/ 抢占式（SRTF 最短剩余时间优先）。
- **优点**：平均等待时间最短、平均周转时间最短。
- **缺点**：对长作业不利（"饥饿"现象）；需预知运行时间（估计不准确）；未考虑紧迫性。
- **SJF 是默认抢占式的吗？** 不是，SJF 通常是非抢占式；抢占式版本叫 SRTF（最短剩余时间优先）。

> **饥饿**：进程长期得不到调度。SJF 中长作业可能饥饿。

#### (3) 优先级调度 PSA

- **规则**：按优先级选择进程，高优先级先执行。
- **方式**：可抢占式或非抢占式。
- **优先级类型**：
  - **静态优先级**：创建时确定，运行期间不变。简单但可能饥饿。
  - **动态优先级**：随进程运行而改变（如等待时间长则提高优先级）。可避免饥饿。
- **优先级设置原则**：
  - 系统进程 > 用户进程。
  - I/O 繁忙型 > CPU 繁忙型（让 I/O 早点执行，提高设备利用率）。
  - 短作业 > 长作业。
- **缺点**：低优先级进程可能饥饿。

#### (4) 时间片轮转 RR

- **规则**：每个就绪进程轮流执行一个时间片，时间片用完后被抢占，排到就绪队列末尾。
- **方式**：抢占式。
- **适用于**：分时系统。
- **优点**：响应时间快，公平，无饥饿。
- **缺点**：时间片大小影响性能。时间片太大→退化为 FCFS；时间片太小→切换频繁，开销大。
- **时间片选择**：一般取 10~100ms，应略大于一次典型交互所需时间。

#### (5) 多级反馈队列调度 MLFQ

- **规则**：设置多个就绪队列，不同队列有不同优先级和时间片。新进程进入最高优先级队列，时间片用完降级到下一队列；高优先级队列空时才调度低优先级队列；高优先级队列有新进程可抢占低优先级。
- **方式**：抢占式。
- **队列设计**：优先级从高到低，时间片从小到大（如队列1时间片为 q，队列2为 2q，队列3为 4q）。
- **优点**：兼顾短作业（高优先级队列）、长作业（最终在最低队列按 RR 调度）；自适应性强；对 I/O 繁忙型有利；无饥饿（若采用"提升"机制）。
- **是公认的较好的调度算法**。

**调度算法对比：**

| 算法 | 抢占 | 公平 | 优点 | 缺点 | 适用 |
|------|------|------|------|------|------|
| FCFS | 否 | 是 | 简单公平 | 短作业不利，护航效应 | 批处理 |
| SJF | 否/可 | 否 | 平均等待最短 | 长作业饥饿 | 批处理 |
| 优先级 | 可 | 否 | 灵活，区分紧迫性 | 低优先级饥饿 | 实时系统 |
| RR | 是 | 是 | 响应快 | 切换开销 | 分时系统 |
| MLFQ | 是 | 较公平 | 自适应，综合最优 | 实现复杂 | 通用 |

### 5. 调度算法计算题模板

**例题格式：** 给定若干作业的提交时间和运行时间，求 FCFS、SJF 下的平均周转时间、平均带权周转时间。

**计算步骤：**
1. 按调度算法确定执行顺序。
2. 逐个计算完成时间、周转时间（完成-提交）、带权周转（周转/运行）。
3. 求平均。

> **注意**：SJF 在多个作业已就绪时选最短，未到达的不能选。SRTF 抢占式则需要在新进程到达或当前进程剩余时间被超越时重新选择。

---

## 二、同步与互斥（基础铺垫）

### 1. 基本概念

- **临界资源**：一段时间内只允许一个进程访问的资源。
- **临界区**：进程中访问临界资源的那段代码。
- **互斥**：多个进程不能同时访问临界资源。
- **同步**：多个进程按一定顺序协调执行（直接制约关系）。
- **互斥是特殊同步**：保证对临界资源排他性访问。

**临界区管理四原则：**
1. **空闲让进**：临界区空闲时，应允许一个进程进入。
2. **忙则等待**：已有进程在临界区时，其他进程必须等待。
3. **有限等待**：等待进程应在有限时间内进入，避免死锁/饥饿。
4. **让权等待**：不能进入临界区的进程应释放 CPU，避免"忙等"。

### 2. 信号量机制（重点）

**信号量 S**：整型变量，表示系统中某类资源的数量。

- **整型信号量**（早期）：wait(S) 和 signal(S)，存在"忙等"，违反让权等待。
- **记录型信号量**（现代）：含 value 和链表 L，value 为资源数，L 为等待队列。能避免忙等。

**P 操作（wait）：** 申请资源
- S.value = S.value - 1
- 若 S.value < 0，进程阻塞，加入 S.L 等待队列。
- 否则继续执行。

**V 操作（signal）：** 释放资源
- S.value = S.value + 1
- 若 S.value ≤ 0，从 S.L 唤醒一个等待进程。
- 否则继续。

**信号量的应用：**
- **互斥**：初值 S=1，使用 P(S)...V(S) 包裹临界区。
- **同步（前驱关系）**：前驱操作完成后 V(S)，后继操作开始前 P(S)，初值 S=0。
- **资源计数**：初值为资源数 n。

> **同步关系判断技巧**：一前一后，前者执行完 V 后者 P。"前 V 后 P"。

### 3. 经典同步问题

**生产者-消费者问题：**
- 三个信号量：mutex=1（互斥访问缓冲区）、empty=n（空闲缓冲区数）、full=0（已用缓冲区数）。
- 生产者：P(empty) → P(mutex) → 放产品 → V(mutex) → V(full)
- 消费者：P(full) → P(mutex) → 取产品 → V(mutex) → V(empty)
- **关键**：P(empty)/P(full) 必须在 P(mutex) 之前，否则可能死锁。

**读者-写者问题：**
- 读读共享、读写互斥、写写互斥。
- 读者优先：用 readcount 统计读者数，第一个读者 P(wmutex)，最后一个读者 V(wmutex)。
- 写者优先：增加信号量限制读者进入。

**哲学家进餐问题：**
- 5 个哲学家，5 根筷子，每人需左右两根筷子才能吃饭。
- 简单方案可能死锁（所有人都拿起左筷子）。
- 解决方法：限制人数（最多4人同时拿筷子）、奇偶号拿筷子顺序相反、一次拿两根（记录型信号量）。

---

## 三、死锁

### 1. 死锁的概念

**死锁**：多个进程因竞争资源而造成的一种僵局，若无外力干预，这些进程都无法继续推进。

**死锁 vs 饥饿 vs 死循环：**
- **死锁**：所有相关进程都被阻塞，互相等待。
- **饥饿**：进程长期得不到资源（但可能未被阻塞，或仍有希望）。
- **死循环**：进程在循环执行，不被阻塞，但不进展（编程错误）。

### 2. 死锁产生的原因

1. **资源竞争**：争夺不可剥夺资源。
2. **进程推进顺序非法**：请求和释放资源顺序不当。

**资源分类：**
- **可重用资源**（永久资源）：如 CPU、内存、I/O 设备，使用时不消耗。
- **消耗性资源**（临时资源）：如消息、信号，使用后消失。
- **可剥夺资源**：可被强制剥夺，如 CPU、内存。
- **不可剥夺资源**：不能强制剥夺，如打印机、磁带机。

### 3. 死锁产生的四个必要条件（必背）

1. **互斥条件**：资源一次只能被一个进程使用。
2. **占有并等待（请求和保持）**：进程已占有至少一个资源，又请求新资源，被阻塞时不释放已占有资源。
3. **不剥夺条件**：进程已获得的资源在未使用完前不能被强行剥夺。
4. **循环等待条件**：存在进程-资源的循环等待链 {P0→P1→...→Pn→P0}。

> **四个条件必须全部满足**才会发生死锁。打破任意一个即可避免死锁。

### 4. 死锁处理策略

四种策略，按严格程度递减：

| 策略 | 思路 | 优缺点 |
|------|------|--------|
| **预防死锁** | 破坏死锁四个必要条件之一 | 简单，但资源利用率低 |
| **避免死锁** | 资源分配前检查是否安全（银行家算法） | 资源利用率较高，但需预知需求 |
| **检测与解除** | 允许死锁发生，检测到后解除 | 性能好，但解除代价大 |
| **鸵鸟策略** | 忽略死锁（认为概率低） | 简单，UNIX/Windows 实际采用 |

### 5. 死锁预防

通过破坏四个必要条件之一：

- **破坏互斥条件**：使资源可共享（如 SPOOLing 把独占设备改共享）。但有些资源（如打印机）本质互斥，难以实现。
- **破坏占有并等待**：进程运行前一次性申请所有资源。**优点**：简单；**缺点**：资源利用率低，可能饥饿。
- **破坏不剥夺条件**：进程请求新资源失败时，释放已占有的资源。**缺点**：实现复杂，可能导致前功尽弃。
- **破坏循环等待**：资源有序分配，给每类资源编号，按序申请。**缺点**：编号困难，资源浪费。

### 6. 死锁避免——银行家算法

**安全状态**：存在一个安全序列，使所有进程都能顺利执行完成。
**不安全状态**：可能（不一定）导致死锁。

**银行家算法（Dijkstra 提出）思想：**
- 进程开始前声明对各类资源的最大需求。
- 每次进程请求资源时，系统先"试探性"分配，检查分配后是否处于安全状态。
- 安全则真正分配；否则让进程等待。

**数据结构：**
- Available[m]：系统当前可用各类资源数。
- Max[n][m]：进程 i 对资源 j 的最大需求。
- Allocation[n][m]：进程 i 已占有资源 j 数。
- Need[n][m]：进程 i 还需资源 j 数 = Max - Allocation。
- Request[n][m]：进程 i 当前请求资源数。

**安全性算法步骤：**
1. 初始化：Work = Available；Finish[i] = false（对所有 i）。
2. 找一个 i 满足 Finish[i]=false 且 Need[i] ≤ Work。
3. 若找到：Work = Work + Allocation[i]；Finish[i] = true；回到步骤2。
4. 若所有 Finish[i] = true，则安全；否则不安全。

**银行家算法流程：**
1. 若 Request[i] ≤ Need[i]，继续；否则出错（请求超过声明）。
2. 若 Request[i] ≤ Available，继续；否则进程等待。
3. 试探分配：Available -= Request[i]；Allocation[i] += Request[i]；Need[i] -= Request[i]。
4. 执行安全性检查：若安全，正式分配；否则回滚，进程等待。

**银行家算法特点：**
- 是一种**死锁避免**算法，不是预防。
- 限制条件：必须预知最大需求、进程数固定、资源数固定。
- 缺点：每次分配都要进行安全性检查，开销大；实际系统很少使用。

### 7. 死锁检测与解除

**死锁检测：**
- 资源分配图：进程 P 用圆圈，资源 R 用方框（含资源实例点），请求边 P→R，分配边 R→P。
- **死锁定理**：资源分配图不可完全化简则发生死锁。
- 化简方法：找既不阻塞又不独立的进程，删除其所有边，重复直至无法化简。

**死锁解除方法：**
1. **资源剥夺法**：挂起某些死锁进程，抢占其资源给其他进程。
2. **撤销进程法（终止进程法）**：撤销部分或全部死锁进程。可按优先级、代价等选择。
3. **进程回退法**：让进程回退到某个检查点，重新执行。需保留检查点信息。

---

## 四、易错点速记

- **高级调度无抢占**（作业一旦调入内存不可剥夺）；低级调度可抢占。
- **SJF 的平均等待时间最短**，但可能让长作业饥饿；SJF 默认非抢占，SRTF 是其抢占版。
- **FCFS 对短作业不利**（长作业在前则短作业等待久），存在"护航效应"。
- **时间片轮转**时间片太大退化为 FCFS，太小则切换开销大。
- **多级反馈队列**是综合最优的算法：短作业高优先级、长作业降级按 RR、I/O 繁忙型有利。
- 死锁四条件**必须全部满足**才死锁；预防死锁打破**任一**即可。
- **破坏循环等待**用资源有序分配；**破坏请求和保持**用一次性申请。
- 银行家算法属于**死锁避免**，需预知最大需求；不安全状态**不一定**死锁。
- **安全序列不唯一**，只要存在一个就安全。
- 银行家算法中，进程释放资源后 Available 增加，可能使原不安全状态变安全。
- **死锁一定不安全，但不安全不一定死锁**。
- P 操作可能使进程阻塞，V 操作可能唤醒进程。
- **P(empty)/P(full) 必须在 P(mutex) 之前**，否则可能死锁（生产者-消费者）。
- 临界区管理原则中**让权等待**：不能进入则应释放 CPU，避免忙等。
- 死锁预防实现简单但资源利用率低；避免（银行家）效率中等；检测解除资源利用率最高但恢复代价大。
- **信号量初值**：互斥=1，同步=0，资源计数=n。
- 周转时间 = 完成时间 - 提交时间；带权周转 = 周转/运行（≥1）。
`,
    timestamp: "2026-08-06 18:30",
    embed: ``
};

export default [note1];
