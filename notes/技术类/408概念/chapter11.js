export const note1 = {
    title: "操作系统（三）：内存管理",
    contentmd: `
# 操作系统 · 内存管理 · 408考试速通复习

> 定位：概念+高频考点速记。重点掌握：分区分配算法对比、分页地址转换与TLB、页面置换算法计算（FIFO/LRU/CLOCK）、Belady异常、虚拟内存原理。

---

## 一、内存管理基本概念

### 1. 内存管理的功能

**内存管理**：操作系统对内存的分配、回收、保护、扩充等功能。

**四大功能：**
1. **内存分配与回收**：为程序分配内存空间，使用完毕回收。
2. **内存保护**：各进程在自己的地址空间运行，互不干扰。
3. **地址转换（重定位）**：将逻辑地址转换为物理地址。
4. **内存扩充**：通过虚拟存储技术，从逻辑上扩充内存容量。

### 2. 程序的装入与链接

**编译→链接→装入：**
- **编译**：源程序 → 目标模块（可重定位目标代码）。
- **链接**：目标模块 + 库 → 装入模块（逻辑地址）。
  - **静态链接**：运行前完成全部链接。
  - **装入时动态链接**：装入内存时链接，便于更新。
  - **运行时动态链接**：运行中需要时才链接，节省内存。
- **装入**：将装入模块放入内存。
  - **绝对装入**：编译时给出物理地址，仅单道程序。
  - **静态重定位（可重定位装入）**：装入时地址转换，运行中不再改变。要求连续空间。
  - **动态重定位（运行时动态装入）**：运行时借助**重定位寄存器（基址寄存器）**进行地址转换，可将进程分散存放。**现代OS采用**。

### 3. 逻辑地址与物理地址

- **逻辑地址（相对地址/虚拟地址）**：程序生成的地址，从0开始。
- **物理地址（绝对地址）**：内存单元的真实地址。
- **地址空间**：逻辑地址的集合（虚拟地址空间）；物理地址的集合（内存空间）。

### 4. 内存保护

- **方法1：上下界寄存器**：存放进程的上下界，访问越界则出错。
- **方法2：基址寄存器 + 限长寄存器**：基址为起始物理地址，限长为进程长度。物理地址 = 基址 + 逻辑地址，需满足 逻辑地址 < 限长。

> 重定位寄存器（基址寄存器）在**用户态不能修改**，由OS在内核态设置。

---

## 二、连续分配方式

### 1. 单一连续分配

- 内存分为系统区（低地址）和用户区（高地址），用户区一次只放一道作业。
- **优点**：简单、无外部碎片。
- **缺点**：只能单道、资源利用率低、有内部碎片。

### 2. 固定分区分配

- 内存划分为若干固定大小的分区，每个分区可放一道作业。
- **分区大小相等**：适用于多个相同程序，浪费空间。
- **分区大小不等**：划分为多个小分区、中分区、大分区，按作业大小分配。
- **数据结构**：分区说明表（分区大小、起始地址、状态）。
- **优点**：可实现多道、实现简单。
- **缺点**：程序太大装不下；程序小于分区则产生**内部碎片**。

> **内部碎片**：分配给进程的分区中未被利用的部分（已分配但没用）。**外部碎片**：分区太小无法分配的空闲空间（太小没用）。

### 3. 动态分区分配（可变分区分配）

- 根据进程实际需要动态划分分区，分区大小等于进程大小。
- **初始无内部碎片，但会产生外部碎片**。
- **数据结构**：空闲分区表或空闲分区链。

**分区分配过程：**
1. 分配：找一满足要求的空闲分区，切出进程所需大小，剩余部分作为新空闲分区。
2. 回收：相邻空闲分区需合并。

**动态分区分配算法（重点对比）：**

| 算法 | 策略 | 特点 |
|------|------|------|
| **首次适应 FF（First Fit）** | 按地址递增顺序，选第一个满足大小的空闲分区 | 简单高效，性能通常最好；低地址碎片多 |
| **最佳适应 BF（Best Fit）** | 选能满足需求的最小空闲分区 | 节省大空间；但产生大量小碎片（外部碎片多） |
| **最坏适应 WF（Worst Fit）/最大适应** | 选最大的空闲分区 | 减少小碎片；但大进程可能无足够空间 |
| **邻近适应 NF（Next Fit）/循环首次适应** | 从上次查找位置继续找下一个满足的 | 避免低地址碎片集中；整体性能接近 FF |

> **记忆**：FF 用地址顺序，BF 用容量升序选最小够用，WF 用容量降序选最大，NF 用循环指针。**首次适应算法实际性能通常最好**。

**分区回收：**
- 相邻空闲分区合并：前邻接、后邻接、前后都邻接则三合一。

**外部碎片解决：紧凑技术（拼接）**：将进程移动使空闲分区合并。需动态重定位支持，开销大。

---

## 三、基本分页存储管理

### 1. 分页机制

**分页**：将进程的逻辑地址空间分为若干大小相等的**页**，内存物理空间分为相同大小的**页框（物理块/帧）**，页与页框大小相同。

- **页面大小**：2 的整数次幂（便于地址转换），通常 2^10 ~ 2^12 B（1~4KB）。
- **页内碎片**：进程最后一页未填满的部分（内部碎片）。平均页内碎片 = 页大小/2。
- 页面太小：页表长，内存开销大；页面太大：内部碎片大。需折中。

**逻辑地址结构：**
- 页号 P（高位）+ 页内偏移 W（低位）。
- 若页面大小 2^k，则逻辑地址低 k 位为偏移 W，高位为页号 P。
- 例：页面大小 4KB = 2^12 B，则低 12 位为偏移，高位为页号。

### 2. 页表

**页表**：记录逻辑页号到物理页框号的映射，存放在内存中。

- 每个进程一张页表。
- 页表项 PTE：页号 → 物理块号（帧号）。
- 页表项长度通常 = 物理块号位数 + 标志位（存在位、访问位、修改位等）。
- **页表本身也占用内存**，可能很大（如 32 位地址、4KB 页，需 1M 项 × 4B = 4MB 页表）。

### 3. 地址转换（重点）

**基本地址转换机构**（无快表）：
1. 由逻辑地址分出页号 P 和页内偏移 W。
2. 比较页号 P 与页表长度 M（页表项数），若 P ≥ M 则越界中断。
3. 页表始址 + P × 页表项长度 → 找到页表项，得到物理块号 b。
4. 物理地址 = b × 页大小 + W。

**两次访存**：第一次访存查页表，第二次访存取目标数据。**访存速度减半**。

**有效访问时间 EAT（无快表）** = 2 × t（t为一次访存时间）。

### 4. 快表 TLB（重点）

**快表（Translation Lookaside Buffer）**：高速缓存（相联存储器），存放最近访问的页表项，加速地址转换。

- **TLB 命中**：直接得到物理块号，无需访存查页表，仅 1 次访存。
- **TLB 未命中**：查内存中的页表（1次访存），并更新 TLB，再取目标（1次访存），共 2 次访存。

**有效访问时间 EAT（有快表）：**
- EAT = 命中率 × (查TLB时间 + 1次访存) + (1 - 命中率) × (查TLB时间 + 2次访存)
- 简化：EAT = t(TLB查询) + 命中率 × t(访存) + (1-命中率) × 2t(访存)
- 实际TLB查询可与cache访问并行，更复杂。

**TLB 特点：**
- 由硬件实现，对程序员透明。
- 容量小但速度快（与寄存器同数量级）。
- 局部性原理使其命中率较高（可达 90% 以上）。
- TLB 中的页表项是页表的子集，二者内容一致。

### 5. 两级页表与多级页表

**问题**：单级页表太大，需连续存放，占用大量内存。

**两级页表**：将页表再分页。
- 逻辑地址：**页目录号 + 页表号 + 页内偏移**。
- 32 位系统：10位页目录 + 10位页表 + 12位偏移 = 32位。
- 访问过程：查页目录表 → 查二级页表 → 取数据，**3次访存**。

**多级页表优点**：页表不必连续存放，可以离散分配；可以只将当前需要的页表调入内存。**缺点**：访存次数增加。

> **注意**：多级页表**不能减少**页表总空间，但能避免页表连续存放的要求。

### 6. 具有快表的分页地址转换流程总结

> 逻辑地址 → 分出页号 P、偏移 W
> ↓
> 查 TLB —— 命中 ——→ 物理块号 b ——→ 物理地址 = b × 页大小 + W
> ↓ 未命中
> 查页表（内存）→ 物理块号 b → 更新 TLB → 物理地址

---

## 四、基本分段存储管理

### 1. 分段机制

**分段**：按程序的逻辑结构（如主程序、子程序、数据段）将地址空间分为若干段，每段从0开始编址。

- **段长**：各段长度可不等，由程序逻辑决定。
- 段是信息的逻辑单位，对程序员可见（分页对程序员透明）。
- 有利于程序共享、保护、动态链接。

**逻辑地址结构：** 段号 S（高位）+ 段内偏移 W（低位）。
- 段号位数决定最大段数；偏移位数决定最大段长。

### 2. 段表

**段表**：记录段号 → 段起始物理地址（基址）+ 段长的映射。

- 段表项 STE：段号、段基址、段长、状态位、访问控制位。
- 段表存放在内存中。

### 3. 地址转换

1. 逻辑地址分出段号 S、偏移 W。
2. 比较段号 S 与段表长度，越界则中断。
3. 段表始址 + S × 段表项长度 → 找到段表项。
4. 比较偏移 W 与段长，越界则中断（**分段需查段长**）。
5. 物理地址 = 段基址 + W。

**两次访存**：查段表 + 取数据。也可用 TLB 加速。

### 4. 分页 vs 分段（必考）

| 项目 | 分页 | 分段 |
|------|------|------|
| 划分依据 | 物理划分，大小固定 | 逻辑划分，大小可变 |
| 程序员可见性 | 透明 | 可见 |
| 地址空间 | 一维地址空间 | 二维地址空间（段号+段内） |
| 大小 | 固定（硬件决定） | 可变（程序决定） |
| 碎片 | 内部碎片 | 外部碎片 |
| 共享/保护 | 困难（一页可能含多个逻辑段） | 容易（一段即一个逻辑单位） |
| 动态链接 | 不便 | 方便 |
| 越界检查 | 仅检查页号（页内偏移必合法） | 检查段号和段内偏移 |

---

## 五、段页式存储管理

### 1. 段页式机制

**段页式**：先分段，每段再分页。结合分段（逻辑清晰、便于共享）与分页（无外部碎片）的优点。

**逻辑地址结构：** 段号 S + 段内页号 P + 页内偏移 W。

**地址转换过程：**
1. 由段号 S 查段表，得到该段的页表始址。
2. 由页号 P 查该段页表，得到物理块号 b。
3. 物理地址 = b × 页大小 + W。

**三次访存**：查段表 + 查页表 + 取数据。可用 TLB 加速。

### 2. 段页式特点

- 既有内部碎片（页内碎片），无外部碎片。
- 系统开销较大，地址转换复杂。
- 现代OS（如Linux x86）多采用段页式（但段相对简单）。

---

## 六、虚拟内存

### 1. 虚拟内存的概念

**虚拟内存**：基于局部性原理，将内存和外存结合，为用户提供比实际物理内存大得多的"虚拟"地址空间。

**局部性原理（重点）：**
- **时间局部性**：最近访问的指令/数据，短期内可能再次访问（如循环）。
- **空间局部性**：访问某地址后，其邻近地址也可能被访问（如顺序执行、数组）。
- 局部性原理是虚拟内存和缓存的基础。

**虚拟内存的实现方式：** 请求分页、请求分段、请求段页式。最常用是**请求分页**。

**虚拟内存特征：**
1. **多次性**：作业可分多次调入内存（每次只调入部分页）。
2. **对换性**：内存与外存之间可换入换出。
3. **虚拟性**：从逻辑上扩充内存容量。

### 2. 请求分页管理

**请求分页**：在基本分页基础上，增加**调页功能**和**页面置换功能**，仅调入当前需要的页。

**页表项扩展（与基本分页相比增加）：**
- **状态位（存在位）**：该页是否在内存中。
- **访问位（引用位）**：最近是否被访问（供置换算法用）。
- **修改位**：是否被修改过（决定换出时是否写回外存）。
- **外存地址**：该页在外存的位置。

### 3. 缺页中断

**缺页中断**：访问的页不在内存时，由硬件产生缺页中断，OS将所需页从外存调入内存。

**缺页中断处理过程：**
1. 查页表，状态位=0（不在内存）→ 产生缺页中断。
2. OS检查内存是否有空闲块：有则直接调入；无则执行页面置换算法选一页换出。
3. 若被换出页修改位=1，先写回外存；否则直接覆盖。
4. 调入新页，更新页表（状态位=1、块号、清访问位/修改位）。
5. 重新执行被中断的指令。

**缺页中断特点：**
- 属于**内中断（异常）**。
- 在**指令执行期间**产生和处理（一般中断在指令执行后）。
- 一条指令可能产生**多次缺页中断**（如跨页指令、操作数跨页）。
- 缺页中断处理后**重新执行**该指令（而非下一条）。

### 4. 地址转换流程（请求分页）

> 逻辑地址 → 分出页号 P、偏移 W → 查 TLB
> ↓
> 查页表 → 状态位 = 1?
> 　 是 → 物理块号 b → 物理地址 → 取数据
> 　 否 → 缺页中断 → 调页/置换 → 更新页表 → 重新执行

### 5. 页面置换算法（重点）

#### (1) 最佳置换 OPT

- **策略**：选择未来最长时间不被访问的页面换出。
- **特点**：理论上最优，**缺页率最低**；但无法实现（需预知未来）。
- **用途**：作为评价其他算法的基准。

#### (2) 先进先出 FIFO

- **策略**：选择最先进入内存的页面换出（队列实现）。
- **特点**：简单；但与进程实际访问规律不符，性能差。
- **Belady 异常**：分配的物理块数增加，缺页率反而增加。**只有FIFO会产生Belady异常**。

> **Belady异常**：FIFO特有的现象。例：访问串 1,2,3,4,1,2,5,1,2,3,4,5，3个块缺页9次，4个块缺页10次。

#### (3) 最近最久未使用 LRU

- **策略**：选择最近最长时间未被访问的页面换出。
- **特点**：性能接近 OPT；但实现复杂，需寄存器或栈记录访问历史，硬件开销大。
- **实现**：每个页设一个计数器或访问时间戳；或用栈结构（访问时移到栈顶，淘汰栈底）。

#### (4) CLOCK 算法（时钟置换 / 二次机会）

- **简单CLOCK（NEC算法）**：所有页组成环形队列，每页有访问位。
  - 初始访问位=1（访问过）。
  - 指针扫描：访问位=1 则清0并跳过；访问位=0 则换出。
  - 最多两轮扫描即可找到换出页。
- **改进CLOCK**：考虑访问位 A 和修改位 M，优先换出 (A=0, M=0)。
  - 第一类 (0,0)：最佳，未访问未修改。
  - 第二类 (0,1)：未访问已修改（写回代价大）。
  - 第三类 (1,0)：已访问未修改。
  - 第四类 (1,1)：已访问已修改。
  - 扫描分两轮：第一轮找(0,0)，跳过时清A；第二轮找(0,0)（此时已清过A）。

**置换算法对比：**

| 算法 | 性能 | 实现 | Belady异常 |
|------|------|------|-----------|
| OPT | 最好 | 不可实现 | 无 |
| FIFO | 较差 | 简单 | **有** |
| LRU | 接近OPT | 复杂 | 无 |
| CLOCK | 介于FIFO和LRU | 较简单 | 无 |
| 改进CLOCK | 优于简单CLOCK | 较复杂 | 无 |

### 6. 页面分配策略与抖动

**驻留集**：分配给进程的物理块数（页框数）。

**分配策略：**
- **固定分配局部置换**：进程驻留集大小固定，缺页时从自己的页中置换。需预先确定大小，难。
- **可变分配全局置换**：驻留集可变，缺页时从全局空闲块分配或换出其他进程的页。简单但可能影响其他进程。
- **可变分配局部置换**：驻留集可变，缺页时只在自己的页中置换；频繁缺页则增加分配，否则减少。性能较好。

**页面调入策略：**
- **预调页策略**：预计将要访问的页提前调入（基于空间局部性）。主要用于进程首次启动。
- **请求调页策略**：缺页时才调入。运行时主要采用。

**从何处调入：**
- 系统区/对换区：对换区I/O速度快，常用。
- 文件区：未运行的程序从文件区调入。

**抖动（颠簸 Thrashing）：**
- **定义**：进程频繁发生缺页中断，大部分时间用于页面的换进换出，CPU 利用率急剧下降。
- **原因**：内存中进程过多，每个进程分到的物理块太少，缺页率极高。
- **预防**：适当控制多道程序度；采用工作集模型；增加物理内存。

**工作集**：在某段时间间隔内，进程实际访问的页面集合。工作集大小反映了进程的"局部性"需求，分配的物理块应不少于工作集大小。

### 7. 影响缺页中断的因素

- **页面大小**：大则缺页少但内部碎片大；小则缺页多。
- **分配的物理块数**：多则缺页少，但能容纳的进程少。
- **置换算法**：好的算法减少缺页。
- **程序局部性**：局部性好则缺页少。
- **程序结构**：如按列遍历数组（顺序访问）缺页少，按行遍历可能缺页多（C语言行优先存储）。

> **编程技巧**：二维数组访问，应按"行优先"顺序（C/C++行优先存储）以利用空间局部性，减少缺页。

### 8. 虚拟化技术简介（第4版新增）

**虚拟化**：在硬件和操作系统之间插入一层虚拟机监控器（VMM / Hypervisor），将一台物理计算机虚拟为多台逻辑计算机，每台虚拟机可运行独立的操作系统。

**虚拟化与 OS 虚拟性的区别：**
- OS 的虚拟性（如虚拟内存）：将物理资源抽象为逻辑资源，服务于**同一操作系统内的多个进程**。
- 虚拟化技术：对**整个硬件平台**的抽象，允许多个不同的操作系统同时运行。

**Hypervisor 的两种类型：**
| 类型 | 位置 | 特点 | 代表 |
|------|------|------|------|
| 第1类（裸机型/Bare-metal） | 直接运行在硬件上，VMM 即是一个精简OS | 性能高，直接用硬件 | VMware ESXi、Xen、KVM |
| 第2类（宿主型/Hosted） | 运行在宿主操作系统之上 | 安装方便，但有宿主OS开销 | VMware Workstation、VirtualBox |

**CPU 虚拟化：**
- 关键挑战：敏感指令（如修改页表基址寄存器、关中断）在用户态执行时不产生 trap，导致 Guest OS 无法被 VMM 截获。
- 解决方法：
  - **陷入再模拟（Trap-and-Emulate）**：硬件支持时，敏感指令自动 trap 到 VMM，VMM 模拟执行。要求敏感指令必须是特权指令（Intel/AMD 早期不满足）。
  - **二进制翻译（BT）**：VMM 在 Guest OS 执行前扫描其代码，将敏感指令替换为安全的等价操作。VMware 早期采用。
  - **硬件辅助虚拟化**：Intel VT-x / AMD-V 新增根模式（root mode）和非根模式（non-root mode），Guest OS 在非根模式运行，执行敏感指令时自动 VM-Exit 到 VMM。

**内存虚拟化：**
- Guest OS 管理"虚拟物理地址"→ VMM 管理"真实物理地址（机器地址）"。
- **影子页表（Shadow Page Table）**：VMM 维护 Guest 页表（虚拟→虚拟物理）和影子页表（虚拟→机器物理），Guest 对页表的修改需被 VMM 截获并同步更新影子页表。
- **扩展页表 EPT（Intel）/ 嵌套页表 NPT（AMD）**：硬件提供两级地址转换（虚拟→虚拟物理→机器物理），由硬件自动完成，无需影子页表，大幅提升性能。

**I/O 虚拟化：**
- **全虚拟化 I/O**：VMM 完全模拟常见 I/O 设备（如网卡、磁盘），Guest OS 使用标准驱动。兼容性好但效率低。
- **半虚拟化 I/O（virtio）**：Guest OS 安装专用驱动（virtio 驱动），与 VMM 通过共享内存高效通信。性能好但需修改 Guest OS。
- **设备直通（Pass-through / VT-d）**：将物理 I/O 设备直接分配给某个虚拟机独占使用，性能接近物理机。Intel VT-d / AMD IOMMU 支持。

**虚拟化的优势：**
- 服务器整合：一台物理机运行多个虚拟机，提高硬件利用率。
- 隔离性：各虚拟机独立运行，互不影响，安全性好。
- 灵活迁移：虚拟机可在不同物理机间**热迁移（Live Migration）**，不影响服务。
- 云计算基础：虚拟化是 IaaS（基础设施即服务）的核心技术。


---


## 七、易错点速记

- **内部碎片**：已分配未使用（固定分区、分页）；**外部碎片**：未分配太小无法用（动态分区、分段）。
- 分页**对程序员透明**，分段**对程序员可见**；分页一维地址，分段二维地址。
- 分页地址转换中**页号越界**才中断，页内偏移不会越界（页大小固定）；分段需检查段内偏移是否越界。
- **基本分页2次访存**，**两级页表3次访存**，**段页式3次访存**。
- **TLB命中只需1次访存**，未命中需2次（分页）。
- 请求分页中缺页中断属于**内中断**，在**指令执行期间**产生，处理后**重新执行该指令**。
- **Belady异常仅FIFO有**，增加物理块反而缺页增加。
- OPT最优但不可实现；LRU性能好但开销大；CLOCK是LRU的近似，开销小。
- **改进CLOCK优先换出(0,0)** 即未访问未修改的页。
- **抖动**因驻留集太小、多道程序度过高；解决方法是降低多道程序度或增加物理块。
- 多级页表**不能减少**页表总空间，但避免连续存放。
- 虚拟内存的"虚拟性"基于**局部性原理**（时间+空间局部性）。
- 顺序访问数组利用空间局部性，缺页少。
- 静态重定位装入后地址不再变；动态重定位运行时转换，靠重定位寄存器。
- **首次适应算法通常性能最好**；最佳适应产生大量小碎片。
- 分页大小固定由硬件决定，分段大小可变由程序决定。
- 页面调入运行时用**请求调页**，启动时可用**预调页**。
- **工作集**是局部性页面的集合，分配物理块应≥工作集大小，否则抖动。
- **虚拟化**在硬件与OS间插入 Hypervisor，第1类裸机型（ESXi/Xen/KVM），第2类宿主型（VMware/VirtualBox）。
- **CPU虚拟化**靠陷入再模拟/二进制翻译/硬件辅助（Intel VT-x）；**内存虚拟化**靠影子页表或 EPT/NPT 二级地址转换。
- **I/O 虚拟化**：全虚拟化（模拟设备）、半虚拟化（virtio专用驱动）、设备直通（VT-d/IOMMU）。
`,
    timestamp: "2026-08-06 18:30",
    embed: ``
};

export default [note1];
