export const note1 = {
    title: "计算机组成原理速通复习",
    contentmd: `
# 计算机组成原理 · 408考试速通复习

> 定位：概念+高频考点速记。重点掌握：补码表示、IEEE754、Cache映射、指令流水线、寻址方式、I/O控制方式。

---

## 一、计算机系统概述

**冯·诺依曼结构（存储程序）要点：**
1. 计算机由**运算器、控制器、存储器、输入设备、输出设备**五大部件组成。
2. **指令和数据以二进制形式存放在存储器中**（存储程序思想的核心）。
3. 指令和数据同等地位，按地址访问。
4. 指令由**操作码 + 地址码**组成。

**计算机性能指标（必背）：**
- **主频（时钟频率）**：CPU每秒时钟周期数，单位Hz。
- **CPU执行时间 = 指令条数 × CPI × 时钟周期**。
- **CPI**：每条指令平均时钟周期数；**IPC = 1/CPI**。
- **MIPS**：每秒执行百万条指令；**MFLOPS**：每秒百万次浮点运算。
- 吞吐率：单位时间内完成的指令/任务数；响应时间：任务完成总时间。
- 字长：CPU一次能处理的二进制位数。

---

## 二、数据的表示与运算

### 1. 进制与码制

| 码制 | 定义 | 特点 |
|------|------|------|
| 原码 | 符号位+绝对值 | 0有正负之分（+0/-0），加减复杂 |
| 补码 | 正数同原码，负数=取反+1 | 0唯一，加减统一用加法 |
| 反码 | 负数=原码除符号位取反 | 0有两种，过渡码 |
| 移码 | 补码符号位取反 | 用于浮点数阶码，方便比较大小 |

- n位补码表示范围：**[-2^(n-1), 2^(n-1)-1]**（比原码多一个最小负数）。
- 补码的性质：\`[x+y]补 = [x]补 + [y]补\`，减法变加法。

### 2. 溢出判断

- 同号相加得异号 → 溢出；异号相减得同号 → 溢出。
- 双符号位法（变形补码）：结果符号位 00 正常、01 正溢出、10 负溢出、11 正常。

### 3. 浮点数

- **IEEE754 单精度（float，32位）**：1位符号 + **8位阶码（偏置127）** + 23位尾数。
  - 真值 = (-1)^s × 1.M × 2^(E-127)
  - 双精度（double，64位）：1 + 11阶码（偏置1023）+ 52尾数。
- 浮点数比较大小：阶码大者大，阶码相同看尾数。
- 浮点加减运算步骤：**对阶（小阶向大阶看齐）→ 尾数加减 → 规格化 → 舍入 → 判溢出**。
- 规格化：尾数最高位为1（原码）；尾数绝对值在 [0.5,1) 之间。

---

## 三、存储系统

### 1. 层次结构

寄存器 → Cache → 主存（内存）→ 辅助存储器（外存）
速度由快到慢、容量由小到大、价格由高到低。**Cache-主存层次**解决速度问题，**主存-辅存层次**解决容量问题。

### 2. SRAM 与 DRAM 对比

| 项目 | SRAM | DRAM |
|------|------|------|
| 存储原理 | 双稳态触发器（6管） | 电容电荷（1管） |
| 是否需刷新 | 不需要 | 需要定期刷新 |
| 速度 | 快 | 慢 |
| 用途 | Cache | 主存 |

### 3. Cache 与主存的映射方式（重点）

| 映射方式 | 特点 | 冲突概率 | 实现复杂度 |
|---------|------|---------|-----------|
| 直接映射 | 主存块只能映射到唯一Cache行 | 高 | 最简单 |
| 全相联映射 | 主存块可映射到任意Cache行 | 最低 | 最复杂 |
| 组相联映射 | 分组建映射，组内全相联 | 中 | 折中 |

**替换算法：** LRU（近期最少使用）、FIFO（先进先出）、随机替换。

- **命中率 h** 越高越好；平均访问时间 **t = h×tc + (1-h)×tm**（tc为Cache访问时间，tm为主存访问时间）。
- 块大小 = 块内地址位数；Cache行数 = 组号位数；标记位区分不同主存块。

### 4. 主存扩展

- **位扩展**：增加存储字长（多片并联，片选信号相同）。
- **字扩展**：增加存储单元数（用高位地址译码产生片选信号）。
- 例：用 8K×8 的芯片组成 16K×16 的存储器，需 (16K/8K)×(16/8)=2×2=4 片。

### 5. 虚拟存储器

- 基于**局部性原理**，主存-辅存层次，采用**页式虚拟存储**（页面固定大小）。
- 快表 TLB：存放最近访问的页表项，加快地址变换。
- 虚拟地址 →（查页表）→ 物理地址；缺页时产生**缺页中断**（内中断）。

---

## 四、指令系统

**指令格式：** 操作码（做什么）+ 地址码（操作数在哪）。

**寻址方式（必背）：**
- **立即寻址**：指令中直接给出操作数本身，最快，无访存。
- **直接寻址**：指令给出操作数地址，访存1次。
- **间接寻址**：指令给出地址的地址，访存2次（可扩大寻址范围）。
- **寄存器寻址**：操作数在寄存器中，不访存。
- **寄存器间接寻址**：寄存器存操作数地址，访存1次。
- **变址寻址**：变址寄存器 + 形式地址，适合数组访问。
- **基址寻址**：基址寄存器 + 偏移，适合程序重定位。
- **相对寻址**：PC + 偏移量，用于转移指令。
- **堆栈寻址**：操作数在栈顶。

**CISC vs RISC：**

| 项目 | CISC | RISC |
|------|------|------|
| 指令 | 复杂、多、长度可变 | 精简、少、定长 |
| 寻址方式 | 多 | 少 |
| 执行 | 微程序控制 | 硬布线控制为主 |
| 寄存器 | 少 | 多，采用load/store结构 |
| 流水线 | 较难 | 易实现指令流水线 |

---

## 五、中央处理器 CPU

### 1. 组成

- **运算器**：ALU（算术逻辑单元）、累加器ACC、通用寄存器、状态寄存器（PSW）。
- **控制器**：程序计数器**PC**（存下一条指令地址）、指令寄存器**IR**（存当前指令）、指令译码器、时序系统、**控制单元CU**。

### 2. 指令周期

取指周期 → 间址周期（可选）→ 执行周期 → 中断周期（可选）。
- 一个指令周期包含若干机器周期（CPU周期），一个机器周期包含若干时钟周期。
- **中断周期**：保存断点（PC内容入栈），转入中断服务程序。

### 3. 指令流水线

- **吞吐率 TP = n / (k + n - 1) × Δt**（n条指令，k段流水线，每段时间Δt）。
- 理想加速比 S = 段数 k；实际受**冒险**影响。
- 流水线冒险：**结构冒险**（资源冲突）、**数据冒险**（RAW依赖）、**控制冒险**（转移指令）。
- 解决数据冒险：**转发（旁路）技术**、指令重排。

---

## 六、总线与 I/O 系统

### 1. 总线

- 按功能分类：**数据总线（双向）**、**地址总线（单向）**、**控制总线**。
- **总线仲裁**（决定谁占用总线）：
  - 集中式：链式查询（优先级固定）、计数器定时查询（公平可改）、独立请求（响应最快）。
- 总线定时：同步通信（固定时钟）、异步通信（握手）。
- 总线带宽 = 总线宽度 × 频率（单位字节/秒）。

### 2. I/O 控制方式（必背）

| 方式 | 特点 | CPU介入 |
|------|------|---------|
| 程序查询 | 轮询，CPU一直等待 | 最多，浪费CPU |
| 程序中断 | 设备就绪后发中断请求 | 较多，可处理突发事件 |
| **DMA** | 直接存储器存取，成块传送 | 仅在预处理/后处理介入，数据传送不需CPU |
| 通道 | 专门的I/O处理机 | 最少 |

**中断处理流程：** 关中断 → 保存断点（PC） → 识别中断源/找入口 → 保护现场 → 执行中断服务程序 → 恢复现场 → 开中断 → 返回断点。

> **易错点：** DMA传送数据不经过CPU，直接在内存与设备间传送；DMA方式可看作"软硬件结合"。
`,
    timestamp: "2026-08-06 09:00",
    embed: ``
};

export default [note1];
