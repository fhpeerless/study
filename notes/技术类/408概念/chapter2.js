export const note1 = {
    title: "数据结构（二）：树与二叉树",
    contentmd: `
# 数据结构（二）：树与二叉树

> 定位：408考研数据结构第二章。重点掌握：二叉树性质5条、四种遍历、线索二叉树、哈夫曼树、BST、AVL四种旋转、B树/B+树。

---

## 一、树的基本概念

### 1. 树的定义
**树（Tree）**：n（n≥0）个结点的有限集。n=0 为空树。
- 非空树特点：有且仅有一个根结点；其余结点可分为 m（m>0）个互不相交的有限集，每个集合本身又是一棵树（子树）。
- 树是一种**递归定义**的结构。

### 2. 基本术语
- **结点的度**：结点拥有的子树数（孩子个数）。
- **树的度**：树中结点的最大度数。
- **叶子/终端结点**：度为0的结点。
- **分支/非终端结点**：度>0的结点。
- **孩子/双亲/兄弟**：结点子树的根是孩子，该结点是双亲，同双亲的孩子互为兄弟。
- **祖先/子孙**：从根到某结点路径上所有结点是其祖先。
- **层次**：根为第1层，其孩子为第2层，依此类推（也有从0开始）。
- **深度/高度**：树中结点的最大层次。
- **有序树/无序树**：子树是否有序。
- **森林**：m（m≥0）棵互不相交的树的集合。

### 3. 树的性质
1. 树中结点数 = 所有结点的度数之和 + 1（即 n = Σ度数 + 1）。
2. 度为 m 的树第 i 层最多有 m^(i-1) 个结点。
3. 高度为 h 的 m 叉树最多有 (m^h - 1)/(m-1) 个结点。
4. 具有 n 个结点的 m 叉树最小高度为 ⌈log m (n(m-1)+1)⌉。

---

## 二、二叉树

### 1. 二叉树定义
**二叉树**：每个结点最多有两棵子树（左子树、右子树），子树有左右之分，次序不能颠倒。
- 二叉树与度为2的树区别：二叉树区分左右，且允许只有一个孩子；度为2的树至少有一个度为2的结点。

**特殊二叉树：**
- **满二叉树**：每层都满，深度为 k 有 2^k - 1 个结点；编号从1起自上而下从左到右。
- **完全二叉树**：深度为 k 的二叉树，前 k-1 层满，第 k 层结点从左到右连续。满二叉树是完全二叉树的特例。
- **二叉排序树 BST**：左<根<右。
- **平衡二叉树 AVL**：左右子树高度差≤1。

### 2. 二叉树性质（必背5条）
1. 非空二叉树第 i 层最多有 **2^(i-1)** 个结点（i≥1）。
2. 深度为 k 的二叉树最多有 **2^k - 1** 个结点（k≥1）。
3. 任意非空二叉树，若叶子数为 n0，度为2的结点数为 n2，则 **n0 = n2 + 1**。
   - 推导：n = n0 + n1 + n2；分支数 = n-1 = n1 + 2×n2 → n0 = n2 + 1。
4. 具有 n 个结点的完全二叉树深度为 **⌊log₂n⌋ + 1**。
5. 完全二叉树中编号 i 的结点：
   - 左孩子编号 2i（若 2i ≤ n）
   - 右孩子编号 2i+1（若 2i+1 ≤ n）
   - 双亲编号 ⌊i/2⌋（若 i > 1）
   - 若 2i > n，则 i 为叶子结点。

### 3. 二叉树存储结构
**顺序存储**：适合完全二叉树/满二叉树，按编号存数组。一般二叉树会造成空间浪费。

**链式存储（二叉链表）**：每个结点含 data + lchild + rchild。
- n 个结点的二叉链表有 **n+1 个空指针域**（n个结点共 2n 个指针，n-1 个非空指向孩子，剩 n+1 个空）。

**三叉链表**：增设 parent 指针，便于找双亲。

---

## 三、二叉树遍历

### 1. 四种遍历方式
- **先序遍历（DLR）**：根→左→右
- **中序遍历（LDR）**：左→根→右
- **后序遍历（LRD）**：左→右→根
- **层序遍历**：自上而下、自左而右，用**队列**实现。

**递归遍历时间复杂度**：O(n)，空间复杂度 O(深度)。

### 2. 遍历序列推导
- 先序序列第一个结点是根；后序序列最后一个结点是根。
- 先序的逆序相当于"根右左"。
- 表达式树：中序得中缀表达式，后序得后缀表达式，先序得前缀表达式。

### 3. 由遍历序列构造二叉树（重点）
- **中序 + 先序**：可唯一确定。
- **中序 + 后序**：可唯一确定。
- **中序 + 层序**：可唯一确定。
- **先序 + 后序**：**不能**唯一确定（无法区分左右子树）。

**方法**：先序/后序/层序定根，再到中序中分左右子树，递归处理。

---

## 四、线索二叉树

### 1. 定义
n 个结点的二叉链表有 n+1 个空指针域。利用这些空指针域指向结点在某种遍历序列下的**前驱/后继**，得到线索二叉树。

- **线索化**：将空指针改为线索的过程。
- 区分指针和线索：增设 ltag、rtag 标志位（0 表示孩子指针，1 表示线索）。

### 2. 线索化类型
- 先序线索二叉树
- **中序线索二叉树**（最常考）
- 后序线索二叉树

### 3. 中序线索化要点
- 左空指针指向中序前驱，右空指针指向中序后继。
- 中序线索树找前驱/后继：直接利用线索 O(1)。
- 中序线索树找第一个结点：一直向左走到底；找最后一个：一直向右走到底。
- 无左孩子的结点，lchild 指向前驱；无右孩子的结点，rchild 指向后继。

> 注意：先序线索化中找前驱、后序线索化中找后继，需知道双亲，普通二叉链表难以实现（需三叉链表）。

---

## 五、哈夫曼树与哈夫曼编码

### 1. 哈夫曼树定义
**带权路径长度 WPL = Σ(权值 × 路径长度)**，其中路径长度为根到该结点的边数。

**哈夫曼树（最优二叉树）**：给定 n 个叶子结点权值，构造 WPL 最小的二叉树。

### 2. 哈夫曼树构造算法
1. 将 n 个权值看作 n 棵只有根的树，构成森林 F。
2. 在 F 中选**两个根权值最小**的树合并为新树，新树根权值=两子树权值之和。
3. 将合并后的新树加入 F，删去原两棵树。
4. 重复 2-3 直到 F 中只剩一棵树，即为哈夫曼树。

### 3. 哈夫曼树性质
- **没有度为1的结点**（每次合并两个）。
- n 个叶子结点的哈夫曼树共有 **2n - 1** 个结点。
- 权值越大的叶子离根越近（路径越短）。
- 哈夫曼树不唯一（但 WPL 唯一且最小）。

### 4. 哈夫曼编码
- **前缀编码**：任一字符的编码都不是另一字符编码的前缀（保证译码唯一）。
- 构造：以字符出现频率为权值构造哈夫曼树，左分支标0、右分支标1（或反之），从根到叶子的路径为该字符编码。
- 用途：数据无损压缩。
- 译码：从根出发，遇0走左、遇1走右，到叶子输出字符后回到根。
- 哈夫曼编码是前缀编码，且总编码长度最短。

---

## 六、二叉排序树 BST

### 1. 定义与性质
**BST（二叉排序树/二叉查找树）**：空树或满足：
- 左子树所有结点值 < 根值
- 右子树所有结点值 > 根值
- 左右子树本身也是 BST

**重要性质**：中序遍历 BST 得到**递增有序序列**。

### 2. 基本操作
**查找**：从根开始比较，小于根走左子树，大于走右子树，O(深度)。

**插入**：查找确定位置后插入，新结点必为叶子。

**删除**：
- 删叶子：直接删。
- 删只有一个孩子的结点：用孩子替代。
- 删有两个孩子的结点：用其中序前驱（左子树最右下）或中序后继（右子树最左下）替换，再递归删除前驱/后继。

### 3. 性能
- 最好（平衡）：O(log n)。
- 最坏（退化为单链）：O(n)。
- BST 形态取决于插入顺序，同一组关键字可能构造不同形态 BST。

---

## 七、平衡二叉树 AVL

### 1. 定义
**平衡二叉树（AVL树）**：空树或满足：
- 左右子树都是平衡二叉树
- 左右子树高度差（**平衡因子 BF = 左子树高 - 右子树高**）的绝对值 ≤ 1

### 2. 失衡调整（4种旋转，重点）
插入新结点后可能破坏平衡，需旋转调整。设插入路径上**离插入点最近的不平衡结点为 A**：

| 失衡类型 | 描述 | 调整方法 |
|---------|------|---------|
| **LL** | 在 A 的左孩子的左子树上插入 | **右旋** A（A 的左孩子上提） |
| **RR** | 在 A 的右孩子的右子树上插入 | **左旋** A（A 的右孩子上提） |
| **LR** | 在 A 的左孩子的右子树上插入 | **先左旋 A 的左孩子，再右旋 A** |
| **RL** | 在 A 的右孩子的左子树上插入 | **先右旋 A 的右孩子，再左旋 A** |

**旋转要点：**
- LL/RR 单旋转；LR/RL 双旋转。
- 旋转后中序序列不变（保持 BST 性质）。
- 每次插入后只需调整离插入点最近的不平衡结点即可恢复平衡。
- LL 型：B=A->lchild，A->lchild=B->rchild，B->rchild=A，A=B。

### 3. AVL 性能
- 含 n 个结点的 AVL 树高度为 O(log n)。
- 查找/插入/删除：O(log n)。
- 插入可能需多次旋转但调整一次即可恢复平衡；删除可能需多次回溯调整。

### 4. AVL 含 n 个结点的最小高度
- 高度 h 的 AVL 树最少结点数 N(h) = N(h-1) + N(h-2) + 1，N(0)=0, N(1)=1（斐波那契形式）。

---

## 八、B树与B+树

### 1. B树（多路平衡查找树）
**m 阶 B 树**性质：
- 每个结点最多有 **m 棵子树**（m 个分支）。
- 每个结点最多有 **m-1 个关键字**。
- 根结点至少 2 棵子树（非叶时）。
- 非根非叶结点至少 **⌈m/2⌉** 棵子树。
- 所有叶子结点**在同一层**（绝对平衡）。
- 结点内关键字有序，且关键字间子树的所有值介于两关键字之间。

**查找**：在结点内顺序或折半查找，确定子树后向下。

**插入**：在最底层非叶结点插入；若关键字数 > m-1，从中间位置**分裂**，中间关键字上移到双亲，可能引起连锁分裂。

**删除**：
- 删非终端结点关键字：用直接前驱（左子树最大）或后继（右子树最小）替换，转化为删终端结点。
- 删终端结点关键字：若删除后关键字数 < ⌈m/2⌉-1，需向兄弟借或与兄弟合并（可能连锁）。

**B树高度**：含 n 个关键字的 m 阶 B 树，最大高度 ⌊log_⌈m/2⌉((n+1)/2)⌋+1。

### 2. B+树
**m 阶 B+树**性质：
- 每个分支结点最多 m 棵子树。
- 非根内部结点至少 ⌈m/2⌉ 棵子树，根至少 2 棵。
- **结点的子树数 = 关键字数**（B树是子树数=关键字数+1）。
- 所有**数据都存放在叶子结点**，内部结点仅存关键字作为索引。
- 叶子结点按关键字大小通过**链表**链接，支持顺序查找。

### 3. B+树 vs B树

| 项目 | B树 | B+树 |
|------|-----|------|
| 数据存储 | 非叶结点也存数据 | 数据只在叶 |
| 子树/关键字 | 子树=关键字+1 | 子树=关键字 |
| 叶子链表 | 无 | 有，相连 |
| 范围查询 | 难 | 容易（沿叶链表） |
| 查找 | 可在非叶终止 | 必到叶 |
| 适用 | 文件系统 | 数据库索引 |

### 4. B+树在数据库索引中的应用（第2版扩充）

**为什么数据库索引用 B+树：**
- **磁盘 I/O 优化**：B+树结点大小通常设计为一页（4KB/8KB/16KB），一个结点可存更多关键字（扇出大），树的高度更低，减少磁盘 I/O 次数。
- **范围查询高效**：叶子结点链表相连，BETWEEN、ORDER BY、GROUP BY 等操作可直接沿链表遍历。
- **稳定性好**：所有查找都要走到叶子结点，查找时间稳定。
- **数据与索引分离**：内部结点只存索引值不存数据，内部结点扇出更大。

**MySQL InnoDB 的 B+树索引：**
- 主键索引（聚簇索引）：叶子结点直接存储整行数据，按主键有序。
- 辅助索引（二级索引）：叶子结点存主键值 + 索引列值，回表查询：先在辅助索引找到主键 → 再到主键索引取完整行。
- 联合索引的最左前缀原则：索引 (a,b,c) 可加速 a=?, a=? AND b=?, a=? AND b=? AND c=? 查询；无法加速仅 b=? 或 c=? 的查询。

**B+树索引的性能估算**：假设页大小 16KB、关键字+指针占 14B，每页约 1100 个索引项。3 层 B+树可索引 1100³ ≈ 13 亿条记录，仅需 2~3 次 I/O。

> B+树已从"仅介绍数据结构"扩展到"数据结构 + 数据库索引应用"，是第2版的重要变化。408 考试逐渐关注存储系统的整体视角。

---

## 九、树与森林的转换

### 1. 树↔二叉树
- **树转二叉树**：左孩子右兄弟法。每个结点的第一个孩子作为左孩子，右侧兄弟作为右孩子。
- **二叉树转树**：逆过程。
- 转换后二叉树的根没有右子树（原树根无兄弟）。

### 2. 森林↔二叉树
- **森林转二叉树**：先将每棵树转为二叉树，再依次将后一棵作为前一棵的右子树连接。
- **二叉树转森林**：根的右链上的每个结点作为一棵树的根。

### 3. 树/森林的遍历
- **树**：先根遍历、后根遍历。
- **森林**：先序遍历、中序遍历。
- 对应关系：
  - 树的先根 = 二叉树先序
  - 树的后根 = 二叉树中序
  - 森林先序 = 二叉树先序
  - 森林中序 = 二叉树中序

---

## 十、红黑树（Red-Black Tree，第2版新增）

### 1. 定义与背景

**为什么需要红黑树**：AVL 树是严格平衡的（|BF|≤1），但插入/删除时需要频繁旋转调整，开销较大。红黑树通过放宽平衡条件，在保持 O(log n) 查找效率的同时，减少了旋转次数，成为实际工程中更广泛使用的平衡二叉查找树（如 C++ STL map/set、Java TreeMap/TreeSet、Linux 内核 CFS 调度器）。

**红黑树定义**：一棵满足以下 5 条**红黑性质**的二叉排序树：

1. 每个结点要么是**红色**，要么是**黑色**。
2. **根结点**必须是黑色。
3. 每个**叶子结点（NIL / 外部结点）**都是黑色。
4. **红色结点的两个子结点必须是黑色**（即没有两个连续的红色结点）。
5. 从**任一结点到其每个叶子结点的简单路径上，黑色结点数量相同**（该数量称为该结点的**黑高**）。

> 注意：教材中通常用 NULL 或 NIL 作为外部叶子结点（黑色），内部结点才是实际数据结点。

### 2. 红黑树的性质

**(1) 高度上界**
含 n 个内部结点的红黑树，高度 ≤ 2×log₂(n+1)。证明思路：任何红黑树，从根到叶的最长路径（红黑交替）不超过最短路径（全黑）的 2 倍。

**(2) 黑高 bh(x)**
结点 x 到叶子（不含 x）的黑色结点数。红黑树根的黑高至少为 h/2（h 为树高）。

**(3) 性能**
查找、插入、删除均为 **O(log n)**。虽然红黑树不如 AVL 树"平衡"（AVL 是高度平衡，红黑树是"近似平衡"），但旋转次数少，插入最多旋转 2 次，删除最多旋转 3 次。

### 3. 红黑树 vs AVL 树

| 项目 | AVL 树 | 红黑树 |
|------|--------|--------|
| 平衡条件 | |BF| ≤ 1（严格高度平衡） | 5 条红黑性质（近似平衡） |
| 高度 | ≤ 1.44 log₂(n+2) | ≤ 2 log₂(n+1) |
| 查找 | 略快（更平衡，高度更低） | 略慢（路径可能更长） |
| 插入旋转 | 最多 O(log n) 次（但插入一次不均衡定旋转不超过1次）| 最多 2 次旋转（+变色） |
| 删除旋转 | 最多 O(log n) 次 | 最多 3 次旋转（+变色） |
| 适用场景 | 查找远多于插入/删除 | 频繁插入/删除 |

> AVL 是严格的平衡树，红黑树是"弱平衡"但旋转更少。408 考试应能说出两者的定义差异和适用场景。

### 4. 红黑树的插入（重点理解）

新插入的结点默认染为**红色**（避免破坏性质5）。若插入后违反红黑性质，分情况修复：

设新插入结点为 **z**（红色），其父结点为 **p**，祖父结点为 **g**，叔叔结点为 **u**（p 的兄弟）。

**情况 1**：z 的叔叔 u 是红色
- 操作：p 和 u 染黑，g 染红，问题上移（z = g 继续检查）。
- 本质：将黑色从 g 下沉到两个子结点。

**情况 2**：u 是黑色，且 z 是 p 的右孩子（LR 型，p 是 g 的左孩子）
- 操作：先对 p 左旋，转化为情况 3。

**情况 3**：u 是黑色，且 z 是 p 的左孩子（LL 型，p 是 g 的左孩子）
- 操作：p 染黑，g 染红，对 g 右旋。
- 修复完成，不再上移。

若 p 是 g 的右孩子，上述情况对称处理（左右互换）。

> 插入修复的核心：变色（情况1）+ 旋转（情况2/3）。最多 2 次旋转即可恢复红黑性质。插入后根始终染黑。

### 5. 红黑树的删除（了解）

删除比插入更复杂，核心是处理"删除黑色结点导致黑高不平衡"问题，主要手段仍是变色 + 旋转。删除黑色叶子后，将其兄弟的额外黑色向上传递，通过借/合并调整。最多 3 次旋转修复。

### 6. 红黑树常考点

- 红黑树 5 条性质必须记准，特别是"根黑、无连续红、同黑高"。
- 新插入结点默认红色（尽量不破坏性质 5）。
- 插入最多 2 次旋转，删除最多 3 次旋转。
- 红黑树高度 ≤ 2×log₂(n+1)，查找/插入/删除均为 O(log n)。
- 与 AVL 对比：AVL 查找更快但插入删除旋转多；红黑树旋转少，工程更常用。

---

## 十一、并查集（Union-Find Set）

### 1. 定义与应用

**并查集**是一种树型数据结构，用于处理**不相交集合**的合并与查询，支持两种基本操作：
- **Find**：确定元素属于哪个集合（找到根结点）。
- **Union**：将两个集合合并为一个集合。

**408 主要应用场景**：
- Kruskal 算法中判断边的两端是否在同一连通分量（判环）。
- 判断无向图的连通分量。
- 等价类划分问题。

### 2. 存储表示

用**双亲表示法（树的双亲存储结构）**：数组 parent[]，parent[i] 表示结点 i 的双亲。树根的 parent[root] = -1（或自身 root）。初始时每个元素各成一个集合，parent[i] = -1。

### 3. 基本操作实现

**Find(x) — 查找根结点**（找到 x 所属集合的代表元）：
    int Find(int x) {
        while (parent[x] >= 0) x = parent[x];
        return x;
    }
时间 O(d)，d 为树的深度。

**Union(x, y) — 合并集合**（将 y 的根连到 x 的根下）：
    void Union(int x, int y) {
        int rx = Find(x), ry = Find(y);
        if (rx != ry) parent[ry] = rx;
    }

### 4. 优化（重点）

**按秩合并（Union by Rank）**：始终将较小树合并到较大树下，用 parent 数组的绝对值存储树的高度（秩）。
    void Union(int x, int y) {
        int rx = Find(x), ry = Find(y);
        if (rx == ry) return;
        if (parent[rx] < parent[ry])  // 负数比较：rx 的秩更大
            parent[ry] = rx;          // ry 连到 rx 下
        else if (parent[rx] > parent[ry])
            parent[rx] = ry;
        else {
            parent[ry] = rx;          // 高度相等时，选 rx 为根，高度+1
            parent[rx]--;
        }
    }
按秩合并保证树深不超过 O(log n)。

**路径压缩（Path Compression）**：Find 操作时，将查找路径上所有结点直接挂在根下。
    int Find(int x) {
        if (parent[x] < 0) return x;
        return parent[x] = Find(parent[x]);  // 递归 + 压缩路径
    }

**同时使用按秩合并 + 路径压缩**：每次操作均摊时间复杂度接近 **O(α(n))**，其中 α 是阿克曼函数的反函数（增长极慢，实际可视为常数 O(1)）。

### 5. 并查集常考点
- 双亲表示法用数组 parent[]，根结点的 parent 为 -1 或自身。
- Find 找根、Union 合并；优化有按秩合并（树深 O(log n)）、路径压缩（递归写法）。
- 路径压缩 + 按秩合并 → 近似 O(1) 均摊时间。
- Kruskal 算法中用来判环：若边的两端 Find 结果相同，则形成环，跳过此边。

---

## 十二、考点速记

- 二叉树性质5条：2^(i-1)、2^k-1、n0=n2+1、⌊log₂n⌋+1、左2i右2i+1父⌊i/2⌋。
- 二叉链表 n 个结点有 n+1 个空指针域。
- 中序+先序 或 中序+后序 可唯一确定二叉树，先序+后序不能。
- 哈夫曼树无度为1的结点，n 个叶子共 2n-1 个结点，WPL 最小。
- 哈夫曼编码是前缀编码，左0右1。
- AVL 四种旋转：LL右旋、RR左旋、LR先左后右、RL先右后左。
- m 阶 B 树：最多 m 棵子树 m-1 关键字，非根至少 ⌈m/2⌉ 子树，叶同层。
- B+树叶链表相连，内部结点只做索引，子树数=关键字数。
- 树转二叉树：左孩子右兄弟；树的先根=二叉树先序，后根=二叉树中序。
- **并查集**：双亲存储 parent[]，根为 -1。Find 找根，Union 合并。按秩合并 + 路径压缩 → 近似 O(1)。
`,
    timestamp: "2026-08-06 22:40",
    embed: ``
};

export default [note1];
