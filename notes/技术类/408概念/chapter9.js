export const note1 = {
    title: "操作系统（一）：进程与线程管理",
    contentmd: `
# 操作系统 · 进程与线程管理 · 408考试速通复习

> 定位：概念+高频考点速记。重点掌握：OS四大特征及并发与并行区别、进程三态转换、PCB作用、线程模型对比、进程通信方式对比。

---

## 一、操作系统基本概念

### 1. 操作系统的概念与功能

**操作系统（OS）**：是计算机系统中最基本、最重要的系统软件，是配置在计算机硬件上的第一层软件，是对硬件系统的首次扩充。

- **资源管理者**：管理处理机、存储器、文件、设备四大资源。
- **用户接口**：为用户和应用程序提供使用计算机的接口。

**OS 的四大功能：**
1. 处理机管理（进程管理）
2. 存储器管理（内存管理）
3. 设备管理（I/O 管理）
4. 文件管理

**OS 提供的服务：** 程序执行、I/O 操作、文件系统操作、通信、错误检测、资源分配、统计、保护与安全。

### 2. 操作系统的基本特征（重点）

| 特征 | 含义 | 关键词 |
|------|------|--------|
| **并发** | 多个事件在同一时间间隔内发生 | 多道程序同时运行 |
| **共享** | 系统资源可供多个并发执行的进程共同使用 | 互斥共享 + 同时访问 |
| **虚拟** | 通过某种技术把一个物理实体变为若干个逻辑上的对应物 | 时分复用 + 空分复用 |
| **异步** | 多道程序环境下，进程以不可预知的速度推进 | 走走停停 |

**并发 vs 并行（必考）：**
- **并行**：多个事件在同一时刻发生（需要多核/多处理机）。
- **并发**：多个事件在同一时间间隔内发生（单核通过分时实现）。
- 并行是并发的特例，没有并发就没有异步。

**共享的两种方式：**
- **互斥共享方式**：一段时间内只允许一个进程访问的资源（临界资源），如打印机。
- **同时访问方式**：一段时间内允许多个进程"同时"访问，如磁盘设备。

**虚拟技术：**
- **时分复用技术**：虚拟处理机（多道程序）、虚拟设备（SPOOLing）。
- **空分复用技术**：虚拟存储器（内存扩充）。

> **最基本特征：并发和共享**，二者互为存在条件。

### 3. 操作系统的发展与分类

- **批处理系统**（单道→多道）：提高资源利用率和系统吞吐量，但无交互能力。多道程序设计是 OS 联系硬件的关键。
- **分时系统**：时间片轮转、人机交互、多用户同时使用。特征：多路性、独立性、及时性、交互性。
- **实时系统**：及时响应外部事件，可靠性高。分为硬实时（绝对截止时间）和软实时（可偶尔错过）。
- **网络操作系统 / 分布式操作系统**：网络OS 松耦合，分布式OS 强耦合、全局统一。

### 4. 操作系统运行机制与体系结构

**双重模式（内核态 vs 用户态）：**
- **内核态（管态/核心态）**：执行特权指令，访问全部硬件和内存。
- **用户态（目态）**：只能执行非特权指令，不能直接访问硬件。
- **特权指令**：I/O 指令、置中断指令、存取用于内存保护的寄存器、程序状态字 PSW 等。
- **用户态 → 内核态**：通过**中断/异常/系统调用**（trap 指令/访管指令）。
- **内核态 → 用户态**：修改 PSW，执行特权指令设置。

> **系统调用是用户态进入内核态的唯一主动方式**；中断/异常是被动的。

**操作系统体系结构：**
- **大内核（单内核/宏内核）**：将 OS 主要功能模块都作为系统内核，运行在内核态。性能高但庞大，Linux 用。
- **微内核**：只把最基本功能（进程管理、IPC、内存管理基本部分）放入内核，其他作为服务运行在用户态。可扩展性、可靠性好但性能较低，Windows NT 用。

**中断与异常：**
- **中断（外中断/异步中断）**：来自 CPU 外部，如 I/O 中断、时钟中断，与当前指令无关。
- **异常（内中断/同步中断）**：来自 CPU 内部，如地址越界、除零、缺页、非法指令。
- 中断处理过程：关中断→保存断点→中断服务程序寻址→保存现场→中断事件处理→恢复现场→开中断→中断返回。

---

## 二、进程与线程

### 1. 进程的概念

**进程**：程序关于某个数据集合的一次执行过程，是系统进行资源分配和调度的基本单位。

- **进程 vs 程序**：程序是静态的（指令集合），进程是动态的（执行实例）；程序可长期保存，进程有生命周期；一个程序可对应多个进程。
- **进程的组成**：**PCB（进程控制块）+ 程序段 + 数据段**。PCB 是进程存在的唯一标志。
- **进程的特征**：动态性（最基本）、并发性、独立性、异步性、结构性。

### 2. 进程的状态与转换（必考）

**进程的三种基本状态：**
- **就绪状态（Ready）**：已获得除 CPU 外的所有资源，等待被调度。
- **运行状态（Running）**：正在 CPU 上执行。单核系统中只有一个进程处于运行态。
- **阻塞状态（Blocked/Wait）**：因等待某事件（如 I/O）而暂停执行。

**五种状态（含创建和终止）：**

| 状态 | 说明 |
|------|------|
| 创建（New） | 正在创建进程，分配 PCB，尚未进入就绪队列 |
| 就绪（Ready） | 等待 CPU |
| 运行（Running） | 占有 CPU |
| 阻塞（Blocked） | 等待事件（I/O、信号量等） |
| 终止（Terminated） | 执行完毕或被撤销，回收资源 |

**状态转换（重点记忆）：**
- 就绪 → 运行：被调度程序选中，获得 CPU。
- 运行 → 就绪：时间片用完，或被高优先级进程抢占。
- 运行 → 阻塞：请求资源或等待事件（**主动**行为，如 I/O 请求、P 操作）。
- 阻塞 → 就绪：等待的事件发生（**被动**响应，如 I/O 完成）。
- 创建 → 就绪：进程创建完成，进入就绪队列。
- 运行 → 终止：进程完成或异常终止。

> **注意：** 不存在"就绪 → 阻塞"和"阻塞 → 运行"的转换。阻塞进程不能直接获得 CPU。

### 3. 进程控制块 PCB

**PCB 的作用**：是进程存在的唯一标志，OS 通过 PCB 管理进程。

**PCB 的内容：**
1. **进程标识信息**：进程 ID（PID）、父进程 ID、用户 ID。
2. **处理机状态信息（现场信息）**：通用寄存器、指令计数器 PC、程序状态字 PSW、栈指针。
3. **进程调度信息**：进程状态、优先级、调度所需信息、等待事件。
4. **进程控制信息**：程序和数据地址、进程同步与通信信息、资源清单、链接指针。

**PCB 的组织方式：**
- **线性方式**：所有 PCB 放在一张线性表中。
- **链接方式**：按状态分别组织成就绪队列、阻塞队列等（最常用）。
- **索引方式**：建立各状态的索引表。

### 4. 进程控制

**原语（Primitive）**：不可中断的原子操作，运行在内核态。包括：创建、撤销、阻塞、唤醒、切换。

- **进程创建（create）**：
  - 申请空白 PCB → 为新进程分配资源（内存、文件）→ 初始化 PCB（标识、状态、CPU 信息）→ 将 PCB 插入就绪队列。
  - 引起创建的事件：用户登录、作业调度、提供服务、应用请求。
  - 子进程继承父进程的资源，父进程与子进程可并发执行。
- **进程终止（撤销）**：
  - 根据标识符检索 PCB → 终止执行 → 终止子孙进程 → 资源归还父进程或系统 → PCB 从队列移除。
  - 引起终止的事件：正常结束、异常结束（越界、非法指令）、外界干预（kill）。
- **进程阻塞（block）**：运行态 → 阻塞态。由进程**自己**调用，主动行为。
- **进程唤醒（wakeup）**：阻塞态 → 就绪态。由**其他**进程（发现等待事件发生者）调用。
- **进程切换（switch）**：保存当前进程现场 → 选取新进程 → 恢复新进程现场。

> **进程切换一定引起调度，但调度不一定引起进程切换**（如选中的仍是原进程）。

### 5. 线程的概念

**线程**：进程内的执行单元，是 CPU 调度和分派的基本单位。进程是资源分配的基本单位。

- 引入线程后，进程只作为资源分配单位，线程作为调度执行单位。
- 线程基本不拥有系统资源，只拥有少量必不可少的资源（线程ID、PC、寄存器、栈）。
- 同一进程的线程共享进程的地址空间和资源。
- **线程的优点**：创建/销毁开销小、切换快、通信方便（共享地址空间）、并发性更好。

**线程的属性：**
- 轻型实体，基本不拥有资源。
- 独立调度和分派的基本单位。
- 可并发执行。
- 共享进程资源。
- 也有就绪、运行、阻塞三态。

### 6. 线程的实现方式

| 类型 | 用户级线程 | 内核级线程 |
|------|-----------|-----------|
| 实现者 | 用户库（线程库） | 操作系统内核 |
| 内核是否感知 | 不可见 | 可见 |
| 调度者 | 应用程序自己 | 内核 |
| 优缺点 | 切换快、无内核开销；但一个线程阻塞整个进程阻塞，不能多核并行 | 内核可调度，能利用多核；但切换需切换到内核态，开销大 |
| 系统调用 | 整个进程阻塞 | 仅该线程阻塞 |

**多线程模型（用户级与内核级线程的组合）：**
- **多对一模型**：多个用户线程映射到一个内核线程。一个线程阻塞则整个进程阻塞，不能多核并行。
- **一对一模型**：每个用户线程对应一个内核线程。并发性好，但创建开销大。Linux、Windows 采用。
- **多对多模型**：n 个用户线程映射到 m 个内核线程（n≥m）。兼顾两者优点。

### 7. 进程通信（IPC）

**进程通信**：进程间信息交换。低级通信：信号量（P/V 操作，仅传信号）；高级通信：大批量数据传输。

**三种高级通信方式：**

| 方式 | 原理 | 特点 |
|------|------|------|
| **共享存储** | 两个进程共享一段内存区域进行数据交换 | 速度最快，需互斥与同步保护；分为基于存储区（高级）和基于数据结构（低级） |
| **消息传递** | 以格式化的消息为单位进行数据交换，由系统同步原语实现 | 适用于分布式系统；分为直接通信（send/receive 显式指明对方）和间接通信（通过信箱 mailbox） |
| **管道通信** | 连接一个写进程和一个读进程的共享文件（pipe 文件） | 半双工；读写需互斥、同步；写满或读空时阻塞 |

**管道详解：**
- 管道是**半双工**的，双向通信需要两个管道。
- 管道本质是内核中的一块缓冲区（固定大小，如 4KB）。
- 写满时写进程阻塞，读空时读进程阻塞。
- 没有写进程时读返回 EOF，没有读进程时写进程收到 SIGPIPE 信号。
- **命名管道（FIFO）** 可用于无亲缘关系进程间的通信。

**消息格式：** 消息头（发送者、接收者、消息类型、消息长度）+ 消息正文。

**消息传递的两种实现：**
- **直接通信**：send(P, msg)、receive(Q, msg)，需显式指定对方。
- **间接通信（信箱）**：send(A, msg)、receive(A, msg)，通过信箱 A 中转，解耦发送者与接收者。

**同步通信 vs 异步通信：**
- **阻塞发送（同步）**：发送方等待接收方收到。
- **非阻塞发送（异步）**：发送方发出即可继续。
- **阻塞接收**：接收方等待消息到达。
- **非阻塞接收**：接收方无消息则返回空。

### 8. 硬件方法实现进程互斥

当临界区很短时，可用硬件指令实现互斥，无需信号量。

**关中断（屏蔽中断）：**
- 进入临界区前**关中断**（CLI），出临界区后**开中断**（STI）。
- CPU 在关中断期间不会响应外部中断和进程调度，保证临界区原子执行。
- **缺点**：仅适用于单核 CPU；可能影响系统响应（关中断太久会丢失中断）；不适用于用户程序。

**Test-and-Set（TS 指令）：**
- 硬件提供的原子指令，测试并设置一个布尔变量。
- 实现原理：boolean TestAndSet(boolean *lock) { boolean old = *lock; *lock = true; return old; }（原子执行）。
- 互斥锁：while(TestAndSet(&lock)) ; // 忙等（自旋锁）
  - 临界区执行结束后：lock = false;
- **自旋锁（Spin Lock）**：进程在获取锁之前"空转"，不阻塞不被调度，适用于**临界区极短、多核系统**。

**Swap / Exchange 指令：**
- 原子交换两个字的内容：void Swap(boolean *a, boolean *b) { boolean t = *a; *a = *b; *b = t; }
- 互斥锁实现：key = true; do { Swap(&lock, &key); } while(key);
- 出临界区：lock = false;

**Test-and-Set / Swap 特点：**
- 实现简单，适用于多核。
- **违反"让权等待"原则**：未进入临界区的进程处于忙等状态（自旋），消耗 CPU 资源。
- 适用于**临界区极短**或有**多核可用**的场景。

### 9. 管程（Monitor，重点）

**管程**：一种高级同步机制，将共享资源和对该资源的操作**封装在一个模块**内，同一时间**最多一个进程**在管程内执行。管程内用**条件变量（Condition Variable）**实现同步。

**管程四大组成部分：**
1. **共享变量**：管程管理的共享数据。
2. **一组过程（函数）**：对共享变量的操作，在管程外部调用。
3. **初始化代码**：对共享变量赋初值。
4. **条件变量**：用于进程间的同步等待与唤醒。

**条件变量（Condition Variable）：**
- 每个条件变量有一个等待队列。
- **wait(c)**：调用进程在条件 c 上等待（阻塞），释放管程的互斥权（让其他进程进入管程），插入 c 的等待队列。
- **signal(c)**：唤醒在条件 c 上等待的一个进程。被唤醒进程需重新获得管程互斥权才能继续执行。
- **区别 P/V 操作**：P/V 操作没有条件语义，只是简单的资源计数；条件变量可实现更灵活的同步逻辑。

**管程 vs 信号量对比：**

| 项目 | 信号量 | 管程 |
|------|--------|------|
| 实现者 | 使用者 | 编程语言/编译器 |
| 同步机制 | P/V 操作 | wait/signal |
| 易出错性 | 易出错（P/V 配对、顺序） | 编译器检查，更安全 |
| 封装性 | 无封装，散落代码各处 | 封装良好 |
| 特点 | 底层、灵活 | 高级、结构化 |

### 10. Linux 进程管理案例（第4版新增）

Linux 内核中，进程和线程统一用 task_struct 结构体描述，task_struct 相当于 Linux 的 PCB。

**task_struct 主要字段：**
- pid（进程ID）、ppid（父进程ID）
- state：进程状态（TASK_RUNNING 运行/就绪、TASK_INTERRUPTIBLE 可中断睡眠、TASK_UNINTERRUPTIBLE 不可中断睡眠、TASK_STOPPED 暂停、TASK_ZOMBIE 僵死）
- mm_struct *mm：进程内存描述符（地址空间指针）
- fs_struct *fs：文件系统信息（当前目录等）
- files_struct *files：打开的文件描述符表
- thread_struct thread：CPU 寄存器上下文信息
- 调度相关信息：优先级（static_prio、rt_priority）、时间片（time_slice）、调度策略等

**Linux 进程创建 —— fork() 系列：**

fork() 系统调用创建新进程，子进程完全复制父进程的地址空间。现代 Linux 采用**写时拷贝（Copy-On-Write，COW）**技术优化 fork()：
- fork() 后父子进程共享同一物理页，页表项标记为只读。
- 任一方尝试写入共享页时，触发缺页保护异常，内核为该页建立独立副本。
- COW 避免了 fork 时立即复制全部地址空间的开销。

exec() 系列函数：通常在 fork() 后由子进程调用，加载新程序替换子进程的地址空间。

**vfork() vs fork()：**
- vfork() 不复制地址空间，父进程阻塞直到子进程调用 exec() 或 exit()。
- 适用于"立刻执行 exec"的场景，比 fork 更轻量。

**Linux 线程实现：**

Linux 不严格区分进程和线程，两者统一用 task_struct 描述。区别在于创建时的标志：
- 创建进程：fork() → 子进程获得**独立**的地址空间（新 mm_struct）。
- 创建线程：clone(CLONE_VM | ...) → 线程**共享**父进程的地址空间（复用同一 mm_struct）。

Linux 线程库演进：
| 实现 | 时期 | 特点 |
|------|------|------|
| LinuxThreads | 早期 | 每个线程对应一个独立进程，不完全符合 POSIX 标准 |
| NPTL（Native POSIX Thread Library） | 当前标准 | 一对一模型，用户线程与内核线程一一对应，完全符合 POSIX |

**NPTL 核心特征：**
- 每个用户线程对应一个内核可调度实体，线程切换由内核调度器完成。
- 线程的创建、同步、销毁等通过系统调用在内核态完成。
- clone() 时设置 CLONE_VM（共享地址空间）、CLONE_FILES（共享文件表）、CLONE_SIGHAND（共享信号处理）等标志。
- 线程间通信极为方便，直接通过共享变量，无需 IPC。
- 可利用多核/多处理器真正并行执行。

**Linux 运行队列与等待队列：**
- **运行队列（runqueue）**：每个 CPU 一个，存放所有 TASK_RUNNING 状态的进程/线程，等待被调度。
- **等待队列（wait queue）**：阻塞状态的进程/线程挂在对应事件的等待队列上，事件发生后被唤醒移入运行队列。

---

## 三、CPU 模式与系统调用（补充）

**系统调用（System Call）：**
- 用户程序使用 OS 服务的接口，运行于内核态。
- 过程：用户程序执行访管指令（trap/int 指令）→ 产生内中断 → 切换到内核态 → 内核执行系统调用服务程序 → 返回用户态。
- 系统调用分类：设备管理、文件管理、进程控制、进程通信、存储管理。

**用户态 vs 内核态切换时机：**
- 用户态 → 内核态：**系统调用、中断、异常**。
- 内核态 → 用户态：执行完中断/系统调用返回时，通过修改 PSW。

> **重要对比**：系统调用属于内中断（陷阱/trap）；外部设备 I/O 完成属于外中断。

---

## 四、易错点速记

- **进程是资源分配的基本单位，线程是 CPU 调度的基本单位**（引入线程后）。
- 进程有三种基本状态，**就绪和阻塞都是等待，但等待的对象不同**：就绪等 CPU，阻塞等事件。
- **阻塞是进程自己主动的**，唤醒是被其他进程动的。
- **不存在就绪→阻塞、阻塞→运行的转换**。
- PCB 是进程存在的**唯一标志**；线程也有 TCB。
- 管道是**半双工**的，且读写需同步互斥。
- 共享存储是**最快**的通信方式，因为不需要内核中转。
- **并发是最基本特征之一**（与共享并列），没有并发就没有异步。
- 用户级线程对内核**透明**，内核级线程是内核可感知的。
- **系统调用**是用户主动进入内核态的唯一方式；**中断/异常**是被动的。
- 多道程序设计的目的是**提高资源利用率和吞吐量**，分时系统目的是**人机交互**。
- 时间片用完导致的运行→就绪是**被动**的；请求 I/O 导致的运行→阻塞是**主动**的。
- **TS/Swap 指令**属于自旋锁，违反"让权等待"，适用于临界区极短和多核场景。
- **管程**封装共享变量与操作，条件变量 wait/signal 替代 P/V；由编译器保证互斥，更安全。
- Linux 用 task_struct 统一表示进程和线程；fork() 采用**写时拷贝 COW** 优化。
- Linux 线程实现 NPTL 采用**一对一模型**，clone(CLONE_VM) 创建线程共享地址空间。
- Linux 每个 CPU 一个 runqueue，阻塞进程挂在 wait queue 上。`,
    timestamp: "2026-08-06 22:10",
    embed: ``
};

export default [note1];
