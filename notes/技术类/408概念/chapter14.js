export const note1 = {
    title: "计算机网络（二）：数据链路层",
    contentmd: `
# 计算机网络 · 数据链路层

> 408 高频考点：CRC 计算、滑动窗口协议（GBN/SR 窗口计算）、CSMA/CD 最小帧长与争用期、以太网帧格式、交换机自学习、VLAN、PPP 协议。

---

## 一、数据链路层功能

数据链路层在物理层提供的比特流服务基础上，向网络层提供**点到点**（point-to-point）的相邻节点间可靠数据传输。三个基本问题：

### 1. 封装成帧（Framing）

- 将网络层交下来的 IP 数据报**前后添加首部和尾部**构成帧。
- 帧首部包含控制信息（如 MAC 地址），尾部通常包含**帧检验序列 FCS**。
- 帧长 = 数据 + 首部 + 尾部，必须规定**最大传送单元 MTU**（以太网 1500 字节）。

### 2. 透明传输

- 不论所传数据是什么样的比特组合，都应当能在链路上传送。
- 当数据中恰好出现与帧定界符相同的比特组合时，需要**转义处理**。

### 3. 差错检测

- 物理层传输会出现**比特差错**：1 变 0、0 变 1。
- **误码率 BER**：传输错误的比特占所传输比特总数的比率。
- 链路层广泛使用 **CRC 循环冗余校验** 实现差错检测（仅检错，不纠错）。

---

## 二、帧定界（封装成帧的方法）

### 1. 字符计数法

- 帧首部使用一个计数字段标明帧的长度。
- 缺点：计数字段出错则后续全部错位。

### 2. 字符填充法（面向字符）

- 使用特殊字符作为帧定界符，如 **SOH（帧首）+ EOT（帧尾）**。
- 当数据中出现 SOH/EOT 时，前面加**转义字符 ESC**；若数据中本身有 ESC，也加 ESC 转义。
- 缺点：只能传输文本，开销大。

### 3. 比特填充法（面向比特，PPP/HDLC 使用）

- 帧定界符为 **01111110**（即 0x7E）。
- 发送端：连续 5 个 1 后**插入 1 个 0**（即 011111 → 0111110）。
- 接收端：连续 5 个 1 后若为 0，**删除该 0**。
- 优点：实现简单，可传输任意比特流，透明性好。

### 4. 违规编码法

- 利用物理层不会出现的信号组合作为定界符。
- 如曼彻斯特编码中，高-高 和 低-低 是违规的，可用作帧定界，无需填充。
- IEEE 802.3 以太网采用此法。

---

## 三、差错控制

### 1. 检错编码

**奇偶校验**：1 位校验位，仅能检出奇数个错误，检错率 50%。

**CRC 循环冗余校验（必考计算）：**

- **原理**：将数据看作一个长二进制数，模 2 除以生成多项式 G(x)，得到余数作为 FCS。
- **步骤**：
  1. 发送方在数据后面**追加 k 个 0**（k = 生成多项式位数 - 1）。
  2. 用追加后的数据**模 2 除**生成多项式，得到 k 位余数 R。
  3. 余数 R 替换追加的 0，作为 FCS 添加到数据后发出。
  4. 接收方用收到的全部数据模 2 除 G(x)，余数为 0 表示无差错。
- **模 2 除法**：异或运算，不借位减法。
- **只能检错不能纠错**。

**例**：数据 M = 101001，生成多项式 G = 1101（3 次多项式）：
1. 追加 3 个 0：101001000
2. 模 2 除：101001000 ÷ 1101
   - 商从高到低：每 4 位一组做异或
   - 101001000 ÷ 1101 余数 = 001
3. 发送帧 = 101001001（数据 + FCS）
4. 接收方 101001001 ÷ 1101 余数为 0 → 无差错

### 2. 纠错编码——海明码

- **海明距离**：两个等长字符串对应位不同的位数。
- 检测 d 位错需海明距离 d+1；纠正 d 位错需海明距离 2d+1。
- **海明码**：能纠正 1 位错误。
- 关系式：**2^r ≥ m + r + 1**，其中 m 为信息位，r 为校验位。
- 校验位放在 2^i 位置（1, 2, 4, 8, ...），其他位置放信息位。

---

## 四、流量控制与可靠传输机制

### 1. 停止-等待协议（Stop-and-Wait, SW）

- **机制**：发送方每发一帧后停下来等待确认 ACK，收到 ACK 才发下一帧。
- **重传**：超时未收到 ACK 则重传。
- **窗口大小**：发送窗口 = 1，接收窗口 = 1。
- **信道利用率**：U = tf / (tf + RTT + ta)，其中 tf 为发送时延，ta 为确认帧发送时延。
- **优点**：简单；**缺点**：信道利用率低。

### 2. 后退 N 帧协议（GBN, Go-Back-N）

- **机制**：发送方可连续发送多个帧，无需等待单个 ACK。
- **累计确认**：接收方对按序正确到达的最高序号帧进行确认，ACK n 表示 n 之前的所有帧均正确收到。
- **重传**：超时后从超时帧开始**全部重传**（包括之后已发送但未确认的帧）。
- **窗口大小**：发送窗口 W ≤ 2^n - 1，接收窗口 = 1，其中 n 为序号位数。
- **特点**：接收方**只接收按序的帧**，乱序帧丢弃。
- **若 W = 2^n**：接收方无法区分是旧帧还是新帧，发生混淆。

### 3. 选择重传协议（SR, Selective Repeat）

- **机制**：只重传出错或丢失的帧，正确到达的帧缓存。
- **接收窗口 > 1**：可以接收乱序帧，缓存在接收窗口内。
- **窗口大小**：发送窗口 = 接收窗口 ≤ 2^(n-1)，即 **W + R ≤ 2^n**。
- 当 W = R = 2^(n-1) 时效率最大。
- **确认**：对每个正确接收的帧**单独确认**。
- **特点**：效率高，但接收方需要足够缓存。

### 4. 三种协议对比

| 协议 | 发送窗口 | 接收窗口 | 确认方式 | 重传范围 | 序号位数限制 |
|------|---------|---------|---------|---------|------------|
| 停止-等待 SW | 1 | 1 | 单独 | 1 帧 | 1 位 |
| 后退 N 帧 GBN | ≤ 2^n - 1 | 1 | 累计 | 全部 | n 位 |
| 选择重传 SR | ≤ 2^(n-1) | ≤ 2^(n-1) | 单独 | 仅出错帧 | n 位 |

### 5. 滑动窗口协议

- 上述三种协议都是滑动窗口协议的特例。
- **发送窗口**：允许连续发送的帧序号范围，随 ACK 到来向前滑动。
- **接收窗口**：允许接收的帧序号范围，按序确认后向前滑动。
- **核心思想**：利用窗口机制实现流量控制和可靠传输。

---

## 五、介质访问控制 MAC

### 1. 信道划分介质访问控制（静态）

- **FDM、TDM、STDM、WDM、CDMA**（详见物理层信道复用）。
- 各用户独占一个子信道，无冲突，适合**广播式信道**。

### 2. 随机访问介质访问控制（动态）

#### 2.1 ALOHA 协议

- **纯 ALOHA**：想发就发，冲突则随机等待后重传。**最脆弱**，吞吐量 S = G × e^(-2G)，最大 0.184。
- **时隙 ALOHA（S-ALOHA）**：仅在时隙开始时才能发送，冲突则下个时隙按概率重发。吞吐量 S = G × e^(-G)，最大 **0.368**，是纯 ALOHA 的两倍。

#### 2.2 CSMA 载波监听多路访问

- **先听后发**：发送前先监听信道，空闲则发送，忙则等待。
- 三种坚持/非坚持策略：

| 类型 | 监听策略 | 特点 |
|------|---------|------|
| 1-坚持 | 空闲即发；忙则持续监听 | 冲突概率高 |
| 非坚持 | 空闲即发；忙则随机等待后重新监听 | 信道利用率低，但冲突少 |
| p-坚持 | 空闲以概率 p 发送，1-p 延迟；忙则持续监听 | 适用于时隙信道 |

#### 2.3 CSMA/CD（以太网使用，必考）

- **载波监听多路访问/碰撞检测**。
- 工作流程：**先听后发，边发边听，冲突停发，随机重发**。
- **碰撞检测**：发送时同时检测信号电压，电压超过阈值判定冲突。
- **争用期（碰撞窗口）**= 2 × 端到端传播时延 τ，即 **2τ**。这是检测冲突所需的最长时间。
- **最小帧长** = 争用期 × 带宽 = **2τ × 带宽**。以太网规定最小帧长 **64 字节**。
  - 例：10 Mbps 以太网，最远距离 2500 m，5 个 500 m 网段，τ ≈ 25.6 μs，2τ ≈ 51.2 μs，64 字节 = 512 bit × 0.1 μs = 51.2 μs。
- **截断二进制指数退避算法**：
  - 发生冲突后，从集合 {0, 1, ..., 2^k - 1} 中随机选取一个数 r，等待 r × 2τ 后重发。
  - k = min(重传次数, 10)，重传次数超过 16 次则丢弃帧并向上层报告。
- **CSMA/CD 适用于有线网络**（总线型、半双工以太网），不适用于无线。
- **CSMA/CD 是半双工通信**，无 MAC 层确认。

#### 2.4 CSMA/CA（无线局域网 Wi-Fi 使用）

- **载波监听多路访问/碰撞避免**。
- **为什么要避免碰撞**：无线环境中**无法边发边听**（信号衰减大，隐藏站、暴露站问题）。
- 机制：
  - **物理载波监听 + 虚拟载波监听**。
  - 发送数据前先发 **RTS（请求发送）**，接收方回 **CTS（允许发送）**，避免隐藏站问题。
  - 数据帧发送后，接收方返回 **ACK** 确认。
- **CSMA/CA 提供链路层确认**，而 CSMA/CD 没有。
- **IFS 间隔**：SIFS（最短，用于 ACK、CTS）、PIFS、DIFS（最长，用于数据帧）。

### 3. 轮询访问介质访问控制

- **令牌传递协议**：节点在逻辑环上传递令牌，只有持有令牌者才能发送。
- 适合令牌环网、负载重时无冲突、效率高。
- 缺点：令牌维护复杂，单点故障。

---

## 六、局域网

### 1. 局域网基本概念

- **特点**：覆盖地理范围小、传输速率高、误码率低、共享传输介质。
- **拓扑结构**：星型、总线型、环形。
- **IEEE 802 标准**：
  - 802.3 = 以太网 CSMA/CD
  - 802.5 = 令牌环网
  - 802.11 = 无线局域网 Wi-Fi

### 2. 以太网帧格式（DIX Ethernet V2，必背）

| 前导码 | 目的地址 | 源地址 | 类型 | 数据 | FCS |
|--------|---------|-------|------|------|-----|
| 8B | 6B | 6B | 2B | 46~1500B | 4B |

- **目的/源地址**：MAC 地址，**6 字节**（48 位）。
- **类型**：上层协议（0x0800 = IP，0x0806 = ARP）。
- **数据字段**：最小 46 字节，最大 1500 字节（MTU）。若数据不足 46 字节需填充。
- **最小帧长**：64 字节（6+6+2+46+4 = 64）。
- **最大帧长**：1518 字节。
- **IEEE 802.3 与以太网 V2 区别**：802.3 长度字段表示数据长度，V2 类型字段表示上层协议。

### 3. MAC 地址

- **48 位（6 字节）**，前 24 位为厂商代码（OUI），后 24 位为序列号。
- **MAC 地址是硬件地址**，固化在网卡 ROM 中，理论上全球唯一。
- **第一字节最低位**：0 = 单播，1 = 组播；次低位：0 = 全局唯一，1 = 本地分配。
- 广播地址 FF-FF-FF-FF-FF-FF。

### 4. 交换机工作原理（必考）

- **二层设备**，基于 **MAC 地址**转发帧。
- **自学习功能**：
  - 收到帧时，记录**源 MAC 地址 → 入端口**映射到转发表（MAC 地址表）。
  - 表项包含：MAC 地址、端口、时间戳（用于老化）。
  - 老化时间默认 300 秒。
- **转发规则**：
  - 查转发表中目的 MAC 地址：
    - 找到且端口与入端口**不同**：从对应端口转发。
    - 找到但端口与入端口**相同**：丢弃（过滤）。
    - **未找到**：**泛洪**（除入端口外所有端口转发）。
  - 收到广播帧：**泛洪**。
- **特点**：
  - **隔离冲突域**：每个端口是一个独立冲突域。
  - **不能隔离广播域**：广播帧会泛洪到整个交换网络。
  - **全双工**，每端口独占带宽。
- **与集线器对比**：交换机按 MAC 转发，独占带宽；集线器广播，共享带宽。

### 5. VLAN 虚拟局域网

- **目的**：将一个物理局域网划分为多个逻辑上的虚拟局域网，**隔离广播域**。
- **实现**：基于**交换机端口**（最常用）、MAC 地址、IP 地址、协议类型。
- **802.1Q 标签**：在以太网帧源地址后插入 4 字节 VLAN 标签。
  - 包含 2 字节 TPI（0x8100）+ 2 字节 TCI（含 12 位 VID，可表示 4094 个 VLAN）。
  - VID 0 和 4095 保留。
- **Trunk 链路**：交换机之间传输多个 VLAN 流量，带 802.1Q 标签。
- **Access 端口**：连接主机，仅属于一个 VLAN，发送时去掉标签。
- **优势**：限制广播域、提高安全性、便于网络管理。
- 不同 VLAN 间通信需通过**路由器（路由器-on-a-stick）或三层交换机**。

---

## 七、广域网协议

### 1. PPP 点对点协议（必考）

- **作用**：用户计算机与 ISP 之间建立连接，支持**全双工**通信。
- **特点**：
  - **面向字节**，帧格式与 HDLC 相似但更简单。
  - **支持多种网络层协议**（IP、IPX、AppleTalk）。
  - **差错检测**（FCS，但不纠错，不重传）。
  - **无连接**、**不可靠**传输。
  - 支持**身份认证**：PAP（密码认证）、CHAP（挑战握手认证）。
- **PPP 帧格式**（与 HDLC 类似）：

| 标志 | 地址 | 控制 | 协议 | 信息 | FCS | 标志 |
|------|------|------|------|------|-----|------|
| 7E | FF | 03 | 2B | ≤1500B | 2B | 7E |

- 标志字段 7E（01111110）为帧定界符，**比特填充**实现透明传输。
- 协议字段：0x0021 = IP 数据报，0xC021 = LCP 数据，0x8021 = NCP 数据。
- **PPP 不使用序号和确认机制**（不提供可靠传输），但 HDLC 提供。

### 2. PPP 三个组成部分

1. **HDLC-like 帧格式**：将 IP 数据报封装到串行链路。
2. **LCP（链路控制协议）**：建立、配置、测试和管理数据链路。
3. **NCP（网络控制协议）**：为网络层协议分配和配置 IP 地址等。

### 3. PPP 链路状态

- **链路不可用 Dead** → **链路建立 Establish**（LCP 协商） → **鉴别 Authenticate**（PAP/CHAP） → **网络层协议 Network**（NCP 分配 IP） → **链路打开 Open** → **链路终止 Terminate** → Dead。

### 4. HDLC 协议

- 面向比特，**只支持同步传输**。
- **信息帧 I、监督帧 S、无编号帧 U** 三类。
- 与 PPP 相比，HDLC **提供可靠传输**（序号+确认），但 PPP 更简单灵活，**适合拨号/异步链路**。

---

## 八、典型考点速记

- **CRC 计算**：模 2 除法，余数 = FCS，发送方拼接数据 + FCS。
- **GBN 窗口**：W ≤ 2^n - 1；SR 窗口：W = R ≤ 2^(n-1)；停止-等待：W = 1。
- **CSMA/CD**：争用期 2τ，最小帧长 = 2τ × 带宽（以太网 64 字节）；退避算法 k = min(n, 10)，最多 16 次。
- **CSMA/CD vs CSMA/CA**：CD 有线边发边听，CA 无线避免冲突 + 链路层 ACK。
- **以太网帧**：目的/源 MAC 各 6B，类型 2B，数据 46~1500B，FCS 4B，最小 64B 最大 1518B。
- **交换机**：自学习源 MAC，按目的 MAC 转发，未找到则泛洪；隔离冲突域不隔离广播域。
- **VLAN**：802.1Q 4 字节标签，VID 12 位共 4094 个；跨交换机用 Trunk；隔离广播域。
- **PPP**：面向字节、不可靠、无序号、有 LCP/NCP、支持 PAP/CHAP 认证；HDLC 面向比特、可靠、有序号。
- **ALOHA**：纯 ALOHA 最大吞吐 0.184，时隙 ALOHA 最大 0.368（两倍）。
`,
    timestamp: "2026-08-06 18:30",
    embed: ``
};

export default [note1];
