export const note1 = {
    title: "计算机组成原理（四）：总线与I/O系统",
    contentmd: `
# 计算机组成原理（四）· 总线与I/O系统

> 定位：408考研复习第四章。重点掌握：总线分类与仲裁、总线周期、I/O控制器、I/O编址方式（统一/独立）、I/O控制方式（程序查询/程序中断/DMA）、中断系统（中断向量/多重中断/断点保护）、DMA方式（控制器/传输三阶段）。

---

## 一、总线基本概念

### 1. 总线定义

- **总线**：连接多个部件的信息传输线，是各部件共享的传输介质。
- **特点**：分时共享（同一时刻只能一对主设备从设备通信）。

### 2. 总线分类

**按数据传送方向**：
| 类型 | 方向 | 用途 |
|------|------|------|
| 单向总线 | 单方向 | 地址总线 |
| 双向总线 | 双方向 | 数据总线 |

**按数据传输位数**：
| 类型 | 说明 |
|------|------|
| 串行总线 | 一位一位传输，线少，长距离 |
| 并行总线 | 多位同时传输，线多，短距离 |

**按总线位置与功能**（重点）：
| 类型 | 连接对象 | 例子 |
|------|---------|------|
| **片内总线** | 芯片内部各部件 | CPU内寄存器之间 |
| **系统总线** | 计算机各大部件间 | CPU、主存、I/O之间 |
| **通信总线** | 计算机之间 | USB、网络线 |

### 3. 系统总线三类（必考）

| 总线 | 方向 | 功能 |
|------|------|------|
| **数据总线** | 双向 | 传输数据，宽度决定每次传输位数 |
| **地址总线** | 单向（CPU→主存/I/O） | 传输地址，宽度决定可寻址空间 |
| **控制总线** | 双向 | 传输控制信号（读、写、中断请求等） |

- 地址总线宽度 n 位 → 可寻址 2^n 个存储单元。
- 数据总线宽度 n 位 → 一次传输 n 位数据。

### 4. 系统总线结构

| 结构 | 特点 | 适用 |
|------|------|------|
| **单总线结构** | 一条总线连接CPU、主存、I/O | 简单但速度慢 |
| **双总线结构** | 主存总线 + I/O总线（通道） | 缓解单总线瓶颈 |
| **三总线结构** | 主存总线 + I/O总线 + DMA总线 | 高速I/O直连主存 |

---

## 二、总线仲裁

### 1. 总线主从设备

- **总线主设备**：可发出总线请求、获得总线控制权的设备（如CPU、DMA控制器）。
- **总线从设备**：被主设备访问的设备，不能主动发起传输（如主存）。

### 2. 总线仲裁方式

| 仲裁方式 | 说明 | 特点 |
|---------|------|------|
| **集中式仲裁** | 由总线仲裁器集中控制 | 分链式查询、计数器定时查询、独立请求方式 |
| **分布式仲裁** | 各模块自带仲裁逻辑，无需中央仲裁器 | 灵活但复杂 |

### 3. 集中式仲裁三种方式（重点）

| 方式 | 实现 | 优缺点 |
|------|------|--------|
| **链式查询** | 共用总线请求BR、总线授权BG串行传递 | 简单、扩充方便；但优先级固定、对电路故障敏感 |
| **计数器定时查询** | 仲裁器内计数器，从当前值开始查询各设备 | 优先级可灵活设置；线数较多 |
| **独立请求方式** | 每个设备有独立的请求线和授权线 | 响应快、优先级灵活；但控制线数多 |

### 4. 总线仲裁优先级

- **链式查询**：离仲裁器越近优先级越高（固定）。
- **计数器定时查询**：计数器初值决定优先级（可变）。
- **独立请求方式**：仲裁器内部可编程决定优先级（最灵活）。

---

## 三、总线通信方式与周期

### 1. 总线通信方式

| 方式 | 说明 | 特点 |
|------|------|------|
| **同步通信** | 双方使用统一时钟，按固定时序工作 | 简单，但要求各部件速度一致 |
| **异步通信** | 不共用时钟，用握手信号（请求/应答）协调 | 灵活，速度匹配适应性强 |
| **半同步通信** | 同步基础上加等待信号WAIT | 折中方案 |
| **分离式通信** | 总线使用分为请求和应答两阶段，期间总线可给其他设备用 | 总线利用率最高 |

### 2. 异步通信三种握手方式

| 类型 | 握手方式 | 适用 |
|------|---------|------|
| **不互锁** | 主设备发请求后等待固定时间，不要求从设备应答 | 速度最快 |
| **半互锁** | 主设备请求后等待从设备应答才撤销请求 | 主从双方等待 |
| **全互锁** | 主从双方互相应答，信号相互确认 | 最可靠 |

### 3. 总线周期

- **总线周期**：完成一次总线操作的时间。
- **总线周期 = 总线申请 + 总线仲裁 + 寻址 + 数据传送 + 释放**

### 4. 总线带宽计算

- **总线带宽** = 总线宽度 × 总线工作频率
- 例：总线宽度32位，工作频率66MHz → 带宽 = 4B × 66M/s = 264 MB/s
- 单位换算：1 Byte = 8 bit

---

## 四、I/O系统基础

### 1. I/O系统组成

**I/O系统 = I/O软件 + I/O硬件**

- **I/O硬件**：I/O设备 + I/O接口 + 设备控制器。
- **I/O软件**：驱动程序、I/O控制程序等。

### 2. I/O设备分类

| 类型 | 例子 |
|------|------|
| 输入设备 | 键盘、鼠标、扫描仪 |
| 输出设备 | 显示器、打印机、绘图仪 |
| 输入输出设备 | 磁盘、磁带、网卡、SSD |

### 3. I/O接口（I/O控制器）

- **作用**：连接主机与外设，协调二者速度差异、数据格式转换。
- **主要功能**：
  - 数据缓冲
  - 状态/控制信号转换
  - 数据格式转换（串并转换、电平转换）
  - 地址译码（设备选择）
  - 中断管理

### 4. I/O接口组成

| 部件 | 功能 |
|------|------|
| **数据缓冲寄存器** | 暂存数据 |
| **状态寄存器** | 记录设备状态（忙、就绪、错误） |
| **控制寄存器** | 接收CPU控制命令 |
| **设备地址译码器** | 选择具体设备 |
| **数据总线缓冲** | 与系统数据总线连接 |
| **控制逻辑** | 实现接口内部时序 |

### 5. I/O端口编址方式（重点）

| 编址方式 | 说明 | 优缺点 |
|---------|------|--------|
| **统一编址（存储器映射编址）** | I/O端口与主存单元共享同一地址空间 | 不需专门的I/O指令，但占用主存空间；RISC常用 |
| **独立编址（I/O映射编址）** | I/O端口地址空间与主存地址空间独立 | 需专门的I/O指令（IN/OUT）；CISC（x86）常用 |

- **统一编址**：可用访存指令访问I/O端口。
- **独立编址**：必须用专用I/O指令访问。

---

## 五、I/O控制方式（高频考点）

### 1. 三种控制方式对比

| 控制方式 | 数据传输方式 | CPU介入 | 速度 | 效率 |
|---------|------------|---------|------|------|
| **程序查询** | CPU主动查询外设状态，就绪后传输 | 全程参与 | 慢 | 低 |
| **程序中断** | 外设准备好后发中断请求，CPU响应后传输 | 中断时参与 | 中 | 中 |
| **DMA** | DMA控制器直接控制数据传输，CPU仅在开头和结尾参与 | 仅开始和结束参与 | 快 | 高 |

### 2. 程序查询方式

- **工作流程**：
  1. CPU向I/O接口发命令字。
  2. CPU循环读取状态寄存器，等待设备就绪。
  3. 设备就绪后，CPU从数据缓冲寄存器读/写数据。
- **特点**：实现简单，但CPU与外设串行工作，效率极低。

### 3. 程序中断方式

**工作流程**：
1. CPU向I/O接口发启动命令。
2. CPU去执行其他程序。
3. I/O设备准备好数据后，向CPU发中断请求。
4. CPU响应中断，保存现场，执行中断服务程序。
5. 中断服务程序完成数据传输。
6. 恢复现场，返回原程序。

### 4. DMA方式

- **DMA（直接存储器存取）**：由DMA控制器直接控制I/O设备与主存之间的数据交换，CPU不参与传输过程。
- **特点**：高速数据传输，CPU只在传输开始和结束时参与。

---

## 六、中断系统

### 1. 中断基本概念

- **中断**：CPU在执行程序时，由于内外部事件，暂停当前程序，转去处理该事件，处理完再返回原程序。
- **中断源分类**：
  - **内部中断（异常）**：由CPU内部产生，如除零、溢出、缺页、非法指令。
  - **外部中断（硬件中断）**：由外部设备产生，如I/O中断、时钟中断、电源故障。
- **自愿中断**：由程序主动发出（如系统调用、陷阱指令TRAP）。
- **强迫中断**：由硬件故障或外部事件强制产生。

### 2. 中断处理过程

    1. 中断请求：中断源发出请求信号
    2. 中断响应条件：
       - 中断允许触发器开（中断未被屏蔽）
       - 当前指令执行完毕
       - 优先级高于当前中断
    3. 保护断点：PC和PSW压栈
    4. 寻找中断服务程序入口：通过中断向量
    5. 保护现场：通用寄存器压栈
    6. 执行中断服务程序
    7. 恢复现场：弹栈恢复寄存器
    8. 中断返回：IRET指令，PC和PSW弹栈，返回原程序

### 3. 中断向量与中断向量表

| 概念 | 含义 |
|------|------|
| **中断向量** | 中断服务程序入口地址（含PC和PSW） |
| **中断向量表** | 存放中断向量的表，由中断号索引 |
| **中断类型号** | 系统给每个中断源的编号 |
| **向量地址** | 中断向量在中断向量表中的位置 |

- **向量中断**：硬件自动根据中断类型号从向量表取出入口地址，速度快。
- **非向量中断**：软件查询中断源，逐个判断处理。

### 4. 多重中断（嵌套中断）

- **多重中断**：CPU在处理某中断时，又响应更高优先级中断。
- **条件**：当前中断未屏蔽、新中断优先级更高。
- **实现**：在保护现场后开中断（即允许中断嵌套）。
- **屏蔽技术**：每个中断源对应一个屏蔽位，可通过编程改变屏蔽字，灵活调整优先级。

### 5. 中断优先级

- **硬件排队链**：硬件决定优先级，固定不可变。
- **软件排队**：按顺序查询中断源，靠前的优先级高。
- **多重中断中**：响应优先级（硬件固定）vs 屏蔽优先级（可编程改变）。

### 6. 断点保护

- **断点**：CPU被中断时下一条指令的地址（PC内容）和程序状态（PSW）。
- **保护方法**：
  - **硬件自动压栈**：CPU响应中断时自动将PC和PSW压栈。
  - **软件保存**：中断服务程序中保存通用寄存器内容。

### 7. 中断响应周期完成的工作

1. 关中断（防止保存断点过程被打断）。
2. 保存断点（PC、PSW压栈）。
3. 引出中断服务程序（PC装入中断服务程序入口地址）。

---

## 七、DMA方式（重点）

### 1. DMA基本概念

- **DMA（Direct Memory Access）**：直接存储器存取，由DMA控制器直接控制I/O设备与主存之间的数据传输。
- **特点**：CPU只在数据传输开始和结束时参与，传输过程中CPU可执行其他程序（但不能访存）。
- **主存与DMA控制器之间的数据通路**：系统总线（与CPU共享）。

### 2. DMA控制器组成

| 部件 | 功能 |
|------|------|
| **主存地址寄存器 MAR** | 存放主存地址，每传输一个数据自动加1 |
| **数据计数器 DC** | 存放待传输数据个数，每传输一个自动减1 |
| **数据缓冲寄存器 DBR** | 暂存传输的数据 |
| **设备地址寄存器 DAR** | 存放I/O设备的地址 |
| **控制/状态逻辑** | 控制DMA工作方式、状态 |
| **中断机构** | 数据传输完成后向CPU发中断请求 |

### 3. DMA传输过程三阶段

| 阶段 | 工作 | 完成者 |
|------|------|--------|
| **预处理（预置）** | CPU初始化DMA控制器：设置主存地址、设备地址、数据个数、读/写命令，启动设备 | CPU |
| **数据传输** | DMA控制器接管总线，控制I/O设备与主存直接传输数据，CPU可执行其他不访存指令 | DMA控制器 |
| **后处理** | 传输完成后DMA向CPU发中断请求，CPU执行后处理：校验数据、决定是否继续、释放总线 | CPU |

### 4. DMA数据传输方式（总线使用权分配）

| 方式 | 说明 | 特点 |
|------|------|------|
| **停止CPU访存（周期窃取）** | DMA传输时CPU停止访存 | 简单，但CPU效率低 |
| **周期挪用（周期窃取）** | DMA每传输一个数据窃取一个总线周期 | CPU工作与DMA交替，效率高 |
| **DMA与CPU交替访问** | DMA与CPU分时使用总线 | 总线利用率最高，硬件复杂 |

### 5. DMA与中断对比（重点）

| 对比项 | 中断方式 | DMA方式 |
|--------|---------|---------|
| 数据传输者 | CPU | DMA控制器 |
| CPU介入程度 | 数据传输时介入 | 仅传输前后介入 |
| 响应时间 | 指令执行周期结束 | 总线周期结束（机器周期） |
| 数据传输单位 | 字/字节 | 块（多个字） |
| 适用场合 | 低速设备（键盘、打印机） | 高速设备（磁盘、网卡） |
| 异常处理 | 可处理复杂逻辑 | 仅数据传输 |
| 优先级 | 较低 | 较高（避免高速设备数据丢失） |

### 6. DMA的工作流程

    1. CPU预处理DMA：设置主存地址、数据量、设备地址、方向
    2. CPU启动I/O设备，转去执行其他程序
    3. I/O设备准备好后向DMA发DMA请求
    4. DMA向CPU发总线请求BR，CPU让出总线
    5. DMA控制总线，直接在I/O与主存间传输数据
    6. 每传输一个数据，地址+1，计数-1
    7. 计数到0时，DMA向CPU发中断请求
    8. CPU响应中断，做后处理（数据校验等）

---

## 八、I/O控制方式进阶

### 1. 通道方式

- **通道**：一个独立于CPU的专用I/O处理机，能执行通道指令。
- **类型**：
  - **字节多路通道**：连接多台低速设备，分时传输字节。
  - **选择通道**：一次只服务一台高速设备，独占方式。
  - **数组多路通道**：结合上面两者，连接多台高速设备分时传输数据块。

### 2. I/O处理机方式

- **IOP（I/O Processor）**：比通道更独立，有自己的指令系统，可独立执行I/O程序。

### 3. 四种I/O控制方式对比

| 控制方式 | CPU利用率 | 数据传输单位 | 适用设备 |
|---------|---------|------------|---------|
| 程序查询 | 极低 | 字 | 慢速设备 |
| 程序中断 | 中 | 字 | 低速设备 |
| DMA | 高 | 块 | 高速设备 |
| 通道 | 最高 | 块 | 多台高速设备 |

---

## 九、典型计算题套路

### 1. 总线带宽计算

- 总线带宽 = 总线宽度 × 总线工作频率
- 例：32位总线，工作频率100MHz → 带宽 = 4B × 100×10^6/s = 400 MB/s

### 2. 中断响应时间

- 中断响应时间 = 中断请求发出 → CPU响应间隔
- 通常 = 当前指令周期 + 中断响应周期

### 3. DMA传输时间

- DMA传输时间 = 数据块大小 / DMA传输速率
- CPU让出总线时间比例 = DMA传输时间 / 总时间

### 4. I/O端口地址译码

- 给定地址线数 n → 可寻址 I/O 端口数 = 2^n
- 全译码：所有地址线参与译码
- 部分译码：部分地址线参与译码，地址空间有重叠

### 5. 中断嵌套判断

- 是否嵌套：新中断优先级 > 当前优先级
- 屏蔽字：屏蔽位为1表示屏蔽对应优先级中断

---

## 十、速通口诀

- **总线三类**：数据双向传、地址单向CPU发、控制双向夹。
- **总线仲裁**：集中式三式——链式查询优先级固定、计数器查询可变、独立请求最灵活。
- **总线通信**：同步统一时钟、异步握手应答、分离式最高效。
- **总线带宽**：宽度 × 频率，简单相乘。
- **I/O端口编址**：统一编址用访存指令、独立编址用IN/OUT。
- **I/O控制三式**：查询CPU全程等、中断CPU打断接、DMA控制器自己干。
- **中断三要素**：中断源、中断向量（入口地址）、中断服务程序。
- **多重中断条件**：开中断、新优先级更高。
- **中断响应三步**：关中断、保护断点、引出服务程序。
- **DMA三阶段**：预处理CPU初始化、数据传输DMA干、后处理CPU收尾。
- **DMA vs 中断**：DMA传块、中断传字；DMA优先级更高，避免高速设备丢数据。
- **DMA核心五部件**：MAR主存地址、DC计数器、DBR缓冲、DAR设备地址、控制逻辑。
- **通道四式**：字节多路低速分时、选择通道高速独占、数组多路折中。
`,
    timestamp: "2026-08-06 18:30",
    embed: ``
};

export default [note1];
